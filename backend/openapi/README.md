# OpenAPI contract boundary

This directory is reserved for the versioned local-sidecar OpenAPI document and
JSON Schemas. P001 intentionally defines no endpoint or DTO contract; those
files are added only by the contract-freezing task and must remain compatible
with the P00 contract baseline.

OpenAI Codex v0.121.0 (research preview)
--------
workdir: D:\project\GitHub\autoplan
model: gpt-5.5
provider: custom
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019efb28-6248-76e3-80ec-cf9cc9e5f9df
--------
user
你是开发执行者。
请只执行指定任务 P002，不要提前执行其它任务，也不要顺手处理其它 checkbox。

plan 文件（只读）：D:\project\GitHub\autoplan\docs\plan\plan_requirement_7_20260625-033918.md
指定任务：
- [ ] P002: 保留任务同步中的上下文 <!-- scope: src/loopService.js -->
任务 scope：src/loopservice.js

完成后必须：
- 当前为并发执行模式，不要读写其它任务的 scope
- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区
- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区
- 只修改当前任务 scope 直接相关的业务文件
- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试
- 只有当前任务本身明确要求全量验证时，才允许运行全量测试
- 不输出完整 diff、源码全文或长文件列表
- 中文文件读写使用 UTF-8
**Planning tool execution**

I need to be cautious because the system mentions a "Before tool calls preamble." I’m thinking about whether update_plan is also part of this tool commentary, but I'm not completely sure. It seems like we can send text, so I should use commentary effectively. Since the user is asking for development execution, I’ll plan to keep things concise in Chinese. Let's make this straightforward and clear!
codex
我会只查看只读计划和 `src/loopService.js`，确认 P002 的具体改动点后再做最小修改。
  → 读取 P002 上下文
  • 修改任务同步逻辑
  • 做最小语法验证
exec
powershell.exe -Command "Get-Content -LiteralPath 'D:\\project\\GitHub\\autoplan\\docs\\plan\\plan_requirement_7_20260625-033918.md' -Encoding UTF8; Write-Output '---TARGET---'; Get-Content -LiteralPath 'D:\\project\\GitHub\\autoplan\\src\\loopService.js' -Encoding UTF8" in D:\project\GitHub\autoplan
 succeeded in 288ms:
# 需求 #7 开发计划：同一 Plan 任务持续使用同一上下文

## 背景

当前 AutoPlan 在执行 plan 任务时，每次调用 Codex 都会通过 `codex exec` 启动一个新的非交互式会话。对于同一个 plan 任务，如果因为失败、停止、重试或手动再次执行而需要继续推进，新的会话无法继承该任务上一次的理解、已做分析和执行上下文，容易造成重复探索、遗漏前序约束或与上一轮执行结论不一致。

需求 #7 要求：同一个 plan 任务应该在同一个上下文内部持续执行。实现目标是以 `plan_id + task_key` 为任务上下文边界，首次执行创建 Codex 会话，后续同一任务继续执行时恢复该会话；不同任务仍保持隔离，避免上下文串扰。

## 任务拆解

- [ ] P001: 持久化任务上下文标识 <!-- scope: src/database.js,src/renderer/types.ts -->
  - 验收要点：`plan_tasks` 表新增可空的 Codex 会话标识字段，能够记录同一任务首次执行产生的 session id。
  - 验收要点：新增字段通过 `ensureColumn` 兼容历史 SQLite 数据库，旧任务缺失会话标识时不影响启动、快照和任务列表展示。
  - 验收要点：前端 `PlanTask` 类型同步新增上下文字段，现有任务耗时、状态、scope、排序字段类型不回退。
  - 验收要点：会话标识仅绑定到具体 `plan_tasks` 记录或 `plan_id + task_key`，不使用全局单例存储，避免跨 plan 或跨任务复用。

- [ ] P002: 保留任务同步中的上下文 <!-- scope: src/loopService.js -->
  - 验收要点：`syncPlanTasks` 在重新解析 plan 文件时保留同一 `plan_id + task_key` 已有任务的 Codex 会话标识、耗时字段和关键状态信息。
  - 验收要点：任务标题、raw line、scope 或排序变化时仍能更新最新解析结果，但不会因为删除重建任务而丢失上下文。
  - 验收要点：任务 key 被移除或改名时不把旧会话错误迁移到新任务；新增任务默认没有会话标识。
  - 验收要点：已完成任务的 checkbox 与进度区由现有 AutoPlan 回写流程继续负责，上下文保留逻辑不扩大 plan 文件写入范围。

- [ ] P003: 支持 Codex 会话恢复执行 <!-- scope: src/loopService.js -->
  - 验收要点：`runCodex` 支持传入已有 session id；存在 session id 时使用 `codex exec resume <session_id> -` 继续会话，不再为同一任务创建全新上下文。
  - 验收要点：没有 session id、session id 为空或恢复失败时能够回退为新建 `codex exec`，并记录可排查事件，不导致任务流程卡死。
  - 验收要点：新建会话时继续保留当前 `--cd`、`--color never`、`--output-last-message`、沙箱和 stdin prompt 行为；恢复会话时不传入 Codex CLI 不支持的参数。
  - 验收要点：从 Codex 输出中捕获真实 session id，写入运行结果，日志文件和 last message 文件路径仍按现有规则生成。

- [ ] P004: 绑定任务执行与上下文生命周期 <!-- scope: src/loopService.js -->
  - 验收要点：`executeTask` 执行前读取当前任务保存的 session id，并把它传给 Codex 调用；执行结束后把捕获到的 session id 写回对应任务。
  - 验收要点：同一任务失败后回到 `pending`、被停止后再次执行、或用户手动点击运行时，都会复用该任务已有上下文。
  - 验收要点：不同任务即使属于同一 plan，也不会复用彼此的 session id；并发任务各自维护独立上下文。
  - 验收要点：任务成功完成后保留 session id 作为审计信息，但不会让后续其他任务自动继承该上下文。

- [ ] P005: 暴露上下文状态与事件信息 <!-- scope: src/loopService.js,src/renderer/types.ts,src/renderer/pages/WorkspacePage.tsx,src/renderer/components/CodexLog.tsx -->
  - 验收要点：`snapshot()` 的 `tasks`、`activeOperation`、`activeOperations` 和 `lastOperation` 能携带当前任务的 session id 或“新建/恢复”状态。
  - 验收要点：事件流在任务开始、恢复、回退新建、失败和完成时包含可读的上下文信息，方便定位某次任务是否复用了上下文。
  - 验收要点：工作区 Codex 日志区域能展示当前执行是新会话还是恢复会话，不影响原有 logTail 和 activity 展示。
  - 验收要点：界面只显示必要的短标识或状态文案，避免长 UUID 造成布局溢出；无 session id 的历史任务显示为普通任务。

- [ ] P006: 补充上下文复用回归测试 <!-- scope: scripts/smoke-test.js -->
  - 验收要点：smoke 测试覆盖同一任务首次执行产生 session id，并在失败后再次执行时传入同一个 session id。
  - 验收要点：smoke 测试覆盖同一 plan 的不同任务不会复用 session id，并发执行时每个任务拥有独立上下文。
  - 验收要点：smoke 测试覆盖 `syncPlanTasks` 后同一 `task_key` 的上下文仍保留，新增或改名任务不会继承旧上下文。
  - 验收要点：现有 plan guard、scope 并发选择、任务完成回写、验收完成和耗时统计断言继续通过。

- [ ] P007: 执行构建与端到端验证 <!-- scope: package.json,scripts/smoke-test.js,src/database.js,src/loopService.js,src/renderer/types.ts,src/renderer/pages/WorkspacePage.tsx,src/renderer/components/CodexLog.tsx -->
  - 验收要点：`npm run check` 通过，确认 TypeScript、Node 语法检查和脚本检查无错误。
  - 验收要点：`npm run smoke` 通过，确认任务上下文复用、计划解析、执行、停止、完成和验收流程无回归。
  - 验收要点：手动验证同一任务连续两次执行时日志显示恢复同一 session，不同任务显示不同 session 或新建上下文。
  - 验收要点：如 Codex CLI 恢复会话不可用，系统能清晰记录回退事件并继续新建会话执行，不出现无限重试或 UI 卡住。

## 总体验收标准

- 上下文连续：同一个 plan 任务在失败、停止、重试或手动再次运行时，优先恢复该任务上一次的 Codex 会话继续执行。
- 任务隔离：上下文以 `plan_id + task_key` 为边界，不同任务、不同 plan、不同项目之间不会共享会话。
- 数据可靠：会话标识持久化到 SQLite，并在 plan 文件重新同步时保留；历史数据和无会话任务保持兼容。
- 流程稳定：plan guard、任务状态流转、并发 scope 控制、日志输出、进度区回写和验收流程保持原有行为。
- 可观测：事件流、活动日志或任务快照能够判断本次执行是新建上下文还是恢复上下文，并能定位关联 session。
- 验证通过：完成实现后 `npm run check` 与 `npm run smoke` 均通过，必要时补充手动验证记录。

## 进度区

- 2026-06-25 AutoPlan 生成：需求 #7 开发计划已创建，待执行。
---TARGET---
const crypto = require('node:crypto');
const { EventEmitter } = require('node:events');
const fs = require('node:fs');
const path = require('node:path');
const { spawn } = require('node:child_process');
const { nowIso } = require('./database');
const { CodexActivityPrinter } = require('./codexActivity');

const ACTIVE_RUNTIME_PHASES = new Set(['running', 'scan', 'generate-plan', 'execute-task', 'validate']);
const ACTIVE_RUNTIME_PHASE_SQL = "('running','scan','generate-plan','execute-task','validate')";
const MAX_PARALLEL_TASKS = 2;
const PARALLEL_BLOCKING_TASK_RE = /(全量|回归|验证|验收|测试|记录|整理|发布|部署|test|validate|regression|release|deploy)/i;
const TASK_SCOPE_LABEL_RE = '(?:scope|scopes|files?|影响范围|并发键)';
const TASK_SCOPE_RE = new RegExp(`${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>\\]\\n]+)`, 'i');
const TASK_SCOPE_COMMENT_RE = new RegExp(`\\s*<!--\\s*${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>]*?)\\s*-->\\s*`, 'i');
const TASK_SCOPE_SPLIT_RE = /[,，、;；]+/;
const TASK_PATH_RE = /[\w./\\-]+\.(?:dart|js|jsx|ts|tsx|css|scss|html|md|json|ya?ml)/gi;

const TASK_EVENT_TYPES = Object.freeze({
  STARTED: 'task.started',
  SUCCEEDED: 'task.succeeded',
  FAILED: 'task.failed',
  STOP_REQUESTED: 'task.stop.requested',
  STOPPED: 'task.stopped',
  INTERRUPTED: 'task.interrupted',
});

const LEGACY_TASK_EVENT_TYPES = Object.freeze({
  EXECUTED: 'task.executed',
  STOPPING: 'task.stopping',
});

const TASK_EVENT_STATUS = Object.freeze({
  PENDING: 'pending',
  RUNNING: 'running',
  COMPLETED: 'completed',
  FAILED: 'failed',
  STOPPING: 'stopping',
  STOPPED: 'stopped',
  INTERRUPTED: 'interrupted',
});

const TASK_EVENT_SEMANTICS = Object.freeze({
  [TASK_EVENT_TYPES.STARTED]: Object.freeze({ status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务' }),
  [TASK_EVENT_TYPES.SUCCEEDED]: Object.freeze({ status: TASK_EVENT_STATUS.COMPLETED, label: '结束了任务' }),
  [TASK_EVENT_TYPES.FAILED]: Object.freeze({ status: TASK_EVENT_STATUS.FAILED, label: '任务失败' }),
  [TASK_EVENT_TYPES.STOP_REQUESTED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPING, label: '请求停止任务' }),
  [TASK_EVENT_TYPES.STOPPED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务' }),
  [TASK_EVENT_TYPES.INTERRUPTED]: Object.freeze({ status: TASK_EVENT_STATUS.INTERRUPTED, label: '已中断任务' }),
});

const TASK_EVENT_COMPATIBILITY = Object.freeze({
  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,
  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,
});

class LoopService extends EventEmitter {
  constructor(db) {
    super();
    this.db = db;
    this.runtimes = new Map();
    this.resetRuntimeState();
  }

  runtime(projectId) {
    const id = Number(projectId || 0);
    if (!id) return null;
    let runtime = this.runtimes.get(id);
    if (!runtime) {
      runtime = createProjectRuntime();
      this.runtimes.set(id, runtime);
    }
    return runtime;
  }

  existingRuntime(projectId) {
    return this.runtimes.get(Number(projectId || 0)) || null;
  }

  projects() {
    return this.db
      .all('SELECT * FROM projects ORDER BY updated_at DESC, id DESC')
      .map((project) => this.withProjectRuntimeSummary(project));
  }

  withProjectRuntimeSummary(project) {
    const state =
      this.db.get(
        'SELECT phase, interval_seconds, validation_command FROM project_states WHERE project_id = ?',
        [project.id],
      ) || {};
    const runtime = this.existingRuntime(project.id);
    const running = runtime?.running ? 1 : 0;
    let phase = state.phase || 'idle';
    if (!running && !runtime?.busy && ACTIVE_RUNTIME_PHASES.has(String(phase || ''))) {
      phase = 'stopped';
    }
    return {
      ...project,
      running,
      phase,
      interval_seconds: Number(state.interval_seconds || 5),
      validation_command: state.validation_command || '',
    };
  }

  project(projectId) {
    if (!projectId) return null;
    return this.db.get('SELECT * FROM projects WHERE id = ?', [projectId]);
  }

  defaultProjectId() {
    return this.projects()[0]?.id || null;
  }

  ensureProjectState(projectId) {
    if (!projectId) return;
    this.db.run(
      `INSERT OR IGNORE INTO project_states
       (project_id, running, phase, interval_seconds, validation_command, updated_at)
       VALUES (?, 0, 'idle', 5, 'flutter analyze', ?)`,
      [projectId, nowIso()],
    );
  }

  resetRuntimeState() {
    const now = nowIso();
    this.db.run(
      `UPDATE project_states
       SET running = 0,
           phase = CASE WHEN phase IN ${ACTIVE_RUNTIME_PHASE_SQL} THEN 'stopped' ELSE phase END,
           updated_at = ?
       WHERE running != 0 OR phase IN ${ACTIVE_RUNTIME_PHASE_SQL}`,
      [now],
    );
    this.db.run(
      `UPDATE loop_state
       SET running = 0,
           phase = CASE WHEN phase IN ${ACTIVE_RUNTIME_PHASE_SQL} THEN 'stopped' ELSE phase END,
           updated_at = ?
       WHERE id = 1 AND (running != 0 OR phase IN ${ACTIVE_RUNTIME_PHASE_SQL})`,
      [now],
    );
    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE status = ?', ['pending', now, 'running']);
  }

  status(projectId = this.defaultProjectId()) {
    if (!projectId) return null;
    this.ensureProjectState(projectId);
    return this.normalizeRuntimeStatus(projectId, this.db.get('SELECT * FROM project_states WHERE project_id = ?', [projectId]));
  }

  normalizeRuntimeStatus(projectId, state) {
    if (!state) return null;
    const runtime = this.existingRuntime(projectId);
    const runtimeRunning = Boolean(runtime?.running);
    const normalized = {
      ...state,
      running: runtimeRunning ? 1 : 0,
    };

    if (!runtimeRunning && !runtime?.busy && ACTIVE_RUNTIME_PHASES.has(String(normalized.phase || ''))) {
      normalized.phase = 'stopped';
    }

    return normalized;
  }

  configure(projectId, { workspacePath, intervalSeconds, validationCommand }) {
    const current = this.status(projectId);
    const project = this.project(projectId);
    const runtime = this.existingRuntime(projectId);
    const nextWorkspace = workspacePath ?? project?.workspace_path;
    const workspaceOwner = this.activeProjectForWorkspace(nextWorkspace, projectId);
    if ((runtime?.running || runtime?.busy) && workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }
    if (!project || !current) throw new Error('项目不存在');

    this.db.run(
      `UPDATE projects
       SET workspace_path = ?, updated_at = ?
       WHERE id = ?`,
      [nextWorkspace, nowIso(), projectId],
    );
    const nextInterval = Number(intervalSeconds || current.interval_seconds || 5);
    this.db.run(
      `UPDATE project_states
       SET interval_seconds = ?, validation_command = ?, updated_at = ?
       WHERE project_id = ?`,
      [
        nextInterval,
        validationCommand ?? current.validation_command,
        nowIso(),
        projectId,
      ],
    );
    if (runtime?.running) this.scheduleProject(projectId, nextInterval);
    this.emitUpdate(projectId);
  }

  scheduleProject(projectId, intervalSeconds) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.timer) clearInterval(runtime.timer);
    runtime.timer = setInterval(() => {
      if (!runtime.running) return;
      this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    }, Math.max(1, Number(intervalSeconds || 5)) * 1000);
  }

  activeProjectForWorkspace(workspace, projectId) {
    const key = workspaceKey(workspace);
    if (!key) return null;
    for (const [id, runtime] of this.runtimes.entries()) {
      if (Number(id) === Number(projectId)) continue;
      if (!runtime.running && !runtime.busy && !runtime.activeChild) continue;
      const project = this.project(id);
      if (workspaceKey(project?.workspace_path) === key) return project;
    }
    return null;
  }

  start(projectId) {
    const state = this.status(projectId);
    const project = this.project(projectId);
    if (!project || !state) throw new Error('项目不存在');
    if (!project.workspace_path) throw new Error('请先设置项目工作区路径');
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.running) return;
    const workspaceOwner = this.activeProjectForWorkspace(project.workspace_path, projectId);
    if (workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }

    runtime.running = true;
    this.db.run(
      'UPDATE project_states SET running = 1, phase = ?, updated_at = ? WHERE project_id = ?',
      ['running', nowIso(), projectId],
    );
    this.emitUpdate(projectId);
    this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    this.scheduleProject(projectId, state.interval_seconds);
  }

  stop(projectId = null) {
    if (!projectId) {
      for (const id of Array.from(this.runtimes.keys())) this.stop(id);
      return;
    }
    const runtime = this.runtime(projectId);
    if (runtime?.timer) clearInterval(runtime.timer);
    if (runtime) {
      runtime.timer = null;
      runtime.running = false;
    }
    if (runtime?.activeOperations?.size) {
      for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {
        const child = runtime.activeChildren.get(operationKey);
        const activeTaskId = operation?.taskId || null;
        killChildProcess(child);
        const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
        if (activeTaskId) {
          this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [
            'pending',
            nowIso(),
            activeTaskId,
          ]);
        }
        this.addEvent(
          projectId,
          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
          operation?.label || '',
          activeTask
            ? taskEventMeta(activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIso() })
            : null,
        );
      }
    }
    this.db.run(
      'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',
      ['stopped', nowIso(), projectId],
    );
    this.addEvent(projectId, 'loop.stopped', '循环已停止');
    this.emitUpdate(projectId);
  }

  async runOnce(projectId = this.defaultProjectId()) {
    if (!projectId) return;
    const runtime = this.runtime(projectId);
    if (!runtime || runtime.busy) return;
    runtime.busy = true;
    try {
      const project = this.project(projectId);
      const workspace = project?.workspace_path;
      if (!workspace) return;
      this.setPhase(projectId, 'scan');
      this.ensureWorkspaceDirs(workspace);

      // 扫描未完成、未中断、尚未生成计划的需求和反馈
      const pendingRequirements = this.db
        .all(
          `SELECT * FROM requirements
           WHERE project_id = ? AND linked_plan_id IS NULL
             AND status NOT IN ('completed', 'closed')
           ORDER BY created_at ASC`,
          [projectId],
        )
        .map((row) => ({ ...row, __type: 'requirement' }));
      const pendingFeedback = this.db
        .all(
          `SELECT * FROM feedback
           WHERE project_id = ? AND linked_plan_id IS NULL
             AND status NOT IN ('completed', 'closed')
           ORDER BY created_at ASC`,
          [projectId],
        )
        .map((row) => ({ ...row, __type: 'feedback' }));
      const pendingIntakes = [...pendingRequirements, ...pendingFeedback];
      this.addEvent(projectId, 'scan.done', `待处理需求/反馈=${pendingIntakes.length}`);

      // 一次只生成一个计划（保持串行，避免 codex 并发），剩余等下一轮 timer
      let generatedPlanId = null;
      if (pendingIntakes.length > 0) {
        generatedPlanId = await this.generatePlanForIntake(projectId, workspace, pendingIntakes[0]);
      }

      // 同步 docs/plan 目录下的 plan 文件（兼容文件式需求）
      const planScan = this.scanDirectory(path.join(workspace, 'docs', 'plan'), workspace, ['.md']);
      this.saveScan(projectId, 'plan', planScan);

      // 执行队列里可运行的 plan
      const nextPlan = generatedPlanId
        ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [generatedPlanId, projectId])
        : this.nextRunnablePlan(projectId);
      if (!nextPlan) {
        if (runtime.running || this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
          this.setPhase(projectId, pendingIntakes.length > 0 && runtime.running ? 'waiting' : 'idle');
        }
        return;
      }

      await this.processPlan(workspace, nextPlan);
      if (runtime.running) {
        this.setPhase(projectId, 'waiting');
      } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
        this.setPhase(projectId, 'idle');
      }
    } finally {
      runtime.busy = false;
      this.emitUpdate(projectId);
    }
  }

  async runTask(projectId, taskId) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.busy) throw new Error('该项目已有任务正在执行，请稍后再试');
    const project = this.project(projectId);
    const task = this.taskForProject(projectId, taskId);
    const plan = task ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [task.plan_id, projectId]) : null;
    const workspace = project?.workspace_path;
    if (!project || !task || !plan) throw new Error('任务不存在');
    if (!workspace) throw new Error('请先设置项目工作区路径');
    const workspaceOwner = this.activeProjectForWorkspace(workspace, projectId);
    if (workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }

    runtime.busy = true;
    try {
      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), taskId]);
      this.emitUpdate(projectId);
      const result = await this.executeTask(workspace, plan, task);
      if (result.exitCode === 0) {
        this.completeTask(workspace, plan, task, result);
      }
      if (runtime.running) {
        this.setPhase(projectId, 'waiting');
      } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
        this.setPhase(projectId, 'idle');
      }
    } finally {
      runtime.busy = false;
      this.emitUpdate(projectId);
    }
  }

  stopTask(projectId, taskId) {
    const task = this.taskForProject(projectId, taskId);
    if (!task) throw new Error('任务不存在');
    const runtime = this.runtime(projectId);

    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));
    if (activeEntry) {
      killChildProcess(activeEntry.child);
      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), taskId]);
      if (!runtime.running) {
        this.addEvent(
          projectId,
          LEGACY_TASK_EVENT_TYPES.STOPPING,
          task.title,
          taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIso() }),
        );
      }
    } else {
      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = ?', [
        'pending',
        nowIso(),
        taskId,
        'running',
      ]);
      this.addEvent(
        projectId,
        TASK_EVENT_TYPES.STOP_REQUESTED,
        task.title,
        taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPING }),
      );
    }

    if (runtime?.running) this.stop(projectId);
    else this.setPhase(projectId, 'stopped');
  }

  taskForProject(projectId, taskId) {
    if (!taskId) return null;
    return this.db.get(
      `SELECT plan_tasks.*
       FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
       WHERE plan_tasks.id = ? AND plans.project_id = ?`,
      [taskId, projectId],
    );
  }

  /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
  interruptPlan(projectId, planId) {
    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
    if (!plan) return;
    // 若当前正在执行该 plan 的任务，kill 进程
    const runtime = this.runtime(projectId);
    const activePlanOperations = runtime
      ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))
      : [];
    for (const activeEntry of activePlanOperations) {
      killChildProcess(activeEntry.child);
      const activeTaskId = activeEntry.operation?.taskId;
      const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
      if (activeTaskId) {
        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked', nowIso(), activeTaskId]);
      }
      this.addEvent(
        projectId,
        LEGACY_TASK_EVENT_TYPES.STOPPING,
        `plan #${planId} 的任务被中断`,
        taskEventMeta(activeTask, {
          planId,
          taskId: activeTaskId || undefined,
          status: TASK_EVENT_STATUS.INTERRUPTED,
          finishedAt: nowIso(),
        }),
      );
    }
    // 其余未完成任务 → blocked
    this.db.run(
      `UPDATE plan_tasks SET status = ?, updated_at = ?
       WHERE plan_id = ? AND status IN ('pending', 'running')`,
      ['blocked', nowIso(), planId],
    );
    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['interrupted', nowIso(), planId]);
    this.addEvent(projectId, 'plan.interrupted', `plan #${planId} 已中断，未完成任务已挂起`);
    this.emitUpdate(projectId);
  }

  /** 恢复被中断的 plan：blocked → pending，plan → pending，循环运行时自动继续执行 */
  resumePlan(projectId, planId) {
    this.db.run(
      `UPDATE plan_tasks SET status = ?, updated_at = ?
       WHERE plan_id = ? AND status = ?`,
      ['pending', nowIso(), planId, 'blocked'],
    );
    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
    this.addEvent(projectId, 'plan.resumed', `plan #${planId} 已恢复`);
    this.emitUpdate(projectId);
  }

  /** 向现有 plan 追加一个任务：写回 plan 文件 + syncPlanTasks 重新解析 */
  appendTask(projectId, planId, title) {
    const project = this.project(projectId);
    const workspace = project?.workspace_path;
    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
    if (!plan) throw new Error('计划不存在');
    if (!workspace) throw new Error('请先设置项目工作区路径');

    const planFile = path.join(workspace, plan.file_path);
    if (!fs.existsSync(planFile)) throw new Error('plan 文件不存在，无法追加任务');

    const cleanTitle = String(title || '').trim();
    if (!cleanTitle) throw new Error('任务标题不能为空');

    // 计算下一个 task_key
    const existing = this.db.all('SELECT task_key FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order', [planId]);
    const maxNum = existing.reduce((max, row) => {
      const m = String(row.task_key || '').match(/P0*(\d+)/i);
      return m ? Math.max(max, Number(m[1])) : max;
    }, 0);
    const taskKey = `P${String(maxNum + 1).padStart(3, '0')}`;

    // 追加到 plan 文件的"## 任务计划"段末尾
    let content = fs.readFileSync(planFile, 'utf8');
    const line = ensureTaskScopeComment(`- [ ] ${taskKey}: ${cleanTitle}`);
    const taskSectionIdx = content.search(/##\s*任务计划/);
    if (taskSectionIdx === -1) {
      content = `${content.trim()}\n\n## 任务计划\n${line}\n`;
    } else {
      content = `${content.trimEnd()}\n${line}\n`;
    }
    fs.writeFileSync(planFile, content, 'utf8');

    // 若 plan 被中断过，恢复为 pending 让新任务可执行
    if (plan.status === 'interrupted') {
      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
    }
    // 重新解析任务入库
    this.syncPlanTasks(planId, planFile);
    const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [planId, taskKey]);
    this.addEvent(
      projectId,
      'task.appended',
      `追加 ${taskKey}: ${cleanTitle}`,
      taskEventMeta(task, {
        planId,
        taskKey,
        taskTitle: cleanTitle,
        status: TASK_EVENT_STATUS.PENDING,
      }),
    );
    this.emitUpdate(projectId);

    // 循环在运行则立即拾取
    if (this.status(projectId)?.running) {
      this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    }
    return taskKey;
  }

  ensureWorkspaceDirs(workspace) {
    for (const dir of ['docs/issues', 'docs/plan', 'docs/progress', 'docs/progress/logs']) {
      fs.mkdirSync(path.join(workspace, dir), { recursive: true });
    }
  }

  scanDirectory(root, workspace, extensions) {
    if (!fs.existsSync(root)) return { root, aggregateHash: hashText(''), files: [] };
    const files = [];
    const visit = (dir) => {
      for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) {
          visit(full);
        } else if (entry.isFile() && extensions.includes(path.extname(entry.name).toLowerCase())) {
          const stat = fs.statSync(full);
          files.push({
            path: normalizeRelative(workspace, full),
            hash: hashFile(full),
            size: stat.size,
            modifiedAt: stat.mtime.toISOString(),
          });
        }
      }
    };
    visit(root);
    files.sort((a, b) => a.path.localeCompare(b.path));
    return {
      root,
      aggregateHash: hashText(files.map((file) => `${file.path}|${file.hash}|${file.size}`).join('\n')),
      files,
    };
  }

  saveScan(projectId, type, scan) {
    const scannedAt = nowIso();
    this.db.run('DELETE FROM scan_files WHERE project_id = ? AND scan_type = ?', [projectId, type]);
    for (const file of scan.files) {
      this.db.run(
        `INSERT OR REPLACE INTO scan_files
         (project_id, scan_type, file_path, hash, size, modified_at, scanned_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)`,
        [projectId, type, file.path, file.hash, file.size, file.modifiedAt, scannedAt],
      );
    }
  }

  hasPlanForIssueHash(projectId, issueHash) {
    return Boolean(
      this.db.get('SELECT id FROM plans WHERE project_id = ? AND issue_hash = ? LIMIT 1', [projectId, issueHash]),
    );
  }

  nextRunnablePlan(projectId) {
    return this.db.get(
      `SELECT * FROM plans
       WHERE project_id = ? AND status NOT IN ('completed', 'interrupted')
       ORDER BY created_at ASC
       LIMIT 1`,
      [projectId],
    );
  }

  async generatePlan(projectId, workspace, issueScan) {
    this.setPhase(projectId, 'generate-plan');
    const planFile = path.join(
      workspace,
      'docs',
      'plan',
      `plan_${timestampForPath()}_${issueScan.aggregateHash.slice(0, 8)}.md`,
    );
    const issueBundle = issueScan.files
      .map((file) => {
        const full = path.join(workspace, file.path);
        return ['---', `path: ${file.path}`, `hash: ${file.hash}`, 'content:', readSnippet(full, 20000)].join('\n');
      })
      .join('\n');

    const prompt = [
      '你是需求整理与开发计划生成者。',
      '请根据 docs/issues 收集到的反馈和需求，生成一个开发计划和验收标准。',
      '',
      `输出文件：${planFile}`,
      '',
      '格式要求：',
      '- 每个任务必须严格使用固定格式：- [ ] P001: 任务标题 <!-- scope: lib/foo.dart,test/foo_test.dart -->',
      '- scope 必填，表示该任务预计修改的文件或模块；多个 scope 用英文逗号分隔；无法判断时写 <!-- scope: unknown -->，unknown 任务不会并发执行',
      '- 每个任务要有验收要点',
      '- 必须包含总体验收标准和进度区',
      '- 只写 plan 文件，不要改业务代码',
      '',
      '需求快照：',
      issueBundle,
    ].join('\n');

    const result = await this.runCodex(workspace, prompt, 'generate-plan', { projectId });
    if (result.exitCode !== 0 || !fs.existsSync(planFile)) {
      this.addEvent(projectId, 'plan.generate.failed', result.logFile);
      return;
    }

    const id = this.db.insert(
      `INSERT INTO plans
       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validation_passed, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
      [
        projectId,
        issueScan.aggregateHash,
        normalizeRelative(workspace, planFile),
        hashFile(planFile),
        'pending',
        nowIso(),
        nowIso(),
      ],
    );
    this.syncPlanTasks(id, planFile);
    this.db.run('UPDATE project_states SET last_issue_hash = ?, updated_at = ? WHERE project_id = ?', [
      issueScan.aggregateHash,
      nowIso(),
      projectId,
    ]);
    this.addEvent(projectId, 'plan.generated', normalizeRelative(workspace, planFile));
  }

  /** 为单条需求/反馈生成计划（调 codex），并回写 linked_plan_id。失败返回 null，下轮重试。 */
  async generatePlanForIntake(projectId, workspace, intake) {
    const table = intake.__type === 'feedback' ? 'feedback' : 'requirements';
    const sourceName = intake.__type === 'feedback' ? '反馈' : '需求';
    this.setPhase(projectId, 'generate-plan');
    const safeId = String(intake.id).replace(/[^0-9a-zA-Z_-]/g, '');
    const planFile = path.join(
      workspace,
      'docs',
      'plan',
      `plan_${intake.__type}_${safeId}_${timestampForPath()}.md`,
    );
    const prompt = [
      '你是需求整理与开发计划生成者。',
      `请根据以下${sourceName}，生成一个开发计划和验收标准。`,
      '',
      `输出文件：${planFile}`,
      '',
      '格式要求：',
      '- 每个任务必须严格使用固定格式：- [ ] P001: 任务标题 <!-- scope: lib/foo.dart,test/foo_test.dart -->',
      '- scope 必填，表示该任务预计修改的文件或模块；多个 scope 用英文逗号分隔；无法判断时写 <!-- scope: unknown -->，unknown 任务不会并发执行',
      '- 每个任务要有验收要点',
      '- 必须包含总体验收标准和进度区',
      '- 只写 plan 文件，不要改业务代码',
      '',
      `${sourceName} #${intake.id} 内容：`,
      String(intake.body || '').trim() || '（正文为空）',
    ].join('\n');

    const result = await this.runCodex(workspace, prompt, `gen-${intake.__type}-${intake.id}`, { projectId });
    if (result.exitCode !== 0 || !fs.existsSync(planFile)) {
      this.addEvent(projectId, 'plan.generate.failed', `${sourceName} #${intake.id} 计划生成失败：${result.logFile}`);
      return null;
    }

    const issueHash = `${intake.__type}-${intake.id}-${hashText(String(intake.body || '')).slice(0, 16)}`;
    const id = this.db.insert(
      `INSERT INTO plans
       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validation_passed, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
      [projectId, issueHash, normalizeRelative(workspace, planFile), hashFile(planFile), 'pending', nowIso(), nowIso()],
    );
    this.syncPlanTasks(id, planFile);
    // 回写关联
    this.db.run(`UPDATE ${table} SET linked_plan_id = ?, updated_at = ? WHERE id = ?`, [id, nowIso(), intake.id]);
    this.addEvent(projectId, 'plan.generated', `${sourceName} #${intake.id} 已生成计划：${normalizeRelative(workspace, planFile)}`);
    return id;
  }

  async processPlan(workspace, plan) {
    const planFile = path.join(workspace, plan.file_path);
    this.syncPlanTasks(plan.id, planFile);
    const pendingTasks = this.db.all(
      `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
       ORDER BY sort_order ASC`,
      [plan.id],
    );
    if (pendingTasks.length) {
      const batch = this.parallelTaskBatch(pendingTasks);
      if (batch.length > 1) {
        await this.executeTaskBatch(workspace, plan, batch);
      } else {
        const task = batch[0] || pendingTasks[0];
        const result = await this.executeTask(workspace, plan, task);
        if (result.exitCode === 0) {
          this.completeTask(workspace, plan, task, result);
        }
      }
      return;
    }
    await this.validatePlan(workspace, plan);
  }

  parallelTaskBatch(tasks) {
    const selected = [];
    const usedScopes = new Set();
    for (const task of tasks) {
      if (selected.length >= MAX_PARALLEL_TASKS) break;
      const scopes = taskParallelScopes(task);
      if (!scopes.length) {
        if (!selected.length) return [task];
        continue;
      }
      if (scopes.some((scope) => usedScopes.has(scope))) continue;
      selected.push(task);
      for (const scope of scopes) usedScopes.add(scope);
    }
    return selected.length > 1 ? selected : [tasks[0]];
  }

  async executeTaskBatch(workspace, plan, tasks) {
    this.setPhase(plan.project_id, 'execute-task');
    this.addEvent(
      plan.project_id,
      'tasks.parallel.started',
      `并发执行 ${tasks.map((task) => task.task_key).join(', ')}`,
      { taskIds: tasks.map((task) => task.id) },
    );
    const results = await Promise.all(
      tasks.map(async (task) => {
        try {
          return {
            task,
            result: await this.executeTask(workspace, plan, task, { parallel: true }),
          };
        } catch (error) {
          this.addEvent(
            plan.project_id,
            TASK_EVENT_TYPES.FAILED,
            task.title,
            taskEventMeta(task, {
              planId: plan.id,
              status: TASK_EVENT_STATUS.FAILED,
              finishedAt: nowIso(),
              error: error?.message || String(error),
            }),
          );
          return { task, result: { exitCode: -1 } };
        }
      }),
    );
    for (const { task, result } of results) {
      if (result.exitCode === 0) {
        this.completeTask(workspace, plan, task, result);
      }
    }
  }

  async executeTask(workspace, plan, task, options = {}) {
    this.setPhase(plan.project_id, 'execute-task');
    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), task.id]);
    this.emitUpdate(plan.project_id);
    const planFile = path.join(workspace, plan.file_path);
    const completionRules = [
      '- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区',
      '- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区',
      '- 只修改当前任务 scope 直接相关的业务文件',
      '- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试',
      '- 只有当前任务本身明确要求全量验证时，才允许运行全量测试',
      '- 不输出完整 diff、源码全文或长文件列表',
      '- 中文文件读写使用 UTF-8',
    ];
    if (options.parallel) {
      completionRules.unshift('- 当前为并发执行模式，不要读写其它任务的 scope');
    }
    const prompt = [
      '你是开发执行者。',
      `请只执行指定任务 ${task.task_key}，不要提前执行其它任务，也不要顺手处理其它 checkbox。`,
      '',
      `plan 文件（只读）：${planFile}`,
      `指定任务：${task.raw_line}`,
      `任务 scope：${task.scope || taskScopeText(task) || 'unknown'}`,
      '',
      '完成后必须：',
      ...completionRules,
    ].join('\n');
    const result = await this.runCodexWithPlanGuard(workspace, prompt, `execute-${task.task_key}`, {
      projectId: plan.project_id,
      planId: plan.id,
      taskId: task.id,
      parallel: Boolean(options.parallel),
    }, planFile);
    if (result.exitCode !== 0) {
      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), task.id]);
      this.emitUpdate(plan.project_id);
    }
    const succeeded = result.exitCode === 0;
    this.addEvent(
      plan.project_id,
      succeeded ? LEGACY_TASK_EVENT_TYPES.EXECUTED : TASK_EVENT_TYPES.FAILED,
      task.title,
      taskEventMeta(task, {
        planId: plan.id,
        status: succeeded ? TASK_EVENT_STATUS.COMPLETED : TASK_EVENT_STATUS.FAILED,
        finishedAt: nowIso(),
        exitCode: result.exitCode,
        log: result.logFile,
      }),
    );
    return result;
  }

  completeTask(workspace, plan, task, result) {
    const planFile = path.join(workspace, plan.file_path);
    this.markTaskCompletedInPlan(workspace, planFile, task, result);
    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['completed', nowIso(), task.id]);
    this.refreshPlanProgress(plan.id, planFile);
    this.emitUpdate(plan.project_id);
  }

  refreshPlanProgress(planId, planFile) {
    const totals = this.db.get(
      `SELECT COUNT(*) AS total,
              SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) AS completed
       FROM plan_tasks
       WHERE plan_id = ?`,
      [planId],
    ) || { total: 0, completed: 0 };
    const total = Number(totals.total || 0);
    const completed = Number(totals.completed || 0);
    const status = total > 0 && completed === total ? 'ready_for_validation' : 'running';
    const hash = fs.existsSync(planFile) ? hashFile(planFile) : '';
    this.db.run(
      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
      [hash, status, total, completed, nowIso(), planId],
    );
  }

  markTaskCompletedInPlan(workspace, planFile, task, result) {
    if (!fs.existsSync(planFile)) return;
    const relativeLog = result?.logFile ? normalizeRelative(workspace, result.logFile) : '';
    const key = escapeRegExp(String(task.task_key || ''));
    const checkboxRe = new RegExp(`(^\\s*[-*]\\s+\\[)([ xX])(\\]\\s+${key}(?:\\b|[:：\\s-]))`, 'm');
    let content = fs.readFileSync(planFile, 'utf8');
    if (checkboxRe.test(content)) {
      content = content.replace(checkboxRe, '$1x$3');
    }

    const note = `- ${task.task_key} AutoPlan 完成：${nowIso()}${relativeLog ? `；日志：${relativeLog}` : ''}`;
    const noteRe = new RegExp(`^\\s*-\\s+${key}\\s+AutoPlan 完成：.*$`, 'm');
    if (noteRe.test(content)) {
      content = content.replace(noteRe, note);
    } else if (/##\s*进度区/.test(content)) {
      content = `${content.trimEnd()}\n${note}\n`;
    } else {
      content = `${content.trimEnd()}\n\n## 进度区\n${note}\n`;
    }
    fs.writeFileSync(planFile, content, 'utf8');
  }

  async validatePlan(workspace, plan) {
    this.setPhase(plan.project_id, 'validate');
    const planFile = path.join(workspace, plan.file_path);
    const command = String(this.status(plan.project_id)?.validation_command || '').trim();
    if (!command) {
      // 验收命令为空：跳过校验，直接标记完成。
      this.db.run(
        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
        ['completed', nowIso(), plan.id],
      );
      this.addEvent(plan.project_id, 'plan.completed', '任务全部完成（验收命令为空，已跳过校验）');
      return;
    }
    let validation = await this.runShell(workspace, command, `validate-${plan.id}`, { projectId: plan.project_id });
    for (let attempt = 1; validation.exitCode !== 0 && attempt <= 2; attempt += 1) {
      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', [
        'validation_failed',
        nowIso(),
        plan.id,
      ]);
      const prompt = [
        'plan 已完成，但宿主项目验收失败。请只修复验证错误。',
        `plan 文件（只读）：${planFile}`,
        '不要修改 plan 文件、checkbox 或进度区。',
        `失败命令：${command}`,
        '失败输出摘要：',
        tailText(validation.output, 12000),
      ].join('\n');
      await this.runCodexWithPlanGuard(workspace, prompt, `repair-${plan.id}-${attempt}`, {
        projectId: plan.project_id,
        planId: plan.id,
      }, planFile);
      validation = await this.runShell(workspace, command, `validate-${plan.id}-repair-${attempt}`, {
        projectId: plan.project_id,
      });
    }

    if (validation.exitCode === 0) {
      this.db.run(
        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
        ['completed', nowIso(), plan.id],
      );
      this.addEvent(plan.project_id, 'plan.completed', plan.file_path);
    } else {
      this.addEvent(plan.project_id, 'validation.failed', validation.logFile);
    }
  }

  syncPlanTasks(planId, planFile) {
    if (!fs.existsSync(planFile)) return;
    normalizePlanTaskScopes(planFile);
    const text = fs.readFileSync(planFile, 'utf8');
    const regex = /^\s*[-*]\s+\[([ xX])\]\s+(.+)$/gm;
    const tasks = [];
    let match;
    let index = 0;
    while ((match = regex.exec(text))) {
      index += 1;
      const rawTitle = match[2].trim();
      const titleWithoutScope = stripTaskScopeComment(rawTitle);
      const idMatch = titleWithoutScope.match(/^([A-Za-z]+[-_]?\d+|P\d+)[:：\s-]+(.+)$/);
      tasks.push({
        key: idMatch?.[1] || `P${String(index).padStart(3, '0')}`,
        title: idMatch?.[2]?.trim() || titleWithoutScope || rawTitle,
        rawLine: ensureTaskScopeComment(match[0]),
        scope: taskScopeText({ raw_line: match[0], title: rawTitle }),
        status: match[1].toLowerCase() === 'x' ? 'completed' : 'pending',
        sortOrder: index,
      });
    }

    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
    for (const task of tasks) {
      this.db.run(
        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
      );
    }

    const completed = tasks.filter((task) => task.status === 'completed').length;
    const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
    this.db.run(
      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
      [hashFile(planFile), status, tasks.length, completed, nowIso(), planId],
    );
  }

  /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
  archiveOperation(projectId, operationKey) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOperation;
    if (op) {
      if (op.activity && typeof op.activity.flush === 'function') {
        op.activity.flush();
      }
      runtime.lastOperation = {
        label: op.label || '',
        projectId: op.projectId || null,
        planId: op.planId || null,
        taskId: op.taskId || null,
        startedAt: op.startedAt || null,
        finishedAt: nowIso(),
        exitCode: typeof op.exitCode === 'number' ? op.exitCode : null,
        logTail: (op.logBuffer || '').slice(-8000),
        activity: op.activity ? op.activity.getLines() : [],
      };
    }
    if (operationKey) {
      runtime.activeChildren.delete(operationKey);
      runtime.activeOperations.delete(operationKey);
    } else {
      runtime.activeChildren.clear();
      runtime.activeOperations.clear();
    }
    refreshRuntimeActive(runtime);
  }

  async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
    const before = planFile && fs.existsSync(planFile) ? fs.readFileSync(planFile, 'utf8') : null;
    const result = await this.runCodex(workspace, prompt, label, operation);
    if (before !== null) {
      const changed = !fs.existsSync(planFile) || fs.readFileSync(planFile, 'utf8') !== before;
      if (changed) {
        fs.writeFileSync(planFile, before, 'utf8');
        this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
          planId: operation.planId || null,
          taskId: operation.taskId || null,
        });
      }
    }
    return result;
  }

  async runCodex(workspace, prompt, label, operation = {}) {
    const projectIdForEmit = operation.projectId;
    const runtime = this.runtime(projectIdForEmit);
    if (!runtime) throw new Error('projectId is required for codex operations');
    const logDir = path.join(workspace, 'docs', 'progress', 'logs');
    fs.mkdirSync(logDir, { recursive: true });
    const prefix = `${timestampForPath()}_${safePart(label)}`;
    const logFile = path.join(logDir, `${prefix}.log`);
    const lastFile = path.join(logDir, `${prefix}.last.txt`);
    const args = [
      'exec',
      '--cd',
      workspace,
      '--color',
      'never',
      '-o',
      lastFile,
      '--sandbox',
      'danger-full-access',
      '-',
    ];
    const child = spawn('codex', args, { shell: process.platform === 'win32', cwd: workspace });
    const activeOperation = {
      ...operation,
      label,
      logBuffer: '',
      activity: new CodexActivityPrinter(200),
      startedAt: nowIso(),
    };
    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
    child.stdin.setDefaultEncoding('utf8');
    child.stdin.end(prompt);
    const stream = fs.createWriteStream(logFile, { encoding: 'utf8' });
    // 累积原始日志 + 提取可读活动行
    const onChunk = (chunk) => {
      const text = chunk.toString('utf8');
      if (!runtime.activeOperations.has(operationKey)) return;
      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
      if (activeOperation.logBuffer.length > 24000) {
        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
      }
      // 喂给活动打印机，提取人类可读的进度行
      if (activeOperation.activity) activeOperation.activity.offer(text);
    };
    child.stdout.on('data', onChunk);
    child.stderr.on('data', onChunk);
    child.stdout.pipe(stream, { end: false });
    child.stderr.pipe(stream, { end: false });
    const tailTimer = setInterval(() => {
      if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
    }, 600);
    try {
      const exitCode = await waitForChild(child, 45 * 60 * 1000);
      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
      return { exitCode, logFile, lastFile };
    } finally {
      clearInterval(tailTimer);
      stream.end();
      if (runtime.activeOperations.has(operationKey)) {
        this.archiveOperation(projectIdForEmit, operationKey);
      }
    }
  }

  async runShell(workspace, command, label, operation = {}) {
    const projectIdForEmit = operation.projectId;
    const runtime = this.runtime(projectIdForEmit);
    if (!runtime) throw new Error('projectId is required for shell operations');
    const logDir = path.join(workspace, 'docs', 'progress', 'logs');
    fs.mkdirSync(logDir, { recursive: true });
    const logFile = path.join(logDir, `${timestampForPath()}_${safePart(label)}.log`);
    const shellCommand = process.platform === 'win32' ? `chcp 65001>nul && ${command}` : command;
    const child = spawn(shellCommand, {
      shell: true,
      cwd: workspace,
      env: { ...process.env },
    });
    const activeOperation = {
      ...operation,
      label,
      logBuffer: '',
      activity: new CodexActivityPrinter(200),
      startedAt: nowIso(),
    };
    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
    let output = '';
    const onChunk = (chunk) => {
      const text = chunk.toString('utf8');
      output += text;
      if (!runtime.activeOperations.has(operationKey)) return;
      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
      if (activeOperation.logBuffer.length > 24000) {
        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
      }
      if (activeOperation.activity) activeOperation.activity.offer(text);
    };
    child.stdout.on('data', onChunk);
    child.stderr.on('data', onChunk);
    const tailTimer = setInterval(() => {
      if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
    }, 600);
    try {
      const exitCode = await waitForChild(child, 10 * 60 * 1000);
      fs.writeFileSync(logFile, output, 'utf8');
      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
      return { exitCode, output, logFile };
    } finally {
      clearInterval(tailTimer);
      if (runtime.activeOperations.has(operationKey)) {
        this.archiveOperation(projectIdForEmit, operationKey);
      }
    }
  }

  setPhase(projectId, phase) {
    this.db.run('UPDATE project_states SET phase = ?, updated_at = ? WHERE project_id = ?', [
      phase,
      nowIso(),
      projectId,
    ]);
    this.emitUpdate(projectId);
  }

  recordError(projectId, error) {
    const message = error?.stack || error?.message || String(error);
    this.db.run(
      'UPDATE project_states SET phase = ?, last_error = ?, updated_at = ? WHERE project_id = ?',
      ['error', message, nowIso(), projectId],
    );
    this.addEvent(projectId, 'loop.error', message);
    this.emitUpdate(projectId);
  }

  addEvent(projectId, type, message, meta = null) {
    this.db.run('INSERT INTO events (project_id, type, message, meta, created_at) VALUES (?, ?, ?, ?, ?)', [
      projectId,
      type,
      message,
      meta ? JSON.stringify(meta) : null,
      nowIso(),
    ]);
    this.emitUpdate(projectId);
  }

  emitUpdate(projectId) {
    this.emit('update', this.snapshot(projectId));
  }

  snapshot(projectId = null) {
    const projects = this.projects();
    if (!projectId) return emptySnapshot(projects);

    const activeProject = this.project(projectId);
    if (!activeProject) return emptySnapshot(projects);

    const state = {
      ...(this.status(projectId) || {}),
      workspace_path: activeProject.workspace_path || '',
    };
    const runtime = this.existingRuntime(projectId);

    return {
      activeProjectId: projectId,
      activeProject,
      projects,
      state,
      requirements: this.db.all(
        `SELECT requirements.*, plans.status AS plan_status,
                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
         FROM requirements
         LEFT JOIN plans ON plans.id = requirements.linked_plan_id
         WHERE requirements.project_id = ?
         ORDER BY requirements.updated_at DESC`,
        [projectId],
      ),
      feedback: this.db.all(
        `SELECT feedback.*, plans.status AS plan_status,
                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
         FROM feedback
         LEFT JOIN plans ON plans.id = feedback.linked_plan_id
         WHERE feedback.project_id = ?
         ORDER BY feedback.updated_at DESC`,
        [projectId],
      ),
      attachments: this.db.all(
        'SELECT * FROM attachments WHERE project_id = ? ORDER BY created_at DESC, id DESC',
        [projectId],
      ),
      planDrafts: this.db.all('SELECT * FROM plan_drafts WHERE project_id = ? ORDER BY updated_at DESC, id DESC', [
        projectId,
      ]),
      plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
      tasks: this.db.all(
        `SELECT plan_tasks.*, plans.file_path
         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
         WHERE plans.project_id = ?
         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
        [projectId],
      ),
      events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
      scans: this.db.all(
        'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
        [projectId],
      ),
      activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) === Number(projectId)
        ? {
            label: runtime.activeOperation.label || '',
            projectId: runtime.activeOperation.projectId || null,
            planId: runtime.activeOperation.planId || null,
            taskId: runtime.activeOperation.taskId || null,
            startedAt: runtime.activeOperation.startedAt || null,
            logTail: (runtime.activeOperation.logBuffer || '').slice(-8000),
            activity: runtime.activeOperation.activity ? runtime.activeOperation.activity.getLines() : [],
          }
        : null,
      activeOperations: runtime?.activeOperations
        ? Array.from(runtime.activeOperations.values())
            .filter((operation) => Number(operation.projectId) === Number(projectId))
            .map((operation) => ({
              label: operation.label || '',
              projectId: operation.projectId || null,
              planId: operation.planId || null,
              taskId: operation.taskId || null,
              startedAt: operation.startedAt || null,
              logTail: (operation.logBuffer || '').slice(-8000),
              activity: operation.activity ? operation.activity.getLines() : [],
            }))
        : [],
      lastOperation:
        runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
          ? runtime.lastOperation
          : null,
    };
  }
}

function taskEventMeta(task, overrides = {}) {
  const meta = {
    ...compactEventMeta({
      taskId: task?.id,
      taskKey: task?.task_key,
      taskTitle: task?.title,
      planId: task?.plan_id,
      status: task?.status,
      startedAt: task?.started_at,
      finishedAt: task?.finished_at,
    }),
    ...compactEventMeta(overrides),
  };
  meta.taskId = normalizeOptionalNumber(meta.taskId);
  meta.planId = normalizeOptionalNumber(meta.planId);
  meta.taskKey = normalizeOptionalString(meta.taskKey);
  meta.taskTitle = normalizeOptionalString(meta.taskTitle);
  meta.status = normalizeOptionalString(meta.status);
  meta.startedAt = normalizeOptionalString(meta.startedAt);
  meta.finishedAt = normalizeOptionalString(meta.finishedAt);
  const compacted = compactEventMeta(meta);
  return Object.keys(compacted).length ? compacted : null;
}

function compactEventMeta(meta) {
  const result = {};
  for (const [key, value] of Object.entries(meta || {})) {
    if (value !== undefined && value !== null && value !== '') result[key] = value;
  }
  return result;
}

function normalizeOptionalNumber(value) {
  if (value === undefined || value === null || value === '') return undefined;
  const number = Number(value);
  return Number.isFinite(number) ? number : undefined;
}

function normalizeOptionalString(value) {
  if (value === undefined || value === null) return undefined;
  const text = String(value).trim();
  return text || undefined;
}

function createProjectRuntime() {
  return {
    timer: null,
    running: false,
    busy: false,
    activeChild: null,
    activeOperation: null,
    activeChildren: new Map(),
    activeOperations: new Map(),
    lastOperation: null,
  };
}

function emptySnapshot(projects) {
  return {
    activeProjectId: null,
    activeProject: null,
    projects,
    state: null,
    requirements: [],
    feedback: [],
    attachments: [],
    planDrafts: [],
    plans: [],
    tasks: [],
    events: [],
    scans: [],
    activeOperation: null,
    activeOperations: [],
    lastOperation: null,
  };
}

function registerRuntimeOperation(runtime, child, operation) {
  if (!runtime.activeChildren) runtime.activeChildren = new Map();
  if (!runtime.activeOperations) runtime.activeOperations = new Map();
  const operationKey = `${Date.now()}-${Math.random().toString(36).slice(2)}`;
  runtime.activeChildren.set(operationKey, child);
  runtime.activeOperations.set(operationKey, operation);
  runtime.activeChild = child;
  runtime.activeOperation = operation;
  return operationKey;
}

function refreshRuntimeActive(runtime) {
  const entries = Array.from(runtime.activeOperations?.entries?.() || []);
  const latest = entries.at(-1);
  if (!latest) {
    runtime.activeChild = null;
    runtime.activeOperation = null;
    return;
  }
  runtime.activeChild = runtime.activeChildren.get(latest[0]) || null;
  runtime.activeOperation = latest[1] || null;
}

function findRuntimeOperation(runtime, predicate) {
  return findRuntimeOperations(runtime, predicate)[0] || null;
}

function findRuntimeOperations(runtime, predicate) {
  if (!runtime?.activeOperations) return [];
  const matches = [];
  for (const [operationKey, operation] of runtime.activeOperations.entries()) {
    if (predicate(operation)) {
      matches.push({
        operationKey,
        operation,
        child: runtime.activeChildren?.get(operationKey) || null,
      });
    }
  }
  return matches;
}

function waitForChild(child, timeoutMs) {
  return new Promise((resolve) => {
    let settled = false;
    let killTimer = null;
    const finish = (code) => {
      if (settled) return;
      settled = true;
      clearTimeout(timer);
      if (killTimer) clearTimeout(killTimer);
      resolve(code ?? 0);
    };
    const timer = setTimeout(() => {
      killChildProcess(child);
      killTimer = setTimeout(() => finish(-1), 5000);
    }, timeoutMs);
    child.on('exit', (code) => {
      finish(code);
    });
    child.on('error', () => finish(-1));
  });
}

function killChildProcess(child) {
  if (!child || child.killed) return;
  if (process.platform === 'win32' && child.pid) {
    const killer = spawn('taskkill', ['/pid', String(child.pid), '/T', '/F'], { windowsHide: true });
    killer.on('error', () => child.kill());
    return;
  }
  child.kill('SIGTERM');
}

function hashFile(filePath) {
  return crypto.createHash('sha256').update(fs.readFileSync(filePath)).digest('hex');
}

function hashText(text) {
  return crypto.createHash('sha256').update(text).digest('hex');
}

function taskParallelScopes(task) {
  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  if (PARALLEL_BLOCKING_TASK_RE.test(raw)) return [];
  return taskDeclaredScopes(task, { keepUnknown: false });
}

function taskScopeText(task) {
  const explicit = taskDeclaredScopes(task, { keepUnknown: true, includePathFallback: false });
  if (explicit.length) return explicit.join(', ');
  const inferred = taskDeclaredScopes(task, { keepUnknown: false });
  return inferred.join(', ') || 'unknown';
}

function taskDeclaredScopes(task, options = {}) {
  const { keepUnknown = false, includePathFallback = true } = options;
  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  const scopes = new Set();

  addScopeParts(scopes, String(task.scope || '').split(TASK_SCOPE_SPLIT_RE), { keepUnknown });
  addScopeParts(scopes, explicitTaskScopeParts(raw), { keepUnknown });

  if (includePathFallback) {
    for (const match of raw.matchAll(TASK_PATH_RE)) {
      const scope = normalizeTaskScope(match[0], { keepUnknown });
      if (scope && !scope.startsWith('docs/plan/') && !scope.startsWith('docs/progress/')) {
        scopes.add(scope);
      }
    }
  }

  return Array.from(scopes);
}

function explicitTaskScopeParts(raw) {
  const explicit = String(raw || '').match(TASK_SCOPE_RE);
  return explicit?.[1] ? explicit[1].split(TASK_SCOPE_SPLIT_RE) : [];
}

function addScopeParts(scopes, parts, options = {}) {
  for (const part of parts) {
    const scope = normalizeTaskScope(part, options);
    if (scope) scopes.add(scope);
  }
}

function normalizeTaskScope(value, options = {}) {
  const scope = String(value || '')
    .trim()
    .replace(/^["'`[{(]+|["'`\]})]+$/g, '')
    .replace(/\s*--$/, '')
    .replaceAll('\\', '/')
    .toLowerCase();
  if (!scope || scope === '-') return '';
  if (scope === 'unknown') return options.keepUnknown ? 'unknown' : '';
  return scope;
}

function ensureTaskScopeComment(line, fallbackScope = 'unknown') {
  const text = String(line || '').trimEnd();
  return TASK_SCOPE_RE.test(text) ? text : `${text} <!-- scope: ${fallbackScope} -->`;
}

function stripTaskScopeComment(value) {
  return String(value || '').replace(TASK_SCOPE_COMMENT_RE, ' ').replace(/\s+/g, ' ').trim();
}

function normalizePlanTaskScopes(planFile) {
  const content = fs.readFileSync(planFile, 'utf8');
  let changed = false;
  const next = content.replace(/^(\s*[-*]\s+\[[ xX]\]\s+.+)$/gm, (line) => {
    if (TASK_SCOPE_RE.test(line)) return line;
    changed = true;
    return ensureTaskScopeComment(line);
  });
  if (changed) fs.writeFileSync(planFile, next, 'utf8');
}

function normalizeRelative(root, fullPath) {
  return path.relative(root, fullPath).replaceAll(path.sep, '/');
}

function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function workspaceKey(workspace) {
  const value = String(workspace || '').trim();
  if (!value) return '';
  const resolved = path.resolve(value);
  return process.platform === 'win32' ? resolved.toLowerCase() : resolved;
}

function timestampForPath() {
  const now = new Date();
  const pad = (n) => String(n).padStart(2, '0');
  return `${now.getFullYear()}${pad(now.getMonth() + 1)}${pad(now.getDate())}-${pad(
    now.getHours(),
  )}${pad(now.getMinutes())}${pad(now.getSeconds())}`;
}

function readSnippet(filePath, maxChars) {
  const text = fs.readFileSync(filePath, 'utf8');
  return text.length > maxChars ? `${text.slice(0, maxChars)}\n...[truncated]` : text;
}

function tailText(text, maxChars) {
  return text.length > maxChars ? text.slice(text.length - maxChars) : text;
}

function safePart(value) {
  return String(value).replace(/[^A-Za-z0-9_.-]+/g, '_');
}

module.exports = {
  LoopService,
  LEGACY_TASK_EVENT_TYPES,
  TASK_EVENT_COMPATIBILITY,
  TASK_EVENT_SEMANTICS,
  TASK_EVENT_STATUS,
  TASK_EVENT_TYPES,
};

codex
已确认 P002 验收点。接下来只定位 `syncPlanTasks` 的实现与相关更新路径。
  ✓ 读取 P002 上下文
  → 修改任务同步逻辑
  • 做最小语法验证
exec
powershell.exe -Command "Select-String -LiteralPath 'src\\loopService.js' -Pattern 'syncPlanTasks|plan_tasks|task_key|duration|status|session' -Context 4,8" in D:\project\GitHub\autoplan
 succeeded in 312ms:

  src\loopService.js:29:  EXECUTED: 'task.executed',
  src\loopService.js:30:  STOPPING: 'task.stopping',
  src\loopService.js:31:});
  src\loopService.js:32:
> src\loopService.js:33:const TASK_EVENT_STATUS = Object.freeze({
  src\loopService.js:34:  PENDING: 'pending',
  src\loopService.js:35:  RUNNING: 'running',
  src\loopService.js:36:  COMPLETED: 'completed',
  src\loopService.js:37:  FAILED: 'failed',
  src\loopService.js:38:  STOPPING: 'stopping',
  src\loopService.js:39:  STOPPED: 'stopped',
  src\loopService.js:40:  INTERRUPTED: 'interrupted',
  src\loopService.js:41:});
  src\loopService.js:42:
  src\loopService.js:43:const TASK_EVENT_SEMANTICS = Object.freeze({
> src\loopService.js:44:  [TASK_EVENT_TYPES.STARTED]: Object.freeze({ status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务'
 }),
> src\loopService.js:45:  [TASK_EVENT_TYPES.SUCCEEDED]: Object.freeze({ status: TASK_EVENT_STATUS.COMPLETED, label: '结束
了任务' }),
> src\loopService.js:46:  [TASK_EVENT_TYPES.FAILED]: Object.freeze({ status: TASK_EVENT_STATUS.FAILED, label: '任务失败' })
,
> src\loopService.js:47:  [TASK_EVENT_TYPES.STOP_REQUESTED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPING, label:
 '请求停止任务' }),
> src\loopService.js:48:  [TASK_EVENT_TYPES.STOPPED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务'
 }),
> src\loopService.js:49:  [TASK_EVENT_TYPES.INTERRUPTED]: Object.freeze({ status: TASK_EVENT_STATUS.INTERRUPTED, label:
 '已中断任务' }),
  src\loopService.js:50:});
  src\loopService.js:51:
  src\loopService.js:52:const TASK_EVENT_COMPATIBILITY = Object.freeze({
  src\loopService.js:53:  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,
  src\loopService.js:54:  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,
  src\loopService.js:55:});
  src\loopService.js:56:
  src\loopService.js:57:class LoopService extends EventEmitter {
  src\loopService.js:140:           updated_at = ?
  src\loopService.js:141:       WHERE id = 1 AND (running != 0 OR phase IN ${ACTIVE_RUNTIME_PHASE_SQL})`,
  src\loopService.js:142:      [now],
  src\loopService.js:143:    );
> src\loopService.js:144:    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE status = ?', ['pending
', now, 'running']);
  src\loopService.js:145:  }
  src\loopService.js:146:
> src\loopService.js:147:  status(projectId = this.defaultProjectId()) {
  src\loopService.js:148:    if (!projectId) return null;
  src\loopService.js:149:    this.ensureProjectState(projectId);
> src\loopService.js:150:    return this.normalizeRuntimeStatus(projectId, this.db.get('SELECT * FROM project_states WH
ERE project_id = ?', [projectId]));
  src\loopService.js:151:  }
  src\loopService.js:152:
> src\loopService.js:153:  normalizeRuntimeStatus(projectId, state) {
  src\loopService.js:154:    if (!state) return null;
  src\loopService.js:155:    const runtime = this.existingRuntime(projectId);
  src\loopService.js:156:    const runtimeRunning = Boolean(runtime?.running);
  src\loopService.js:157:    const normalized = {
  src\loopService.js:158:      ...state,
  src\loopService.js:159:      running: runtimeRunning ? 1 : 0,
  src\loopService.js:160:    };
  src\loopService.js:161:
  src\loopService.js:166:    return normalized;
  src\loopService.js:167:  }
  src\loopService.js:168:
  src\loopService.js:169:  configure(projectId, { workspacePath, intervalSeconds, validationCommand }) {
> src\loopService.js:170:    const current = this.status(projectId);
  src\loopService.js:171:    const project = this.project(projectId);
  src\loopService.js:172:    const runtime = this.existingRuntime(projectId);
  src\loopService.js:173:    const nextWorkspace = workspacePath ?? project?.workspace_path;
  src\loopService.js:174:    const workspaceOwner = this.activeProjectForWorkspace(nextWorkspace, projectId);
  src\loopService.js:175:    if ((runtime?.running || runtime?.busy) && workspaceOwner) {
  src\loopService.js:176:      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
  src\loopService.js:177:    }
  src\loopService.js:178:    if (!project || !current) throw new Error('项目不存在');
  src\loopService.js:221:    return null;
  src\loopService.js:222:  }
  src\loopService.js:223:
  src\loopService.js:224:  start(projectId) {
> src\loopService.js:225:    const state = this.status(projectId);
  src\loopService.js:226:    const project = this.project(projectId);
  src\loopService.js:227:    if (!project || !state) throw new Error('项目不存在');
  src\loopService.js:228:    if (!project.workspace_path) throw new Error('请先设置项目工作区路径');
  src\loopService.js:229:    const runtime = this.runtime(projectId);
  src\loopService.js:230:    if (!runtime) return;
  src\loopService.js:231:    if (runtime.running) return;
  src\loopService.js:232:    const workspaceOwner = this.activeProjectForWorkspace(project.workspace_path, projectId);
  src\loopService.js:233:    if (workspaceOwner) {
  src\loopService.js:261:        const activeTaskId = operation?.taskId || null;
  src\loopService.js:262:        killChildProcess(child);
  src\loopService.js:263:        const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
  src\loopService.js:264:        if (activeTaskId) {
> src\loopService.js:265:          this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [
  src\loopService.js:266:            'pending',
  src\loopService.js:267:            nowIso(),
  src\loopService.js:268:            activeTaskId,
  src\loopService.js:269:          ]);
  src\loopService.js:270:        }
  src\loopService.js:271:        this.addEvent(
  src\loopService.js:272:          projectId,
  src\loopService.js:273:          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
  src\loopService.js:274:          operation?.label || '',
  src\loopService.js:275:          activeTask
> src\loopService.js:276:            ? taskEventMeta(activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIs
o() })
  src\loopService.js:277:            : null,
  src\loopService.js:278:        );
  src\loopService.js:279:      }
  src\loopService.js:280:    }
  src\loopService.js:281:    this.db.run(
  src\loopService.js:282:      'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',
  src\loopService.js:283:      ['stopped', nowIso(), projectId],
  src\loopService.js:284:    );
  src\loopService.js:302:      const pendingRequirements = this.db
  src\loopService.js:303:        .all(
  src\loopService.js:304:          `SELECT * FROM requirements
  src\loopService.js:305:           WHERE project_id = ? AND linked_plan_id IS NULL
> src\loopService.js:306:             AND status NOT IN ('completed', 'closed')
  src\loopService.js:307:           ORDER BY created_at ASC`,
  src\loopService.js:308:          [projectId],
  src\loopService.js:309:        )
  src\loopService.js:310:        .map((row) => ({ ...row, __type: 'requirement' }));
  src\loopService.js:311:      const pendingFeedback = this.db
  src\loopService.js:312:        .all(
  src\loopService.js:313:          `SELECT * FROM feedback
  src\loopService.js:314:           WHERE project_id = ? AND linked_plan_id IS NULL
> src\loopService.js:315:             AND status NOT IN ('completed', 'closed')
  src\loopService.js:316:           ORDER BY created_at ASC`,
  src\loopService.js:317:          [projectId],
  src\loopService.js:318:        )
  src\loopService.js:319:        .map((row) => ({ ...row, __type: 'feedback' }));
  src\loopService.js:320:      const pendingIntakes = [...pendingRequirements, ...pendingFeedback];
  src\loopService.js:321:      this.addEvent(projectId, 'scan.done', `待处理需求/反馈=${pendingIntakes.length}`);
  src\loopService.js:322:
  src\loopService.js:323:      // 一次只生成一个计划（保持串行，避免 codex 并发），剩余等下一轮 timer
  src\loopService.js:369:    }
  src\loopService.js:370:
  src\loopService.js:371:    runtime.busy = true;
  src\loopService.js:372:    try {
> src\loopService.js:373:      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running',
 nowIso(), taskId]);
  src\loopService.js:374:      this.emitUpdate(projectId);
  src\loopService.js:375:      const result = await this.executeTask(workspace, plan, task);
  src\loopService.js:376:      if (result.exitCode === 0) {
  src\loopService.js:377:        this.completeTask(workspace, plan, task, result);
  src\loopService.js:378:      }
  src\loopService.js:379:      if (runtime.running) {
  src\loopService.js:380:        this.setPhase(projectId, 'waiting');
  src\loopService.js:381:      } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [project
Id])?.phase !== 'stopped') {
  src\loopService.js:394:
  src\loopService.js:395:    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId)
 === Number(taskId));
  src\loopService.js:396:    if (activeEntry) {
  src\loopService.js:397:      killChildProcess(activeEntry.child);
> src\loopService.js:398:      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending',
 nowIso(), taskId]);
  src\loopService.js:399:      if (!runtime.running) {
  src\loopService.js:400:        this.addEvent(
  src\loopService.js:401:          projectId,
  src\loopService.js:402:          LEGACY_TASK_EVENT_TYPES.STOPPING,
  src\loopService.js:403:          task.title,
> src\loopService.js:404:          taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIso() }),
  src\loopService.js:405:        );
  src\loopService.js:406:      }
  src\loopService.js:407:    } else {
> src\loopService.js:408:      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = 
?', [
  src\loopService.js:409:        'pending',
  src\loopService.js:410:        nowIso(),
  src\loopService.js:411:        taskId,
  src\loopService.js:412:        'running',
  src\loopService.js:413:      ]);
  src\loopService.js:414:      this.addEvent(
  src\loopService.js:415:        projectId,
  src\loopService.js:416:        TASK_EVENT_TYPES.STOP_REQUESTED,
  src\loopService.js:417:        task.title,
> src\loopService.js:418:        taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPING }),
  src\loopService.js:419:      );
  src\loopService.js:420:    }
  src\loopService.js:421:
  src\loopService.js:422:    if (runtime?.running) this.stop(projectId);
  src\loopService.js:423:    else this.setPhase(projectId, 'stopped');
  src\loopService.js:424:  }
  src\loopService.js:425:
  src\loopService.js:426:  taskForProject(projectId, taskId) {
  src\loopService.js:427:    if (!taskId) return null;
  src\loopService.js:428:    return this.db.get(
> src\loopService.js:429:      `SELECT plan_tasks.*
> src\loopService.js:430:       FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
> src\loopService.js:431:       WHERE plan_tasks.id = ? AND plans.project_id = ?`,
  src\loopService.js:432:      [taskId, projectId],
  src\loopService.js:433:    );
  src\loopService.js:434:  }
  src\loopService.js:435:
  src\loopService.js:436:  /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
  src\loopService.js:437:  interruptPlan(projectId, planId) {
  src\loopService.js:438:    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, p
rojectId]);
  src\loopService.js:439:    if (!plan) return;
  src\loopService.js:446:      killChildProcess(activeEntry.child);
  src\loopService.js:447:      const activeTaskId = activeEntry.operation?.taskId;
  src\loopService.js:448:      const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
  src\loopService.js:449:      if (activeTaskId) {
> src\loopService.js:450:        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked
', nowIso(), activeTaskId]);
  src\loopService.js:451:      }
  src\loopService.js:452:      this.addEvent(
  src\loopService.js:453:        projectId,
  src\loopService.js:454:        LEGACY_TASK_EVENT_TYPES.STOPPING,
  src\loopService.js:455:        `plan #${planId} 的任务被中断`,
  src\loopService.js:456:        taskEventMeta(activeTask, {
  src\loopService.js:457:          planId,
  src\loopService.js:458:          taskId: activeTaskId || undefined,
> src\loopService.js:459:          status: TASK_EVENT_STATUS.INTERRUPTED,
  src\loopService.js:460:          finishedAt: nowIso(),
  src\loopService.js:461:        }),
  src\loopService.js:462:      );
  src\loopService.js:463:    }
  src\loopService.js:464:    // 其余未完成任务 → blocked
  src\loopService.js:465:    this.db.run(
> src\loopService.js:466:      `UPDATE plan_tasks SET status = ?, updated_at = ?
> src\loopService.js:467:       WHERE plan_id = ? AND status IN ('pending', 'running')`,
  src\loopService.js:468:      ['blocked', nowIso(), planId],
  src\loopService.js:469:    );
> src\loopService.js:470:    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['interrupted', no
wIso(), planId]);
  src\loopService.js:471:    this.addEvent(projectId, 'plan.interrupted', `plan #${planId} 已中断，未完成任务已挂起`);
  src\loopService.js:472:    this.emitUpdate(projectId);
  src\loopService.js:473:  }
  src\loopService.js:474:
  src\loopService.js:475:  /** 恢复被中断的 plan：blocked → pending，plan → pending，循环运行时自动继续执行 */
  src\loopService.js:476:  resumePlan(projectId, planId) {
  src\loopService.js:477:    this.db.run(
> src\loopService.js:478:      `UPDATE plan_tasks SET status = ?, updated_at = ?
> src\loopService.js:479:       WHERE plan_id = ? AND status = ?`,
  src\loopService.js:480:      ['pending', nowIso(), planId, 'blocked'],
  src\loopService.js:481:    );
> src\loopService.js:482:    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso
(), planId]);
  src\loopService.js:483:    this.addEvent(projectId, 'plan.resumed', `plan #${planId} 已恢复`);
  src\loopService.js:484:    this.emitUpdate(projectId);
  src\loopService.js:485:  }
  src\loopService.js:486:
> src\loopService.js:487:  /** 向现有 plan 追加一个任务：写回 plan 文件 + syncPlanTasks 重新解析 */
  src\loopService.js:488:  appendTask(projectId, planId, title) {
  src\loopService.js:489:    const project = this.project(projectId);
  src\loopService.js:490:    const workspace = project?.workspace_path;
  src\loopService.js:491:    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, p
rojectId]);
  src\loopService.js:492:    if (!plan) throw new Error('计划不存在');
  src\loopService.js:493:    if (!workspace) throw new Error('请先设置项目工作区路径');
  src\loopService.js:494:
  src\loopService.js:495:    const planFile = path.join(workspace, plan.file_path);
  src\loopService.js:497:
  src\loopService.js:498:    const cleanTitle = String(title || '').trim();
  src\loopService.js:499:    if (!cleanTitle) throw new Error('任务标题不能为空');
  src\loopService.js:500:
> src\loopService.js:501:    // 计算下一个 task_key
> src\loopService.js:502:    const existing = this.db.all('SELECT task_key FROM plan_tasks WHERE plan_id = ? ORDER BY s
ort_order', [planId]);
  src\loopService.js:503:    const maxNum = existing.reduce((max, row) => {
> src\loopService.js:504:      const m = String(row.task_key || '').match(/P0*(\d+)/i);
  src\loopService.js:505:      return m ? Math.max(max, Number(m[1])) : max;
  src\loopService.js:506:    }, 0);
  src\loopService.js:507:    const taskKey = `P${String(maxNum + 1).padStart(3, '0')}`;
  src\loopService.js:508:
  src\loopService.js:509:    // 追加到 plan 文件的"## 任务计划"段末尾
  src\loopService.js:510:    let content = fs.readFileSync(planFile, 'utf8');
  src\loopService.js:511:    const line = ensureTaskScopeComment(`- [ ] ${taskKey}: ${cleanTitle}`);
  src\loopService.js:512:    const taskSectionIdx = content.search(/##\s*任务计划/);
  src\loopService.js:517:    }
  src\loopService.js:518:    fs.writeFileSync(planFile, content, 'utf8');
  src\loopService.js:519:
  src\loopService.js:520:    // 若 plan 被中断过，恢复为 pending 让新任务可执行
> src\loopService.js:521:    if (plan.status === 'interrupted') {
> src\loopService.js:522:      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowI
so(), planId]);
  src\loopService.js:523:    }
  src\loopService.js:524:    // 重新解析任务入库
> src\loopService.js:525:    this.syncPlanTasks(planId, planFile);
> src\loopService.js:526:    const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [p
lanId, taskKey]);
  src\loopService.js:527:    this.addEvent(
  src\loopService.js:528:      projectId,
  src\loopService.js:529:      'task.appended',
  src\loopService.js:530:      `追加 ${taskKey}: ${cleanTitle}`,
  src\loopService.js:531:      taskEventMeta(task, {
  src\loopService.js:532:        planId,
  src\loopService.js:533:        taskKey,
  src\loopService.js:534:        taskTitle: cleanTitle,
> src\loopService.js:535:        status: TASK_EVENT_STATUS.PENDING,
  src\loopService.js:536:      }),
  src\loopService.js:537:    );
  src\loopService.js:538:    this.emitUpdate(projectId);
  src\loopService.js:539:
  src\loopService.js:540:    // 循环在运行则立即拾取
> src\loopService.js:541:    if (this.status(projectId)?.running) {
  src\loopService.js:542:      this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
  src\loopService.js:543:    }
  src\loopService.js:544:    return taskKey;
  src\loopService.js:545:  }
  src\loopService.js:546:
  src\loopService.js:547:  ensureWorkspaceDirs(workspace) {
  src\loopService.js:548:    for (const dir of ['docs/issues', 'docs/plan', 'docs/progress', 'docs/progress/logs']) {
  src\loopService.js:549:      fs.mkdirSync(path.join(workspace, dir), { recursive: true });
  src\loopService.js:599:
  src\loopService.js:600:  nextRunnablePlan(projectId) {
  src\loopService.js:601:    return this.db.get(
  src\loopService.js:602:      `SELECT * FROM plans
> src\loopService.js:603:       WHERE project_id = ? AND status NOT IN ('completed', 'interrupted')
  src\loopService.js:604:       ORDER BY created_at ASC
  src\loopService.js:605:       LIMIT 1`,
  src\loopService.js:606:      [projectId],
  src\loopService.js:607:    );
  src\loopService.js:608:  }
  src\loopService.js:609:
  src\loopService.js:610:  async generatePlan(projectId, workspace, issueScan) {
  src\loopService.js:611:    this.setPhase(projectId, 'generate-plan');
  src\loopService.js:646:    }
  src\loopService.js:647:
  src\loopService.js:648:    const id = this.db.insert(
  src\loopService.js:649:      `INSERT INTO plans
> src\loopService.js:650:       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validat
ion_passed, created_at, updated_at)
  src\loopService.js:651:       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
  src\loopService.js:652:      [
  src\loopService.js:653:        projectId,
  src\loopService.js:654:        issueScan.aggregateHash,
  src\loopService.js:655:        normalizeRelative(workspace, planFile),
  src\loopService.js:656:        hashFile(planFile),
  src\loopService.js:657:        'pending',
  src\loopService.js:658:        nowIso(),
  src\loopService.js:659:        nowIso(),
  src\loopService.js:660:      ],
  src\loopService.js:661:    );
> src\loopService.js:662:    this.syncPlanTasks(id, planFile);
  src\loopService.js:663:    this.db.run('UPDATE project_states SET last_issue_hash = ?, updated_at = ? WHERE project_i
d = ?', [
  src\loopService.js:664:      issueScan.aggregateHash,
  src\loopService.js:665:      nowIso(),
  src\loopService.js:666:      projectId,
  src\loopService.js:667:    ]);
  src\loopService.js:668:    this.addEvent(projectId, 'plan.generated', normalizeRelative(workspace, planFile));
  src\loopService.js:669:  }
  src\loopService.js:670:
  src\loopService.js:705:
  src\loopService.js:706:    const issueHash = `${intake.__type}-${intake.id}-${hashText(String(intake.body || '')).sli
ce(0, 16)}`;
  src\loopService.js:707:    const id = this.db.insert(
  src\loopService.js:708:      `INSERT INTO plans
> src\loopService.js:709:       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validat
ion_passed, created_at, updated_at)
  src\loopService.js:710:       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
  src\loopService.js:711:      [projectId, issueHash, normalizeRelative(workspace, planFile), hashFile(planFile), 'pend
ing', nowIso(), nowIso()],
  src\loopService.js:712:    );
> src\loopService.js:713:    this.syncPlanTasks(id, planFile);
  src\loopService.js:714:    // 回写关联
  src\loopService.js:715:    this.db.run(`UPDATE ${table} SET linked_plan_id = ?, updated_at = ? WHERE id = ?`, [id, no
wIso(), intake.id]);
  src\loopService.js:716:    this.addEvent(projectId, 'plan.generated', `${sourceName} #${intake.id} 已生成计划：${normalizeR
elative(workspace, planFile)}`);
  src\loopService.js:717:    return id;
  src\loopService.js:718:  }
  src\loopService.js:719:
  src\loopService.js:720:  async processPlan(workspace, plan) {
  src\loopService.js:721:    const planFile = path.join(workspace, plan.file_path);
> src\loopService.js:722:    this.syncPlanTasks(plan.id, planFile);
  src\loopService.js:723:    const pendingTasks = this.db.all(
> src\loopService.js:724:      `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
  src\loopService.js:725:       ORDER BY sort_order ASC`,
  src\loopService.js:726:      [plan.id],
  src\loopService.js:727:    );
  src\loopService.js:728:    if (pendingTasks.length) {
  src\loopService.js:729:      const batch = this.parallelTaskBatch(pendingTasks);
  src\loopService.js:730:      if (batch.length > 1) {
  src\loopService.js:731:        await this.executeTaskBatch(workspace, plan, batch);
  src\loopService.js:732:      } else {
  src\loopService.js:762:    this.setPhase(plan.project_id, 'execute-task');
  src\loopService.js:763:    this.addEvent(
  src\loopService.js:764:      plan.project_id,
  src\loopService.js:765:      'tasks.parallel.started',
> src\loopService.js:766:      `并发执行 ${tasks.map((task) => task.task_key).join(', ')}`,
  src\loopService.js:767:      { taskIds: tasks.map((task) => task.id) },
  src\loopService.js:768:    );
  src\loopService.js:769:    const results = await Promise.all(
  src\loopService.js:770:      tasks.map(async (task) => {
  src\loopService.js:771:        try {
  src\loopService.js:772:          return {
  src\loopService.js:773:            task,
  src\loopService.js:774:            result: await this.executeTask(workspace, plan, task, { parallel: true }),
  src\loopService.js:779:            TASK_EVENT_TYPES.FAILED,
  src\loopService.js:780:            task.title,
  src\loopService.js:781:            taskEventMeta(task, {
  src\loopService.js:782:              planId: plan.id,
> src\loopService.js:783:              status: TASK_EVENT_STATUS.FAILED,
  src\loopService.js:784:              finishedAt: nowIso(),
  src\loopService.js:785:              error: error?.message || String(error),
  src\loopService.js:786:            }),
  src\loopService.js:787:          );
  src\loopService.js:788:          return { task, result: { exitCode: -1 } };
  src\loopService.js:789:        }
  src\loopService.js:790:      }),
  src\loopService.js:791:    );
  src\loopService.js:797:  }
  src\loopService.js:798:
  src\loopService.js:799:  async executeTask(workspace, plan, task, options = {}) {
  src\loopService.js:800:    this.setPhase(plan.project_id, 'execute-task');
> src\loopService.js:801:    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', n
owIso(), task.id]);
  src\loopService.js:802:    this.emitUpdate(plan.project_id);
  src\loopService.js:803:    const planFile = path.join(workspace, plan.file_path);
  src\loopService.js:804:    const completionRules = [
  src\loopService.js:805:      '- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区',
  src\loopService.js:806:      '- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区',
  src\loopService.js:807:      '- 只修改当前任务 scope 直接相关的业务文件',
  src\loopService.js:808:      '- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试',
  src\loopService.js:809:      '- 只有当前任务本身明确要求全量验证时，才允许运行全量测试',
  src\loopService.js:814:      completionRules.unshift('- 当前为并发执行模式，不要读写其它任务的 scope');
  src\loopService.js:815:    }
  src\loopService.js:816:    const prompt = [
  src\loopService.js:817:      '你是开发执行者。',
> src\loopService.js:818:      `请只执行指定任务 ${task.task_key}，不要提前执行其它任务，也不要顺手处理其它 checkbox。`,
  src\loopService.js:819:      '',
  src\loopService.js:820:      `plan 文件（只读）：${planFile}`,
  src\loopService.js:821:      `指定任务：${task.raw_line}`,
  src\loopService.js:822:      `任务 scope：${task.scope || taskScopeText(task) || 'unknown'}`,
  src\loopService.js:823:      '',
  src\loopService.js:824:      '完成后必须：',
  src\loopService.js:825:      ...completionRules,
  src\loopService.js:826:    ].join('\n');
> src\loopService.js:827:    const result = await this.runCodexWithPlanGuard(workspace, prompt, `execute-${task.task_ke
y}`, {
  src\loopService.js:828:      projectId: plan.project_id,
  src\loopService.js:829:      planId: plan.id,
  src\loopService.js:830:      taskId: task.id,
  src\loopService.js:831:      parallel: Boolean(options.parallel),
  src\loopService.js:832:    }, planFile);
  src\loopService.js:833:    if (result.exitCode !== 0) {
> src\loopService.js:834:      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending',
 nowIso(), task.id]);
  src\loopService.js:835:      this.emitUpdate(plan.project_id);
  src\loopService.js:836:    }
  src\loopService.js:837:    const succeeded = result.exitCode === 0;
  src\loopService.js:838:    this.addEvent(
  src\loopService.js:839:      plan.project_id,
  src\loopService.js:840:      succeeded ? LEGACY_TASK_EVENT_TYPES.EXECUTED : TASK_EVENT_TYPES.FAILED,
  src\loopService.js:841:      task.title,
  src\loopService.js:842:      taskEventMeta(task, {
  src\loopService.js:843:        planId: plan.id,
> src\loopService.js:844:        status: succeeded ? TASK_EVENT_STATUS.COMPLETED : TASK_EVENT_STATUS.FAILED,
  src\loopService.js:845:        finishedAt: nowIso(),
  src\loopService.js:846:        exitCode: result.exitCode,
  src\loopService.js:847:        log: result.logFile,
  src\loopService.js:848:      }),
  src\loopService.js:849:    );
  src\loopService.js:850:    return result;
  src\loopService.js:851:  }
  src\loopService.js:852:
  src\loopService.js:853:  completeTask(workspace, plan, task, result) {
  src\loopService.js:854:    const planFile = path.join(workspace, plan.file_path);
  src\loopService.js:855:    this.markTaskCompletedInPlan(workspace, planFile, task, result);
> src\loopService.js:856:    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['completed',
 nowIso(), task.id]);
  src\loopService.js:857:    this.refreshPlanProgress(plan.id, planFile);
  src\loopService.js:858:    this.emitUpdate(plan.project_id);
  src\loopService.js:859:  }
  src\loopService.js:860:
  src\loopService.js:861:  refreshPlanProgress(planId, planFile) {
  src\loopService.js:862:    const totals = this.db.get(
  src\loopService.js:863:      `SELECT COUNT(*) AS total,
> src\loopService.js:864:              SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) AS completed
> src\loopService.js:865:       FROM plan_tasks
  src\loopService.js:866:       WHERE plan_id = ?`,
  src\loopService.js:867:      [planId],
  src\loopService.js:868:    ) || { total: 0, completed: 0 };
  src\loopService.js:869:    const total = Number(totals.total || 0);
  src\loopService.js:870:    const completed = Number(totals.completed || 0);
> src\loopService.js:871:    const status = total > 0 && completed === total ? 'ready_for_validation' : 'running';
  src\loopService.js:872:    const hash = fs.existsSync(planFile) ? hashFile(planFile) : '';
  src\loopService.js:873:    this.db.run(
> src\loopService.js:874:      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at
 = ? WHERE id = ?',
> src\loopService.js:875:      [hash, status, total, completed, nowIso(), planId],
  src\loopService.js:876:    );
  src\loopService.js:877:  }
  src\loopService.js:878:
  src\loopService.js:879:  markTaskCompletedInPlan(workspace, planFile, task, result) {
  src\loopService.js:880:    if (!fs.existsSync(planFile)) return;
  src\loopService.js:881:    const relativeLog = result?.logFile ? normalizeRelative(workspace, result.logFile) : '';
> src\loopService.js:882:    const key = escapeRegExp(String(task.task_key || ''));
  src\loopService.js:883:    const checkboxRe = new RegExp(`(^\\s*[-*]\\s+\\[)([ xX])(\\]\\s+${key}(?:\\b|[:：\\s-]))`, 
'm');
  src\loopService.js:884:    let content = fs.readFileSync(planFile, 'utf8');
  src\loopService.js:885:    if (checkboxRe.test(content)) {
  src\loopService.js:886:      content = content.replace(checkboxRe, '$1x$3');
  src\loopService.js:887:    }
  src\loopService.js:888:
> src\loopService.js:889:    const note = `- ${task.task_key} AutoPlan 完成：${nowIso()}${relativeLog ? `；日志：${relativeLog
}` : ''}`;
  src\loopService.js:890:    const noteRe = new RegExp(`^\\s*-\\s+${key}\\s+AutoPlan 完成：.*$`, 'm');
  src\loopService.js:891:    if (noteRe.test(content)) {
  src\loopService.js:892:      content = content.replace(noteRe, note);
  src\loopService.js:893:    } else if (/##\s*进度区/.test(content)) {
  src\loopService.js:894:      content = `${content.trimEnd()}\n${note}\n`;
  src\loopService.js:895:    } else {
  src\loopService.js:896:      content = `${content.trimEnd()}\n\n## 进度区\n${note}\n`;
  src\loopService.js:897:    }
  src\loopService.js:900:
  src\loopService.js:901:  async validatePlan(workspace, plan) {
  src\loopService.js:902:    this.setPhase(plan.project_id, 'validate');
  src\loopService.js:903:    const planFile = path.join(workspace, plan.file_path);
> src\loopService.js:904:    const command = String(this.status(plan.project_id)?.validation_command || '').trim();
  src\loopService.js:905:    if (!command) {
  src\loopService.js:906:      // 验收命令为空：跳过校验，直接标记完成。
  src\loopService.js:907:      this.db.run(
> src\loopService.js:908:        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
  src\loopService.js:909:        ['completed', nowIso(), plan.id],
  src\loopService.js:910:      );
  src\loopService.js:911:      this.addEvent(plan.project_id, 'plan.completed', '任务全部完成（验收命令为空，已跳过校验）');
  src\loopService.js:912:      return;
  src\loopService.js:913:    }
  src\loopService.js:914:    let validation = await this.runShell(workspace, command, `validate-${plan.id}`, { projectI
d: plan.project_id });
  src\loopService.js:915:    for (let attempt = 1; validation.exitCode !== 0 && attempt <= 2; attempt += 1) {
> src\loopService.js:916:      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', [
  src\loopService.js:917:        'validation_failed',
  src\loopService.js:918:        nowIso(),
  src\loopService.js:919:        plan.id,
  src\loopService.js:920:      ]);
  src\loopService.js:921:      const prompt = [
  src\loopService.js:922:        'plan 已完成，但宿主项目验收失败。请只修复验证错误。',
  src\loopService.js:923:        `plan 文件（只读）：${planFile}`,
  src\loopService.js:924:        '不要修改 plan 文件、checkbox 或进度区。',
  src\loopService.js:936:    }
  src\loopService.js:937:
  src\loopService.js:938:    if (validation.exitCode === 0) {
  src\loopService.js:939:      this.db.run(
> src\loopService.js:940:        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
  src\loopService.js:941:        ['completed', nowIso(), plan.id],
  src\loopService.js:942:      );
  src\loopService.js:943:      this.addEvent(plan.project_id, 'plan.completed', plan.file_path);
  src\loopService.js:944:    } else {
  src\loopService.js:945:      this.addEvent(plan.project_id, 'validation.failed', validation.logFile);
  src\loopService.js:946:    }
  src\loopService.js:947:  }
  src\loopService.js:948:
> src\loopService.js:949:  syncPlanTasks(planId, planFile) {
  src\loopService.js:950:    if (!fs.existsSync(planFile)) return;
  src\loopService.js:951:    normalizePlanTaskScopes(planFile);
  src\loopService.js:952:    const text = fs.readFileSync(planFile, 'utf8');
  src\loopService.js:953:    const regex = /^\s*[-*]\s+\[([ xX])\]\s+(.+)$/gm;
  src\loopService.js:954:    const tasks = [];
  src\loopService.js:955:    let match;
  src\loopService.js:956:    let index = 0;
  src\loopService.js:957:    while ((match = regex.exec(text))) {
  src\loopService.js:963:        key: idMatch?.[1] || `P${String(index).padStart(3, '0')}`,
  src\loopService.js:964:        title: idMatch?.[2]?.trim() || titleWithoutScope || rawTitle,
  src\loopService.js:965:        rawLine: ensureTaskScopeComment(match[0]),
  src\loopService.js:966:        scope: taskScopeText({ raw_line: match[0], title: rawTitle }),
> src\loopService.js:967:        status: match[1].toLowerCase() === 'x' ? 'completed' : 'pending',
  src\loopService.js:968:        sortOrder: index,
  src\loopService.js:969:      });
  src\loopService.js:970:    }
  src\loopService.js:971:
> src\loopService.js:972:    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
  src\loopService.js:973:    for (const task of tasks) {
  src\loopService.js:974:      this.db.run(
> src\loopService.js:975:        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order
, updated_at)
  src\loopService.js:976:         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
> src\loopService.js:977:        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, 
nowIso()],
  src\loopService.js:978:      );
  src\loopService.js:979:    }
  src\loopService.js:980:
> src\loopService.js:981:    const completed = tasks.filter((task) => task.status === 'completed').length;
> src\loopService.js:982:    const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : '
running';
  src\loopService.js:983:    this.db.run(
> src\loopService.js:984:      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at
 = ? WHERE id = ?',
> src\loopService.js:985:      [hashFile(planFile), status, tasks.length, completed, nowIso(), planId],
  src\loopService.js:986:    );
  src\loopService.js:987:  }
  src\loopService.js:988:
  src\loopService.js:989:  /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
  src\loopService.js:990:  archiveOperation(projectId, operationKey) {
  src\loopService.js:991:    const runtime = this.runtime(projectId);
  src\loopService.js:992:    if (!runtime) return;
  src\loopService.js:993:    const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOpera
tion;
  src\loopService.js:1188:    const activeProject = this.project(projectId);
  src\loopService.js:1189:    if (!activeProject) return emptySnapshot(projects);
  src\loopService.js:1190:
  src\loopService.js:1191:    const state = {
> src\loopService.js:1192:      ...(this.status(projectId) || {}),
  src\loopService.js:1193:      workspace_path: activeProject.workspace_path || '',
  src\loopService.js:1194:    };
  src\loopService.js:1195:    const runtime = this.existingRuntime(projectId);
  src\loopService.js:1196:
  src\loopService.js:1197:    return {
  src\loopService.js:1198:      activeProjectId: projectId,
  src\loopService.js:1199:      activeProject,
  src\loopService.js:1200:      projects,
  src\loopService.js:1201:      state,
  src\loopService.js:1202:      requirements: this.db.all(
> src\loopService.js:1203:        `SELECT requirements.*, plans.status AS plan_status,
  src\loopService.js:1204:                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
  src\loopService.js:1205:         FROM requirements
  src\loopService.js:1206:         LEFT JOIN plans ON plans.id = requirements.linked_plan_id
  src\loopService.js:1207:         WHERE requirements.project_id = ?
  src\loopService.js:1208:         ORDER BY requirements.updated_at DESC`,
  src\loopService.js:1209:        [projectId],
  src\loopService.js:1210:      ),
  src\loopService.js:1211:      feedback: this.db.all(
> src\loopService.js:1212:        `SELECT feedback.*, plans.status AS plan_status,
  src\loopService.js:1213:                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
  src\loopService.js:1214:         FROM feedback
  src\loopService.js:1215:         LEFT JOIN plans ON plans.id = feedback.linked_plan_id
  src\loopService.js:1216:         WHERE feedback.project_id = ?
  src\loopService.js:1217:         ORDER BY feedback.updated_at DESC`,
  src\loopService.js:1218:        [projectId],
  src\loopService.js:1219:      ),
  src\loopService.js:1220:      attachments: this.db.all(
  src\loopService.js:1225:        projectId,
  src\loopService.js:1226:      ]),
  src\loopService.js:1227:      plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC',
 [projectId]),
  src\loopService.js:1228:      tasks: this.db.all(
> src\loopService.js:1229:        `SELECT plan_tasks.*, plans.file_path
> src\loopService.js:1230:         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
  src\loopService.js:1231:         WHERE plans.project_id = ?
> src\loopService.js:1232:         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
  src\loopService.js:1233:        [projectId],
  src\loopService.js:1234:      ),
  src\loopService.js:1235:      events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 8
0', [projectId]),
  src\loopService.js:1236:      scans: this.db.all(
  src\loopService.js:1237:        'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path AS
C',
  src\loopService.js:1238:        [projectId],
  src\loopService.js:1239:      ),
  src\loopService.js:1240:      activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) 
=== Number(projectId)
  src\loopService.js:1272:function taskEventMeta(task, overrides = {}) {
  src\loopService.js:1273:  const meta = {
  src\loopService.js:1274:    ...compactEventMeta({
  src\loopService.js:1275:      taskId: task?.id,
> src\loopService.js:1276:      taskKey: task?.task_key,
  src\loopService.js:1277:      taskTitle: task?.title,
  src\loopService.js:1278:      planId: task?.plan_id,
> src\loopService.js:1279:      status: task?.status,
  src\loopService.js:1280:      startedAt: task?.started_at,
  src\loopService.js:1281:      finishedAt: task?.finished_at,
  src\loopService.js:1282:    }),
  src\loopService.js:1283:    ...compactEventMeta(overrides),
  src\loopService.js:1284:  };
  src\loopService.js:1285:  meta.taskId = normalizeOptionalNumber(meta.taskId);
  src\loopService.js:1286:  meta.planId = normalizeOptionalNumber(meta.planId);
  src\loopService.js:1287:  meta.taskKey = normalizeOptionalString(meta.taskKey);
  src\loopService.js:1288:  meta.taskTitle = normalizeOptionalString(meta.taskTitle);
> src\loopService.js:1289:  meta.status = normalizeOptionalString(meta.status);
  src\loopService.js:1290:  meta.startedAt = normalizeOptionalString(meta.startedAt);
  src\loopService.js:1291:  meta.finishedAt = normalizeOptionalString(meta.finishedAt);
  src\loopService.js:1292:  const compacted = compactEventMeta(meta);
  src\loopService.js:1293:  return Object.keys(compacted).length ? compacted : null;
  src\loopService.js:1294:}
  src\loopService.js:1295:
  src\loopService.js:1296:function compactEventMeta(meta) {
  src\loopService.js:1297:  const result = {};
  src\loopService.js:1428:  return crypto.createHash('sha256').update(text).digest('hex');
  src\loopService.js:1429:}
  src\loopService.js:1430:
  src\loopService.js:1431:function taskParallelScopes(task) {
> src\loopService.js:1432:  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  src\loopService.js:1433:  if (PARALLEL_BLOCKING_TASK_RE.test(raw)) return [];
  src\loopService.js:1434:  return taskDeclaredScopes(task, { keepUnknown: false });
  src\loopService.js:1435:}
  src\loopService.js:1436:
  src\loopService.js:1437:function taskScopeText(task) {
  src\loopService.js:1438:  const explicit = taskDeclaredScopes(task, { keepUnknown: true, includePathFallback: false }
);
  src\loopService.js:1439:  if (explicit.length) return explicit.join(', ');
  src\loopService.js:1440:  const inferred = taskDeclaredScopes(task, { keepUnknown: false });
  src\loopService.js:1442:}
  src\loopService.js:1443:
  src\loopService.js:1444:function taskDeclaredScopes(task, options = {}) {
  src\loopService.js:1445:  const { keepUnknown = false, includePathFallback = true } = options;
> src\loopService.js:1446:  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  src\loopService.js:1447:  const scopes = new Set();
  src\loopService.js:1448:
  src\loopService.js:1449:  addScopeParts(scopes, String(task.scope || '').split(TASK_SCOPE_SPLIT_RE), { keepUnknown })
;
  src\loopService.js:1450:  addScopeParts(scopes, explicitTaskScopeParts(raw), { keepUnknown });
  src\loopService.js:1451:
  src\loopService.js:1452:  if (includePathFallback) {
  src\loopService.js:1453:    for (const match of raw.matchAll(TASK_PATH_RE)) {
  src\loopService.js:1454:      const scope = normalizeTaskScope(match[0], { keepUnknown });
  src\loopService.js:1545:  LoopService,
  src\loopService.js:1546:  LEGACY_TASK_EVENT_TYPES,
  src\loopService.js:1547:  TASK_EVENT_COMPATIBILITY,
  src\loopService.js:1548:  TASK_EVENT_SEMANTICS,
> src\loopService.js:1549:  TASK_EVENT_STATUS,
  src\loopService.js:1550:  TASK_EVENT_TYPES,
  src\loopService.js:1551:};



**Focusing on output size**

I realize the output is too large, and I need to narrow it down. My next step involves focusing on specific line ranges around syncPlanTasks. To do this efficiently, I'll use the Select-String function to pinpoint the location I need. This way, I can manage the output better and make it more digestible. It feels good to have a clearer path forward!
codex
`syncPlanTasks` 在同一文件内，接下来读取它周边的小范围代码。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; "'$match=Select-String -LiteralPath $path -Pattern '"'syncPlanTasks\\(' | Select-Object -First 1; "'$start=[Math]::Max(1,$match.LineNumber-20); $end=$match.LineNumber+120; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }' in D:\project\GitHub\autoplan
 succeeded in 237ms:
  505:       return m ? Math.max(max, Number(m[1])) : max;
  506:     }, 0);
  507:     const taskKey = `P${String(maxNum + 1).padStart(3, '0')}`;
  508: 
  509:     // 追加到 plan 文件的"## 任务计划"段末尾
  510:     let content = fs.readFileSync(planFile, 'utf8');
  511:     const line = ensureTaskScopeComment(`- [ ] ${taskKey}: ${cleanTitle}`);
  512:     const taskSectionIdx = content.search(/##\s*任务计划/);
  513:     if (taskSectionIdx === -1) {
  514:       content = `${content.trim()}\n\n## 任务计划\n${line}\n`;
  515:     } else {
  516:       content = `${content.trimEnd()}\n${line}\n`;
  517:     }
  518:     fs.writeFileSync(planFile, content, 'utf8');
  519: 
  520:     // 若 plan 被中断过，恢复为 pending 让新任务可执行
  521:     if (plan.status === 'interrupted') {
  522:       this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
  523:     }
  524:     // 重新解析任务入库
  525:     this.syncPlanTasks(planId, planFile);
  526:     const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [planId, taskKey]);
  527:     this.addEvent(
  528:       projectId,
  529:       'task.appended',
  530:       `追加 ${taskKey}: ${cleanTitle}`,
  531:       taskEventMeta(task, {
  532:         planId,
  533:         taskKey,
  534:         taskTitle: cleanTitle,
  535:         status: TASK_EVENT_STATUS.PENDING,
  536:       }),
  537:     );
  538:     this.emitUpdate(projectId);
  539: 
  540:     // 循环在运行则立即拾取
  541:     if (this.status(projectId)?.running) {
  542:       this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
  543:     }
  544:     return taskKey;
  545:   }
  546: 
  547:   ensureWorkspaceDirs(workspace) {
  548:     for (const dir of ['docs/issues', 'docs/plan', 'docs/progress', 'docs/progress/logs']) {
  549:       fs.mkdirSync(path.join(workspace, dir), { recursive: true });
  550:     }
  551:   }
  552: 
  553:   scanDirectory(root, workspace, extensions) {
  554:     if (!fs.existsSync(root)) return { root, aggregateHash: hashText(''), files: [] };
  555:     const files = [];
  556:     const visit = (dir) => {
  557:       for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
  558:         const full = path.join(dir, entry.name);
  559:         if (entry.isDirectory()) {
  560:           visit(full);
  561:         } else if (entry.isFile() && extensions.includes(path.extname(entry.name).toLowerCase())) {
  562:           const stat = fs.statSync(full);
  563:           files.push({
  564:             path: normalizeRelative(workspace, full),
  565:             hash: hashFile(full),
  566:             size: stat.size,
  567:             modifiedAt: stat.mtime.toISOString(),
  568:           });
  569:         }
  570:       }
  571:     };
  572:     visit(root);
  573:     files.sort((a, b) => a.path.localeCompare(b.path));
  574:     return {
  575:       root,
  576:       aggregateHash: hashText(files.map((file) => `${file.path}|${file.hash}|${file.size}`).join('\n')),
  577:       files,
  578:     };
  579:   }
  580: 
  581:   saveScan(projectId, type, scan) {
  582:     const scannedAt = nowIso();
  583:     this.db.run('DELETE FROM scan_files WHERE project_id = ? AND scan_type = ?', [projectId, type]);
  584:     for (const file of scan.files) {
  585:       this.db.run(
  586:         `INSERT OR REPLACE INTO scan_files
  587:          (project_id, scan_type, file_path, hash, size, modified_at, scanned_at)
  588:          VALUES (?, ?, ?, ?, ?, ?, ?)`,
  589:         [projectId, type, file.path, file.hash, file.size, file.modifiedAt, scannedAt],
  590:       );
  591:     }
  592:   }
  593: 
  594:   hasPlanForIssueHash(projectId, issueHash) {
  595:     return Boolean(
  596:       this.db.get('SELECT id FROM plans WHERE project_id = ? AND issue_hash = ? LIMIT 1', [projectId, issueHash]),
  597:     );
  598:   }
  599: 
  600:   nextRunnablePlan(projectId) {
  601:     return this.db.get(
  602:       `SELECT * FROM plans
  603:        WHERE project_id = ? AND status NOT IN ('completed', 'interrupted')
  604:        ORDER BY created_at ASC
  605:        LIMIT 1`,
  606:       [projectId],
  607:     );
  608:   }
  609: 
  610:   async generatePlan(projectId, workspace, issueScan) {
  611:     this.setPhase(projectId, 'generate-plan');
  612:     const planFile = path.join(
  613:       workspace,
  614:       'docs',
  615:       'plan',
  616:       `plan_${timestampForPath()}_${issueScan.aggregateHash.slice(0, 8)}.md`,
  617:     );
  618:     const issueBundle = issueScan.files
  619:       .map((file) => {
  620:         const full = path.join(workspace, file.path);
  621:         return ['---', `path: ${file.path}`, `hash: ${file.hash}`, 'content:', readSnippet(full, 20000)].join('\n');
  622:       })
  623:       .join('\n');
  624: 
  625:     const prompt = [
  626:       '你是需求整理与开发计划生成者。',
  627:       '请根据 docs/issues 收集到的反馈和需求，生成一个开发计划和验收标准。',
  628:       '',
  629:       `输出文件：${planFile}`,
  630:       '',
  631:       '格式要求：',
  632:       '- 每个任务必须严格使用固定格式：- [ ] P001: 任务标题 <!-- scope: lib/foo.dart,test/foo_test.dart -->',
  633:       '- scope 必填，表示该任务预计修改的文件或模块；多个 scope 用英文逗号分隔；无法判断时写 <!-- scope: unknown -->，unknown 任务不会并发执行',
  634:       '- 每个任务要有验收要点',
  635:       '- 必须包含总体验收标准和进度区',
  636:       '- 只写 plan 文件，不要改业务代码',
  637:       '',
  638:       '需求快照：',
  639:       issueBundle,
  640:     ].join('\n');
  641: 
  642:     const result = await this.runCodex(workspace, prompt, 'generate-plan', { projectId });
  643:     if (result.exitCode !== 0 || !fs.existsSync(planFile)) {
  644:       this.addEvent(projectId, 'plan.generate.failed', result.logFile);
  645:       return;

codex
先前命中的是调用点；我继续定位方法定义，避免扩大读取范围。
exec
powershell.exe -Command "Select-String -LiteralPath 'src\\loopService.js' -Pattern '"'^'"\\s*syncPlanTasks\\s*\\(' | ForEach-Object { "'$_.LineNumber }' in D:\project\GitHub\autoplan
 succeeded in 182ms:
949

codex
找到定义位置了，下面只读取该方法及相邻解析逻辑。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; "'$start=900; $end=1045; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }' in D:\project\GitHub\autoplan
 succeeded in 183ms:
  900: 
  901:   async validatePlan(workspace, plan) {
  902:     this.setPhase(plan.project_id, 'validate');
  903:     const planFile = path.join(workspace, plan.file_path);
  904:     const command = String(this.status(plan.project_id)?.validation_command || '').trim();
  905:     if (!command) {
  906:       // 验收命令为空：跳过校验，直接标记完成。
  907:       this.db.run(
  908:         'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
  909:         ['completed', nowIso(), plan.id],
  910:       );
  911:       this.addEvent(plan.project_id, 'plan.completed', '任务全部完成（验收命令为空，已跳过校验）');
  912:       return;
  913:     }
  914:     let validation = await this.runShell(workspace, command, `validate-${plan.id}`, { projectId: plan.project_id });
  915:     for (let attempt = 1; validation.exitCode !== 0 && attempt <= 2; attempt += 1) {
  916:       this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', [
  917:         'validation_failed',
  918:         nowIso(),
  919:         plan.id,
  920:       ]);
  921:       const prompt = [
  922:         'plan 已完成，但宿主项目验收失败。请只修复验证错误。',
  923:         `plan 文件（只读）：${planFile}`,
  924:         '不要修改 plan 文件、checkbox 或进度区。',
  925:         `失败命令：${command}`,
  926:         '失败输出摘要：',
  927:         tailText(validation.output, 12000),
  928:       ].join('\n');
  929:       await this.runCodexWithPlanGuard(workspace, prompt, `repair-${plan.id}-${attempt}`, {
  930:         projectId: plan.project_id,
  931:         planId: plan.id,
  932:       }, planFile);
  933:       validation = await this.runShell(workspace, command, `validate-${plan.id}-repair-${attempt}`, {
  934:         projectId: plan.project_id,
  935:       });
  936:     }
  937: 
  938:     if (validation.exitCode === 0) {
  939:       this.db.run(
  940:         'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
  941:         ['completed', nowIso(), plan.id],
  942:       );
  943:       this.addEvent(plan.project_id, 'plan.completed', plan.file_path);
  944:     } else {
  945:       this.addEvent(plan.project_id, 'validation.failed', validation.logFile);
  946:     }
  947:   }
  948: 
  949:   syncPlanTasks(planId, planFile) {
  950:     if (!fs.existsSync(planFile)) return;
  951:     normalizePlanTaskScopes(planFile);
  952:     const text = fs.readFileSync(planFile, 'utf8');
  953:     const regex = /^\s*[-*]\s+\[([ xX])\]\s+(.+)$/gm;
  954:     const tasks = [];
  955:     let match;
  956:     let index = 0;
  957:     while ((match = regex.exec(text))) {
  958:       index += 1;
  959:       const rawTitle = match[2].trim();
  960:       const titleWithoutScope = stripTaskScopeComment(rawTitle);
  961:       const idMatch = titleWithoutScope.match(/^([A-Za-z]+[-_]?\d+|P\d+)[:：\s-]+(.+)$/);
  962:       tasks.push({
  963:         key: idMatch?.[1] || `P${String(index).padStart(3, '0')}`,
  964:         title: idMatch?.[2]?.trim() || titleWithoutScope || rawTitle,
  965:         rawLine: ensureTaskScopeComment(match[0]),
  966:         scope: taskScopeText({ raw_line: match[0], title: rawTitle }),
  967:         status: match[1].toLowerCase() === 'x' ? 'completed' : 'pending',
  968:         sortOrder: index,
  969:       });
  970:     }
  971: 
  972:     this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
  973:     for (const task of tasks) {
  974:       this.db.run(
  975:         `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
  976:          VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
  977:         [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
  978:       );
  979:     }
  980: 
  981:     const completed = tasks.filter((task) => task.status === 'completed').length;
  982:     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
  983:     this.db.run(
  984:       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
  985:       [hashFile(planFile), status, tasks.length, completed, nowIso(), planId],
  986:     );
  987:   }
  988: 
  989:   /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
  990:   archiveOperation(projectId, operationKey) {
  991:     const runtime = this.runtime(projectId);
  992:     if (!runtime) return;
  993:     const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOperation;
  994:     if (op) {
  995:       if (op.activity && typeof op.activity.flush === 'function') {
  996:         op.activity.flush();
  997:       }
  998:       runtime.lastOperation = {
  999:         label: op.label || '',
 1000:         projectId: op.projectId || null,
 1001:         planId: op.planId || null,
 1002:         taskId: op.taskId || null,
 1003:         startedAt: op.startedAt || null,
 1004:         finishedAt: nowIso(),
 1005:         exitCode: typeof op.exitCode === 'number' ? op.exitCode : null,
 1006:         logTail: (op.logBuffer || '').slice(-8000),
 1007:         activity: op.activity ? op.activity.getLines() : [],
 1008:       };
 1009:     }
 1010:     if (operationKey) {
 1011:       runtime.activeChildren.delete(operationKey);
 1012:       runtime.activeOperations.delete(operationKey);
 1013:     } else {
 1014:       runtime.activeChildren.clear();
 1015:       runtime.activeOperations.clear();
 1016:     }
 1017:     refreshRuntimeActive(runtime);
 1018:   }
 1019: 
 1020:   async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
 1021:     const before = planFile && fs.existsSync(planFile) ? fs.readFileSync(planFile, 'utf8') : null;
 1022:     const result = await this.runCodex(workspace, prompt, label, operation);
 1023:     if (before !== null) {
 1024:       const changed = !fs.existsSync(planFile) || fs.readFileSync(planFile, 'utf8') !== before;
 1025:       if (changed) {
 1026:         fs.writeFileSync(planFile, before, 'utf8');
 1027:         this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
 1028:           planId: operation.planId || null,
 1029:           taskId: operation.taskId || null,
 1030:         });
 1031:       }
 1032:     }
 1033:     return result;
 1034:   }
 1035: 
 1036:   async runCodex(workspace, prompt, label, operation = {}) {
 1037:     const projectIdForEmit = operation.projectId;
 1038:     const runtime = this.runtime(projectIdForEmit);
 1039:     if (!runtime) throw new Error('projectId is required for codex operations');
 1040:     const logDir = path.join(workspace, 'docs', 'progress', 'logs');
 1041:     fs.mkdirSync(logDir, { recursive: true });
 1042:     const prefix = `${timestampForPath()}_${safePart(label)}`;
 1043:     const logFile = path.join(logDir, `${prefix}.log`);
 1044:     const lastFile = path.join(logDir, `${prefix}.last.txt`);
 1045:     const args = [

**Modifying task synchronization**

I need to focus on modifying the `syncPlanTasks` function. First, do I need to know the database schema columns? It seems like task P001 isn't completed, possibly due to the absence of a session ID. For P002, I should ensure existing fields are preserved if they're already present. 

My scope is only within `src/loopService.js`, but I wonder if I can inspect the database. I should try to infer the `plan_tasks` columns from queries rather than reading the database directly. I think I need to target specific field names, like `started_at`, `finished_at`, and `duration`.
codex
我需要确认同文件里 `plan_tasks` 的字段用法，确保只保留已有状态字段。
exec
powershell.exe -Command "Select-String -LiteralPath 'src\\loopService.js' -Pattern 'plan_tasks|started_at|finished_at|duration|elapsed|codex|session' | ForEach-Object { '{0,5}: {1}' -f "'$_.LineNumber,$_.Line.Trim() }' in D:\project\GitHub\autoplan
 succeeded in 204ms:
    7: const { CodexActivityPrinter } = require('./codexActivity');
  144: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE status = ?', ['pending', now, 'running']);
  265: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [
  323: // 一次只生成一个计划（保持串行，避免 codex 并发），剩余等下一轮 timer
  373: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), taskId]);
  398: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), taskId]);
  408: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = ?', [
  429: `SELECT plan_tasks.*
  430: FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
  431: WHERE plan_tasks.id = ? AND plans.project_id = ?`,
  436: /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
  450: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked', nowIso(), activeTaskId]);
  466: `UPDATE plan_tasks SET status = ?, updated_at = ?
  478: `UPDATE plan_tasks SET status = ?, updated_at = ?
  502: const existing = this.db.all('SELECT task_key FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order', [planId]);
  526: const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [planId, taskKey]);
  642: const result = await this.runCodex(workspace, prompt, 'generate-plan', { projectId });
  671: /** 为单条需求/反馈生成计划（调 codex），并回写 linked_plan_id。失败返回 null，下轮重试。 */
  700: const result = await this.runCodex(workspace, prompt, `gen-${intake.__type}-${intake.id}`, { projectId });
  724: `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
  801: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), task.id]);
  827: const result = await this.runCodexWithPlanGuard(workspace, prompt, `execute-${task.task_key}`, {
  834: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), task.id]);
  856: this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['completed', nowIso(), task.id]);
  865: FROM plan_tasks
  929: await this.runCodexWithPlanGuard(workspace, prompt, `repair-${plan.id}-${attempt}`, {
  972: this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
  975: `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
 1020: async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
 1022: const result = await this.runCodex(workspace, prompt, label, operation);
 1027: this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
 1036: async runCodex(workspace, prompt, label, operation = {}) {
 1039: if (!runtime) throw new Error('projectId is required for codex operations');
 1057: const child = spawn('codex', args, { shell: process.platform === 'win32', cwd: workspace });
 1062: activity: new CodexActivityPrinter(200),
 1117: activity: new CodexActivityPrinter(200),
 1229: `SELECT plan_tasks.*, plans.file_path
 1230: FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
 1232: ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
 1280: startedAt: task?.started_at,
 1281: finishedAt: task?.finished_at,

codex
同步逻辑会影响执行状态，我再看同文件的执行完成回写顺序。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; "'$start=710; $end=875; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }' in D:\project\GitHub\autoplan
 succeeded in 187ms:
  710:        VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
  711:       [projectId, issueHash, normalizeRelative(workspace, planFile), hashFile(planFile), 'pending', nowIso(), nowIso()],
  712:     );
  713:     this.syncPlanTasks(id, planFile);
  714:     // 回写关联
  715:     this.db.run(`UPDATE ${table} SET linked_plan_id = ?, updated_at = ? WHERE id = ?`, [id, nowIso(), intake.id]);
  716:     this.addEvent(projectId, 'plan.generated', `${sourceName} #${intake.id} 已生成计划：${normalizeRelative(workspace, planFile)}`);
  717:     return id;
  718:   }
  719: 
  720:   async processPlan(workspace, plan) {
  721:     const planFile = path.join(workspace, plan.file_path);
  722:     this.syncPlanTasks(plan.id, planFile);
  723:     const pendingTasks = this.db.all(
  724:       `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
  725:        ORDER BY sort_order ASC`,
  726:       [plan.id],
  727:     );
  728:     if (pendingTasks.length) {
  729:       const batch = this.parallelTaskBatch(pendingTasks);
  730:       if (batch.length > 1) {
  731:         await this.executeTaskBatch(workspace, plan, batch);
  732:       } else {
  733:         const task = batch[0] || pendingTasks[0];
  734:         const result = await this.executeTask(workspace, plan, task);
  735:         if (result.exitCode === 0) {
  736:           this.completeTask(workspace, plan, task, result);
  737:         }
  738:       }
  739:       return;
  740:     }
  741:     await this.validatePlan(workspace, plan);
  742:   }
  743: 
  744:   parallelTaskBatch(tasks) {
  745:     const selected = [];
  746:     const usedScopes = new Set();
  747:     for (const task of tasks) {
  748:       if (selected.length >= MAX_PARALLEL_TASKS) break;
  749:       const scopes = taskParallelScopes(task);
  750:       if (!scopes.length) {
  751:         if (!selected.length) return [task];
  752:         continue;
  753:       }
  754:       if (scopes.some((scope) => usedScopes.has(scope))) continue;
  755:       selected.push(task);
  756:       for (const scope of scopes) usedScopes.add(scope);
  757:     }
  758:     return selected.length > 1 ? selected : [tasks[0]];
  759:   }
  760: 
  761:   async executeTaskBatch(workspace, plan, tasks) {
  762:     this.setPhase(plan.project_id, 'execute-task');
  763:     this.addEvent(
  764:       plan.project_id,
  765:       'tasks.parallel.started',
  766:       `并发执行 ${tasks.map((task) => task.task_key).join(', ')}`,
  767:       { taskIds: tasks.map((task) => task.id) },
  768:     );
  769:     const results = await Promise.all(
  770:       tasks.map(async (task) => {
  771:         try {
  772:           return {
  773:             task,
  774:             result: await this.executeTask(workspace, plan, task, { parallel: true }),
  775:           };
  776:         } catch (error) {
  777:           this.addEvent(
  778:             plan.project_id,
  779:             TASK_EVENT_TYPES.FAILED,
  780:             task.title,
  781:             taskEventMeta(task, {
  782:               planId: plan.id,
  783:               status: TASK_EVENT_STATUS.FAILED,
  784:               finishedAt: nowIso(),
  785:               error: error?.message || String(error),
  786:             }),
  787:           );
  788:           return { task, result: { exitCode: -1 } };
  789:         }
  790:       }),
  791:     );
  792:     for (const { task, result } of results) {
  793:       if (result.exitCode === 0) {
  794:         this.completeTask(workspace, plan, task, result);
  795:       }
  796:     }
  797:   }
  798: 
  799:   async executeTask(workspace, plan, task, options = {}) {
  800:     this.setPhase(plan.project_id, 'execute-task');
  801:     this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), task.id]);
  802:     this.emitUpdate(plan.project_id);
  803:     const planFile = path.join(workspace, plan.file_path);
  804:     const completionRules = [
  805:       '- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区',
  806:       '- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区',
  807:       '- 只修改当前任务 scope 直接相关的业务文件',
  808:       '- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试',
  809:       '- 只有当前任务本身明确要求全量验证时，才允许运行全量测试',
  810:       '- 不输出完整 diff、源码全文或长文件列表',
  811:       '- 中文文件读写使用 UTF-8',
  812:     ];
  813:     if (options.parallel) {
  814:       completionRules.unshift('- 当前为并发执行模式，不要读写其它任务的 scope');
  815:     }
  816:     const prompt = [
  817:       '你是开发执行者。',
  818:       `请只执行指定任务 ${task.task_key}，不要提前执行其它任务，也不要顺手处理其它 checkbox。`,
  819:       '',
  820:       `plan 文件（只读）：${planFile}`,
  821:       `指定任务：${task.raw_line}`,
  822:       `任务 scope：${task.scope || taskScopeText(task) || 'unknown'}`,
  823:       '',
  824:       '完成后必须：',
  825:       ...completionRules,
  826:     ].join('\n');
  827:     const result = await this.runCodexWithPlanGuard(workspace, prompt, `execute-${task.task_key}`, {
  828:       projectId: plan.project_id,
  829:       planId: plan.id,
  830:       taskId: task.id,
  831:       parallel: Boolean(options.parallel),
  832:     }, planFile);
  833:     if (result.exitCode !== 0) {
  834:       this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), task.id]);
  835:       this.emitUpdate(plan.project_id);
  836:     }
  837:     const succeeded = result.exitCode === 0;
  838:     this.addEvent(
  839:       plan.project_id,
  840:       succeeded ? LEGACY_TASK_EVENT_TYPES.EXECUTED : TASK_EVENT_TYPES.FAILED,
  841:       task.title,
  842:       taskEventMeta(task, {
  843:         planId: plan.id,
  844:         status: succeeded ? TASK_EVENT_STATUS.COMPLETED : TASK_EVENT_STATUS.FAILED,
  845:         finishedAt: nowIso(),
  846:         exitCode: result.exitCode,
  847:         log: result.logFile,
  848:       }),
  849:     );
  850:     return result;
  851:   }
  852: 
  853:   completeTask(workspace, plan, task, result) {
  854:     const planFile = path.join(workspace, plan.file_path);
  855:     this.markTaskCompletedInPlan(workspace, planFile, task, result);
  856:     this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['completed', nowIso(), task.id]);
  857:     this.refreshPlanProgress(plan.id, planFile);
  858:     this.emitUpdate(plan.project_id);
  859:   }
  860: 
  861:   refreshPlanProgress(planId, planFile) {
  862:     const totals = this.db.get(
  863:       `SELECT COUNT(*) AS total,
  864:               SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) AS completed
  865:        FROM plan_tasks
  866:        WHERE plan_id = ?`,
  867:       [planId],
  868:     ) || { total: 0, completed: 0 };
  869:     const total = Number(totals.total || 0);
  870:     const completed = Number(totals.completed || 0);
  871:     const status = total > 0 && completed === total ? 'ready_for_validation' : 'running';
  872:     const hash = fs.existsSync(planFile) ? hashFile(planFile) : '';
  873:     this.db.run(
  874:       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
  875:       [hash, status, total, completed, nowIso(), planId],

codex
还需要确认完成后是否重新同步，从而决定状态覆盖策略。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; "'$start=875; $end=900; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }' in D:\project\GitHub\autoplan
 succeeded in 199ms:
  875:       [hash, status, total, completed, nowIso(), planId],
  876:     );
  877:   }
  878: 
  879:   markTaskCompletedInPlan(workspace, planFile, task, result) {
  880:     if (!fs.existsSync(planFile)) return;
  881:     const relativeLog = result?.logFile ? normalizeRelative(workspace, result.logFile) : '';
  882:     const key = escapeRegExp(String(task.task_key || ''));
  883:     const checkboxRe = new RegExp(`(^\\s*[-*]\\s+\\[)([ xX])(\\]\\s+${key}(?:\\b|[:：\\s-]))`, 'm');
  884:     let content = fs.readFileSync(planFile, 'utf8');
  885:     if (checkboxRe.test(content)) {
  886:       content = content.replace(checkboxRe, '$1x$3');
  887:     }
  888: 
  889:     const note = `- ${task.task_key} AutoPlan 完成：${nowIso()}${relativeLog ? `；日志：${relativeLog}` : ''}`;
  890:     const noteRe = new RegExp(`^\\s*-\\s+${key}\\s+AutoPlan 完成：.*$`, 'm');
  891:     if (noteRe.test(content)) {
  892:       content = content.replace(noteRe, note);
  893:     } else if (/##\s*进度区/.test(content)) {
  894:       content = `${content.trimEnd()}\n${note}\n`;
  895:     } else {
  896:       content = `${content.trimEnd()}\n\n## 进度区\n${note}\n`;
  897:     }
  898:     fs.writeFileSync(planFile, content, 'utf8');
  899:   }
  900: 

codex
我会再看同文件的中断/停止处理，避免同步后破坏恢复行为。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; "'$start=240; $end=490; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }' in D:\project\GitHub\autoplan
 succeeded in 213ms:
  240:       ['running', nowIso(), projectId],
  241:     );
  242:     this.emitUpdate(projectId);
  243:     this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
  244:     this.scheduleProject(projectId, state.interval_seconds);
  245:   }
  246: 
  247:   stop(projectId = null) {
  248:     if (!projectId) {
  249:       for (const id of Array.from(this.runtimes.keys())) this.stop(id);
  250:       return;
  251:     }
  252:     const runtime = this.runtime(projectId);
  253:     if (runtime?.timer) clearInterval(runtime.timer);
  254:     if (runtime) {
  255:       runtime.timer = null;
  256:       runtime.running = false;
  257:     }
  258:     if (runtime?.activeOperations?.size) {
  259:       for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {
  260:         const child = runtime.activeChildren.get(operationKey);
  261:         const activeTaskId = operation?.taskId || null;
  262:         killChildProcess(child);
  263:         const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
  264:         if (activeTaskId) {
  265:           this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [
  266:             'pending',
  267:             nowIso(),
  268:             activeTaskId,
  269:           ]);
  270:         }
  271:         this.addEvent(
  272:           projectId,
  273:           activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
  274:           operation?.label || '',
  275:           activeTask
  276:             ? taskEventMeta(activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIso() })
  277:             : null,
  278:         );
  279:       }
  280:     }
  281:     this.db.run(
  282:       'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',
  283:       ['stopped', nowIso(), projectId],
  284:     );
  285:     this.addEvent(projectId, 'loop.stopped', '循环已停止');
  286:     this.emitUpdate(projectId);
  287:   }
  288: 
  289:   async runOnce(projectId = this.defaultProjectId()) {
  290:     if (!projectId) return;
  291:     const runtime = this.runtime(projectId);
  292:     if (!runtime || runtime.busy) return;
  293:     runtime.busy = true;
  294:     try {
  295:       const project = this.project(projectId);
  296:       const workspace = project?.workspace_path;
  297:       if (!workspace) return;
  298:       this.setPhase(projectId, 'scan');
  299:       this.ensureWorkspaceDirs(workspace);
  300: 
  301:       // 扫描未完成、未中断、尚未生成计划的需求和反馈
  302:       const pendingRequirements = this.db
  303:         .all(
  304:           `SELECT * FROM requirements
  305:            WHERE project_id = ? AND linked_plan_id IS NULL
  306:              AND status NOT IN ('completed', 'closed')
  307:            ORDER BY created_at ASC`,
  308:           [projectId],
  309:         )
  310:         .map((row) => ({ ...row, __type: 'requirement' }));
  311:       const pendingFeedback = this.db
  312:         .all(
  313:           `SELECT * FROM feedback
  314:            WHERE project_id = ? AND linked_plan_id IS NULL
  315:              AND status NOT IN ('completed', 'closed')
  316:            ORDER BY created_at ASC`,
  317:           [projectId],
  318:         )
  319:         .map((row) => ({ ...row, __type: 'feedback' }));
  320:       const pendingIntakes = [...pendingRequirements, ...pendingFeedback];
  321:       this.addEvent(projectId, 'scan.done', `待处理需求/反馈=${pendingIntakes.length}`);
  322: 
  323:       // 一次只生成一个计划（保持串行，避免 codex 并发），剩余等下一轮 timer
  324:       let generatedPlanId = null;
  325:       if (pendingIntakes.length > 0) {
  326:         generatedPlanId = await this.generatePlanForIntake(projectId, workspace, pendingIntakes[0]);
  327:       }
  328: 
  329:       // 同步 docs/plan 目录下的 plan 文件（兼容文件式需求）
  330:       const planScan = this.scanDirectory(path.join(workspace, 'docs', 'plan'), workspace, ['.md']);
  331:       this.saveScan(projectId, 'plan', planScan);
  332: 
  333:       // 执行队列里可运行的 plan
  334:       const nextPlan = generatedPlanId
  335:         ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [generatedPlanId, projectId])
  336:         : this.nextRunnablePlan(projectId);
  337:       if (!nextPlan) {
  338:         if (runtime.running || this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
  339:           this.setPhase(projectId, pendingIntakes.length > 0 && runtime.running ? 'waiting' : 'idle');
  340:         }
  341:         return;
  342:       }
  343: 
  344:       await this.processPlan(workspace, nextPlan);
  345:       if (runtime.running) {
  346:         this.setPhase(projectId, 'waiting');
  347:       } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
  348:         this.setPhase(projectId, 'idle');
  349:       }
  350:     } finally {
  351:       runtime.busy = false;
  352:       this.emitUpdate(projectId);
  353:     }
  354:   }
  355: 
  356:   async runTask(projectId, taskId) {
  357:     const runtime = this.runtime(projectId);
  358:     if (!runtime) return;
  359:     if (runtime.busy) throw new Error('该项目已有任务正在执行，请稍后再试');
  360:     const project = this.project(projectId);
  361:     const task = this.taskForProject(projectId, taskId);
  362:     const plan = task ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [task.plan_id, projectId]) : null;
  363:     const workspace = project?.workspace_path;
  364:     if (!project || !task || !plan) throw new Error('任务不存在');
  365:     if (!workspace) throw new Error('请先设置项目工作区路径');
  366:     const workspaceOwner = this.activeProjectForWorkspace(workspace, projectId);
  367:     if (workspaceOwner) {
  368:       throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
  369:     }
  370: 
  371:     runtime.busy = true;
  372:     try {
  373:       this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), taskId]);
  374:       this.emitUpdate(projectId);
  375:       const result = await this.executeTask(workspace, plan, task);
  376:       if (result.exitCode === 0) {
  377:         this.completeTask(workspace, plan, task, result);
  378:       }
  379:       if (runtime.running) {
  380:         this.setPhase(projectId, 'waiting');
  381:       } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
  382:         this.setPhase(projectId, 'idle');
  383:       }
  384:     } finally {
  385:       runtime.busy = false;
  386:       this.emitUpdate(projectId);
  387:     }
  388:   }
  389: 
  390:   stopTask(projectId, taskId) {
  391:     const task = this.taskForProject(projectId, taskId);
  392:     if (!task) throw new Error('任务不存在');
  393:     const runtime = this.runtime(projectId);
  394: 
  395:     const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));
  396:     if (activeEntry) {
  397:       killChildProcess(activeEntry.child);
  398:       this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), taskId]);
  399:       if (!runtime.running) {
  400:         this.addEvent(
  401:           projectId,
  402:           LEGACY_TASK_EVENT_TYPES.STOPPING,
  403:           task.title,
  404:           taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPED, finishedAt: nowIso() }),
  405:         );
  406:       }
  407:     } else {
  408:       this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = ?', [
  409:         'pending',
  410:         nowIso(),
  411:         taskId,
  412:         'running',
  413:       ]);
  414:       this.addEvent(
  415:         projectId,
  416:         TASK_EVENT_TYPES.STOP_REQUESTED,
  417:         task.title,
  418:         taskEventMeta(task, { status: TASK_EVENT_STATUS.STOPPING }),
  419:       );
  420:     }
  421: 
  422:     if (runtime?.running) this.stop(projectId);
  423:     else this.setPhase(projectId, 'stopped');
  424:   }
  425: 
  426:   taskForProject(projectId, taskId) {
  427:     if (!taskId) return null;
  428:     return this.db.get(
  429:       `SELECT plan_tasks.*
  430:        FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
  431:        WHERE plan_tasks.id = ? AND plans.project_id = ?`,
  432:       [taskId, projectId],
  433:     );
  434:   }
  435: 
  436:   /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
  437:   interruptPlan(projectId, planId) {
  438:     const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
  439:     if (!plan) return;
  440:     // 若当前正在执行该 plan 的任务，kill 进程
  441:     const runtime = this.runtime(projectId);
  442:     const activePlanOperations = runtime
  443:       ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))
  444:       : [];
  445:     for (const activeEntry of activePlanOperations) {
  446:       killChildProcess(activeEntry.child);
  447:       const activeTaskId = activeEntry.operation?.taskId;
  448:       const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
  449:       if (activeTaskId) {
  450:         this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked', nowIso(), activeTaskId]);
  451:       }
  452:       this.addEvent(
  453:         projectId,
  454:         LEGACY_TASK_EVENT_TYPES.STOPPING,
  455:         `plan #${planId} 的任务被中断`,
  456:         taskEventMeta(activeTask, {
  457:           planId,
  458:           taskId: activeTaskId || undefined,
  459:           status: TASK_EVENT_STATUS.INTERRUPTED,
  460:           finishedAt: nowIso(),
  461:         }),
  462:       );
  463:     }
  464:     // 其余未完成任务 → blocked
  465:     this.db.run(
  466:       `UPDATE plan_tasks SET status = ?, updated_at = ?
  467:        WHERE plan_id = ? AND status IN ('pending', 'running')`,
  468:       ['blocked', nowIso(), planId],
  469:     );
  470:     this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['interrupted', nowIso(), planId]);
  471:     this.addEvent(projectId, 'plan.interrupted', `plan #${planId} 已中断，未完成任务已挂起`);
  472:     this.emitUpdate(projectId);
  473:   }
  474: 
  475:   /** 恢复被中断的 plan：blocked → pending，plan → pending，循环运行时自动继续执行 */
  476:   resumePlan(projectId, planId) {
  477:     this.db.run(
  478:       `UPDATE plan_tasks SET status = ?, updated_at = ?
  479:        WHERE plan_id = ? AND status = ?`,
  480:       ['pending', nowIso(), planId, 'blocked'],
  481:     );
  482:     this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
  483:     this.addEvent(projectId, 'plan.resumed', `plan #${planId} 已恢复`);
  484:     this.emitUpdate(projectId);
  485:   }
  486: 
  487:   /** 向现有 plan 追加一个任务：写回 plan 文件 + syncPlanTasks 重新解析 */
  488:   appendTask(projectId, planId, title) {
  489:     const project = this.project(projectId);
  490:     const workspace = project?.workspace_path;

**Implementing task status merging**

I’m working on a function to merge task statuses. If there's no existing status, I'll just return the parsed status. If the parsed status is 'completed', I’ll return that. And if the existing status is 'completed,' I should maintain that too, preserving the completed state. Hmm, if the existing status is 'running,' I'm considering returning 'running' if the process is still syncing during that state. I just need to get this logic right!
**Evaluating task status**

I'm thinking about how to handle task status updates. If I’m parsing completed overrides, it could be okay, but "critical status" includes blocked tasks, so it might be best to preserve that status unless there are pending tasks. I'm considering if it’s correct to treat the same task as one entity: sync should update the status from the checkbox, but maybe I should keep certain critical status info intact even if a task is running or blocked.
codex
准备把全量删除重建改为按 `task_key` 更新，避免丢失同任务的上下文字段。
apply patch
patch: completed
D:\project\GitHub\autoplan\src\loopService.js
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..77c597468ce213ee338d711edff484e8da61205b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..77c597468ce213ee338d711edff484e8da61205b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';

**Confirming function definition**

I see a potential issue regarding `normalizeOptionalString`. It seems like it might be defined later in the code. I should check whether function declarations are hoisted in this context. It’s important for me to confirm that the function actually exists before I try to use it. So, I’ll need to look into it and see if I can find its definition or ensure it's accessible at the time it's being called.
codex
已改为按 key 同步；还需补上状态合并函数以保持语义完整。
exec
powershell.exe -Command "Select-String -LiteralPath 'src\\loopService.js' -Pattern '"'^function normalizeOptionalString|''^function taskEventMeta'"' | ForEach-Object { '{0,5}: {1}' -f "'$_.LineNumber,$_.Line.Trim() }' in D:\project\GitHub\autoplan
 succeeded in 217ms:
 1303: function taskEventMeta(task, overrides = {}) {
 1341: function normalizeOptionalString(value) {

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..77c597468ce213ee338d711edff484e8da61205b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';

codex
我会把状态合并函数放在同文件辅助函数区，不触碰其它文件。
apply patch
patch: completed
D:\project\GitHub\autoplan\src\loopService.js
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..571b3bc2587580b4e199811d11bb3488e7c3ea96
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
@@ -1269,7 +1300,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1322,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..571b3bc2587580b4e199811d11bb3488e7c3ea96
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
@@ -1269,7 +1300,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1322,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

**Inspecting function placement**

I need to check the actual placement of a function, possibly inserted after something called taskEventMeta. I'm considering if the context from `function taskEventMeta(task...` to `}` might match the wrong first `}` inside the code? Maybe the `apply_patch` with @@ could insert after the first `}` I find. I really need to inspect around lines 1270-1360 and also make sure to look into sync as well. So, let's get into it!
codex
补丁已落地；现在只检查修改片段，确认没有误插或语法问题。
exec
powershell.exe -Command '$path='"'src\\loopService.js'; foreach("'$range in @(@(949,1015),@(1295,1365))){ $start=$range[0]; $end=$range[1]; $i=0; Get-Content -LiteralPath $path -Encoding UTF8 | ForEach-Object { $i++; if($i -ge $start -and $i -le $end){ '"'{0,5}: {1}' -f "'$i,$_ } }; Write-Output '"'---' }" in D:\project\GitHub\autoplan
 succeeded in 228ms:
  949:   syncPlanTasks(planId, planFile) {
  950:     if (!fs.existsSync(planFile)) return;
  951:     normalizePlanTaskScopes(planFile);
  952:     const text = fs.readFileSync(planFile, 'utf8');
  953:     const regex = /^\s*[-*]\s+\[([ xX])\]\s+(.+)$/gm;
  954:     const tasks = [];
  955:     let match;
  956:     let index = 0;
  957:     while ((match = regex.exec(text))) {
  958:       index += 1;
  959:       const rawTitle = match[2].trim();
  960:       const titleWithoutScope = stripTaskScopeComment(rawTitle);
  961:       const idMatch = titleWithoutScope.match(/^([A-Za-z]+[-_]?\d+|P\d+)[:：\s-]+(.+)$/);
  962:       tasks.push({
  963:         key: idMatch?.[1] || `P${String(index).padStart(3, '0')}`,
  964:         title: idMatch?.[2]?.trim() || titleWithoutScope || rawTitle,
  965:         rawLine: ensureTaskScopeComment(match[0]),
  966:         scope: taskScopeText({ raw_line: match[0], title: rawTitle }),
  967:         status: match[1].toLowerCase() === 'x' ? 'completed' : 'pending',
  968:         sortOrder: index,
  969:       });
  970:     }
  971: 
  972:     const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
  973:     const existingByKey = new Map();
  974:     for (const existing of existingTasks) {
  975:       const matches = existingByKey.get(existing.task_key) || [];
  976:       matches.push(existing);
  977:       existingByKey.set(existing.task_key, matches);
  978:     }
  979: 
  980:     for (const task of tasks) {
  981:       const existing = existingByKey.get(task.key)?.shift();
  982:       if (existing) {
  983:         this.db.run(
  984:           `UPDATE plan_tasks
  985:            SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
  986:            WHERE id = ?`,
  987:           [
  988:             task.title,
  989:             task.rawLine,
  990:             task.scope,
  991:             syncedTaskStatus(task.status, existing.status),
  992:             task.sortOrder,
  993:             nowIso(),
  994:             existing.id,
  995:           ],
  996:         );
  997:       } else {
  998:         this.db.run(
  999:           `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
 1000:            VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
 1001:           [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
 1002:         );
 1003:       }
 1004:     }
 1005: 
 1006:     for (const matches of existingByKey.values()) {
 1007:       for (const stale of matches) {
 1008:         this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
 1009:       }
 1010:     }
 1011: 
 1012:     const completed = tasks.filter((task) => task.status === 'completed').length;
 1013:     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
 1014:     this.db.run(
 1015:       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
---
 1295:       lastOperation:
 1296:         runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
 1297:           ? runtime.lastOperation
 1298:           : null,
 1299:     };
 1300:   }
 1301: }
 1302: 
 1303: function taskEventMeta(task, overrides = {}) {
 1304:   const meta = {
 1305:     ...compactEventMeta({
 1306:       taskId: task?.id,
 1307:       taskKey: task?.task_key,
 1308:       taskTitle: task?.title,
 1309:       planId: task?.plan_id,
 1310:       status: task?.status,
 1311:       startedAt: task?.started_at,
 1312:       finishedAt: task?.finished_at,
 1313:     }),
 1314:     ...compactEventMeta(overrides),
 1315:   };
 1316:   meta.taskId = normalizeOptionalNumber(meta.taskId);
 1317:   meta.planId = normalizeOptionalNumber(meta.planId);
 1318:   meta.taskKey = normalizeOptionalString(meta.taskKey);
 1319:   meta.taskTitle = normalizeOptionalString(meta.taskTitle);
 1320:   meta.status = normalizeOptionalString(meta.status);
 1321:   meta.startedAt = normalizeOptionalString(meta.startedAt);
 1322:   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
 1323:   const compacted = compactEventMeta(meta);
 1324:   return Object.keys(compacted).length ? compacted : null;
 1325: }
 1326: 
 1327: function syncedTaskStatus(parsedStatus, existingStatus) {
 1328:   const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
 1329:   const current = normalizeOptionalString(existingStatus);
 1330:   if (!current) return next;
 1331:   if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
 1332:   if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
 1333:   return next;
 1334: }
 1335: 
 1336: function compactEventMeta(meta) {
 1337:   const result = {};
 1338:   for (const [key, value] of Object.entries(meta || {})) {
 1339:     if (value !== undefined && value !== null && value !== '') result[key] = value;
 1340:   }
 1341:   return result;
 1342: }
 1343: 
 1344: function normalizeOptionalNumber(value) {
 1345:   if (value === undefined || value === null || value === '') return undefined;
 1346:   const number = Number(value);
 1347:   return Number.isFinite(number) ? number : undefined;
 1348: }
 1349: 
 1350: function normalizeOptionalString(value) {
 1351:   if (value === undefined || value === null) return undefined;
 1352:   const text = String(value).trim();
 1353:   return text || undefined;
 1354: }
 1355: 
 1356: function createProjectRuntime() {
 1357:   return {
 1358:     timer: null,
 1359:     running: false,
 1360:     busy: false,
 1361:     activeChild: null,
 1362:     activeOperation: null,
 1363:     activeChildren: new Map(),
 1364:     activeOperations: new Map(),
 1365:     lastOperation: null,
---

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..571b3bc2587580b4e199811d11bb3488e7c3ea96
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,14 +969,45 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            syncedTaskStatus(task.status, existing.status),
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
     const completed = tasks.filter((task) => task.status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
@@ -1269,7 +1300,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1322,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

**Updating task status logic**

I need to modify the loop to compute `const status = existing ? syncedTaskStatus(...) : task.status;`, which will be used for update or insert actions. I plan to set `status = ready_for_validation` when all final statuses are complete. If there are blocked statuses, ‘running’ can indicate the current state. I'll add a `syncedTasks` array and ensure tasks are processed correctly. The line length may need adjustment, but it’s not essential. Let's patch this up!
codex
发现计划完成数应按合并后的状态计算，我会同步修正这个细节。
apply patch
patch: completed
D:\project\GitHub\autoplan\src\loopService.js
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

**Validating syntax**

I need to validate the syntax for my code, specifically using the command: `node --check src/loopService.js`. But before I do that, I should confirm whether Node is installed on my system. Also, I should check to see if the update_plan status is necessary. It feels important to have those steps properly set up, like ensuring step 2 is completed and step 3 is in progress before moving ahead with the validation.
codex
同步逻辑已完成，现在运行单文件语法检查作为最小验证。
  ✓ 读取 P002 上下文
  ✓ 修改任务同步逻辑
  → 做最小语法验证
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

exec
powershell.exe -Command "node --check 'src\\loopService.js'" in D:\project\GitHub\autoplan
 succeeded in 176ms:
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

codex
语法检查通过；再做一次只针对该文件的 whitespace diff 检查。
exec
powershell.exe -Command "git diff --check -- 'src/loopService.js'" in D:\project\GitHub\autoplan
 succeeded in 165ms:
warning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

**Evaluating Git warnings**

I'm seeing a warning about LF to CRLF line endings, even though the diff check exited successfully. It seems like it might be related to the working copy of the file. I think I need to mention the Git line ending warning, but there's no whitespace error. Maybe I should inspect the output with `git diff --stat`, but I’m not sure if that's necessary. Looks like I’ll need to clarify my update plan to complete this.
codex
验证完成，没有 whitespace 错误；仅有 Git 的换行转换提示。
  ✓ 读取 P002 上下文
  ✓ 修改任务同步逻辑
  ✓ 做最小语法验证
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

codex
我再取一下最终行号，方便交付时准确引用。
exec
powershell.exe -Command "Select-String -LiteralPath 'src\\loopService.js' -Pattern 'existingTasks|syncedStatuses|syncedTaskStatus\\(|const completed = syncedStatuses' | ForEach-Object { '{0}:{1}' -f "'$_.LineNumber,$_.Line.Trim() }' in D:\project\GitHub\autoplan
 succeeded in 200ms:
972:const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
974:for (const existing of existingTasks) {
980:const syncedStatuses = [];
983:const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
984:syncedStatuses.push(status);
1015:const completed = syncedStatuses.filter((status) => status === 'completed').length;
1330:function syncedTaskStatus(parsedStatus, existingStatus) {

diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

**Ensuring clarity in instructions**

I need to clarify that there won't be any modifications to the plan and that not all tests are complete. It's essential to ensure the final output has no citations, using file path lines instead. I want to keep it in Chinese, and the verbosity should be around 5 to 10 lines. I should include "已完成 P002" and add a line like "只修改了..." but remember to keep it concise throughout.
codex
已完成 P002，仅修改 `src/loopService.js`，未修改 plan 文件或其它任务 scope。

**变更**
- `src/loopService.js:972` 将 `syncPlanTasks` 从全量删除重建改为按 `plan_id + task_key` 匹配更新，保留同任务已有行的上下文字段。
- `src/loopService.js:983` 更新标题、raw line、scope、排序等解析结果，同时保留/合并关键状态。
- `src/loopService.js:1015` 计划完成数改为按同步后的最终状态统计。
- `src/loopService.js:1330` 新增状态合并逻辑，避免已完成、运行中、blocked 状态被普通同步误重置。

**验证**
- `node --check src\loopService.js` 通过。
- `git diff --check -- src/loopService.js` 通过；仅有 Git 的 LF/CRLF 换行转换提示。
diff --git a/src/loopService.js b/src/loopService.js
index a215c25820244913af2de2c3054865f5f4414e84..2e801f0bb1f6dd3cb9a389c58a3563d46bd79f64
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -969,16 +969,50 @@
       });
     }
 
-    this.db.run('DELETE FROM plan_tasks WHERE plan_id = ?', [planId]);
-    for (const task of tasks) {
-      this.db.run(
-        `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
-        [planId, task.key, task.title, task.rawLine, task.scope, task.status, task.sortOrder, nowIso()],
-      );
-    }
+    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
+    const existingByKey = new Map();
+    for (const existing of existingTasks) {
+      const matches = existingByKey.get(existing.task_key) || [];
+      matches.push(existing);
+      existingByKey.set(existing.task_key, matches);
+    }
+
+    const syncedStatuses = [];
+    for (const task of tasks) {
+      const existing = existingByKey.get(task.key)?.shift();
+      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
+      syncedStatuses.push(status);
+      if (existing) {
+        this.db.run(
+          `UPDATE plan_tasks
+           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
+           WHERE id = ?`,
+          [
+            task.title,
+            task.rawLine,
+            task.scope,
+            status,
+            task.sortOrder,
+            nowIso(),
+            existing.id,
+          ],
+        );
+      } else {
+        this.db.run(
+          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
+           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
+          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
+        );
+      }
+    }
+
+    for (const matches of existingByKey.values()) {
+      for (const stale of matches) {
+        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
+      }
+    }
 
-    const completed = tasks.filter((task) => task.status === 'completed').length;
+    const completed = syncedStatuses.filter((status) => status === 'completed').length;
     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
     this.db.run(
       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
@@ -1269,7 +1303,7 @@
   }
 }
 
-function taskEventMeta(task, overrides = {}) {
+function taskEventMeta(task, overrides = {}) {
   const meta = {
     ...compactEventMeta({
       taskId: task?.id,
@@ -1291,9 +1325,18 @@
   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
   const compacted = compactEventMeta(meta);
   return Object.keys(compacted).length ? compacted : null;
-}
-
-function compactEventMeta(meta) {
+}
+
+function syncedTaskStatus(parsedStatus, existingStatus) {
+  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
+  const current = normalizeOptionalString(existingStatus);
+  if (!current) return next;
+  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
+  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
+  return next;
+}
+
+function compactEventMeta(meta) {
   const result = {};
   for (const [key, value] of Object.entries(meta || {})) {
     if (value !== undefined && value !== null && value !== '') result[key] = value;

tokens used
57,103
已完成 P002，仅修改 `src/loopService.js`，未修改 plan 文件或其它任务 scope。

**变更**
- `src/loopService.js:972` 将 `syncPlanTasks` 从全量删除重建改为按 `plan_id + task_key` 匹配更新，保留同任务已有行的上下文字段。
- `src/loopService.js:983` 更新标题、raw line、scope、排序等解析结果，同时保留/合并关键状态。
- `src/loopService.js:1015` 计划完成数改为按同步后的最终状态统计。
- `src/loopService.js:1330` 新增状态合并逻辑，避免已完成、运行中、blocked 状态被普通同步误重置。

**验证**
- `node --check src\loopService.js` 通过。
- `git diff --check -- src/loopService.js` 通过；仅有 Git 的 LF/CRLF 换行转换提示。

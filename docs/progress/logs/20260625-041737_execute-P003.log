OpenAI Codex v0.121.0 (research preview)
--------
workdir: D:\project\GitHub\autoplan
model: gpt-5.5
provider: custom
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019efb47-f726-7ec0-a6c3-d287a014fd16
--------
user
你是开发执行者。
请只执行指定任务 P003，不要提前执行其它任务，也不要顺手处理其它 checkbox。

plan 文件（只读）：D:\project\GitHub\autoplan\docs\plan\plan_requirement_4_20260625-030248.md
指定任务：
- [ ] P003: 在快照数据中暴露耗时信息 <!-- scope: src/loopService.js,src/renderer/types.ts -->
任务 scope：src/loopservice.js, src/renderer/types.ts

完成后必须：
- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区
- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区
- 只修改当前任务 scope 直接相关的业务文件
- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试
- 只有当前任务本身明确要求全量验证时，才允许运行全量测试
- 不输出完整 diff、源码全文或长文件列表
- 中文文件读写使用 UTF-8
codex
我会先定位快照数据结构和类型定义，只看本任务范围内文件。
mcp: codegraph/codegraph_explore started
mcp: codegraph/codegraph_explore (failed)
codex
CodeGraph 需要显式项目路径；我会用当前仓库路径重试。
mcp: codegraph/codegraph_explore started
mcp: codegraph/codegraph_explore (failed)
codex
CodeGraph 未初始化；改用本地只读检索相关文件和约定。
exec
powershell.exe -Command 'Get-ChildItem -Path . -Filter AGENTS.md -Recurse -Force | ForEach-Object { $_.FullName }' in D:\project\GitHub\autoplan
 succeeded in 352ms:
codex
没有发现额外 AGENTS 指令；现在读取任务范围内文件。
exec
powershell.exe -Command "Get-ChildItem -Path src -Force | Select-Object Name; Write-Host '---'; Get-Content -Path src\\loopService.js -Raw -Encoding UTF8; Write-Host '---TYPES---'; Get-Content -Path src\\renderer\\types.ts -Raw -Encoding UTF8" in D:\project\GitHub\autoplan
 succeeded in 319ms:

---
---TYPES---
Name            
----            
renderer        
attachments.js  
codexActivity.js
database.js     
loopService.js  
main.js         
planDrafts.js   
preload.js      
styles.css      
vite-env.d.ts   
const crypto = require('node:crypto');
const { EventEmitter } = require('node:events');
const fs = require('node:fs');
const path = require('node:path');
const { spawn } = require('node:child_process');
const { nowIso } = require('./database');
const { CodexActivityPrinter } = require('./codexActivity');

const ACTIVE_RUNTIME_PHASES = new Set(['running', 'scan', 'generate-plan', 'execute-task', 'validate']);
const ACTIVE_RUNTIME_PHASE_SQL = "('running','scan','generate-plan','execute-task','validate')";
const MAX_PARALLEL_TASKS = 2;
const PARALLEL_BLOCKING_TASK_RE = /(全量|回归|验证|验收|测试|记录|整理|发布|部署|test|validate|regression|release|deploy)/i;
const TASK_SCOPE_LABEL_RE = '(?:scope|scopes|files?|影响范围|并发键)';
const TASK_SCOPE_RE = new RegExp(`${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>\\]\\n]+)`, 'i');
const TASK_SCOPE_COMMENT_RE = new RegExp(`\\s*<!--\\s*${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>]*?)\\s*-->\\s*`, 'i');
const TASK_SCOPE_SPLIT_RE = /[,，、;；]+/;
const TASK_PATH_RE = /[\w./\\-]+\.(?:dart|js|jsx|ts|tsx|css|scss|html|md|json|ya?ml)/gi;

const TASK_EVENT_TYPES = Object.freeze({
  STARTED: 'task.started',
  SUCCEEDED: 'task.succeeded',
  FAILED: 'task.failed',
  STOP_REQUESTED: 'task.stop.requested',
  STOPPED: 'task.stopped',
  INTERRUPTED: 'task.interrupted',
});

const LEGACY_TASK_EVENT_TYPES = Object.freeze({
  EXECUTED: 'task.executed',
  STOPPING: 'task.stopping',
});

const TASK_EVENT_STATUS = Object.freeze({
  PENDING: 'pending',
  RUNNING: 'running',
  COMPLETED: 'completed',
  FAILED: 'failed',
  STOPPING: 'stopping',
  STOPPED: 'stopped',
  INTERRUPTED: 'interrupted',
});

const TASK_EVENT_SEMANTICS = Object.freeze({
  [TASK_EVENT_TYPES.STARTED]: Object.freeze({ status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务' }),
  [TASK_EVENT_TYPES.SUCCEEDED]: Object.freeze({ status: TASK_EVENT_STATUS.COMPLETED, label: '结束了任务' }),
  [TASK_EVENT_TYPES.FAILED]: Object.freeze({ status: TASK_EVENT_STATUS.FAILED, label: '任务失败' }),
  [TASK_EVENT_TYPES.STOP_REQUESTED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPING, label: '请求停止任务' }),
  [TASK_EVENT_TYPES.STOPPED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务' }),
  [TASK_EVENT_TYPES.INTERRUPTED]: Object.freeze({ status: TASK_EVENT_STATUS.INTERRUPTED, label: '已中断任务' }),
});

const TASK_EVENT_COMPATIBILITY = Object.freeze({
  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,
  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,
});

class LoopService extends EventEmitter {
  constructor(db) {
    super();
    this.db = db;
    this.runtimes = new Map();
    this.resetRuntimeState();
  }

  runtime(projectId) {
    const id = Number(projectId || 0);
    if (!id) return null;
    let runtime = this.runtimes.get(id);
    if (!runtime) {
      runtime = createProjectRuntime();
      this.runtimes.set(id, runtime);
    }
    return runtime;
  }

  existingRuntime(projectId) {
    return this.runtimes.get(Number(projectId || 0)) || null;
  }

  projects() {
    return this.db
      .all('SELECT * FROM projects ORDER BY updated_at DESC, id DESC')
      .map((project) => this.withProjectRuntimeSummary(project));
  }

  withProjectRuntimeSummary(project) {
    const state =
      this.db.get(
        'SELECT phase, interval_seconds, validation_command FROM project_states WHERE project_id = ?',
        [project.id],
      ) || {};
    const runtime = this.existingRuntime(project.id);
    const running = runtime?.running ? 1 : 0;
    let phase = state.phase || 'idle';
    if (!running && !runtime?.busy && ACTIVE_RUNTIME_PHASES.has(String(phase || ''))) {
      phase = 'stopped';
    }
    return {
      ...project,
      running,
      phase,
      interval_seconds: Number(state.interval_seconds || 5),
      validation_command: state.validation_command || '',
    };
  }

  project(projectId) {
    if (!projectId) return null;
    return this.db.get('SELECT * FROM projects WHERE id = ?', [projectId]);
  }

  defaultProjectId() {
    return this.projects()[0]?.id || null;
  }

  ensureProjectState(projectId) {
    if (!projectId) return;
    this.db.run(
      `INSERT OR IGNORE INTO project_states
       (project_id, running, phase, interval_seconds, validation_command, updated_at)
       VALUES (?, 0, 'idle', 5, 'flutter analyze', ?)`,
      [projectId, nowIso()],
    );
  }

  resetRuntimeState() {
    const now = nowIso();
    this.db.run(
      `UPDATE project_states
       SET running = 0,
           phase = CASE WHEN phase IN ${ACTIVE_RUNTIME_PHASE_SQL} THEN 'stopped' ELSE phase END,
           updated_at = ?
       WHERE running != 0 OR phase IN ${ACTIVE_RUNTIME_PHASE_SQL}`,
      [now],
    );
    this.db.run(
      `UPDATE loop_state
       SET running = 0,
           phase = CASE WHEN phase IN ${ACTIVE_RUNTIME_PHASE_SQL} THEN 'stopped' ELSE phase END,
           updated_at = ?
       WHERE id = 1 AND (running != 0 OR phase IN ${ACTIVE_RUNTIME_PHASE_SQL})`,
      [now],
    );
    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE status = ?', ['pending', now, 'running']);
  }

  status(projectId = this.defaultProjectId()) {
    if (!projectId) return null;
    this.ensureProjectState(projectId);
    return this.normalizeRuntimeStatus(projectId, this.db.get('SELECT * FROM project_states WHERE project_id = ?', [projectId]));
  }

  normalizeRuntimeStatus(projectId, state) {
    if (!state) return null;
    const runtime = this.existingRuntime(projectId);
    const runtimeRunning = Boolean(runtime?.running);
    const normalized = {
      ...state,
      running: runtimeRunning ? 1 : 0,
    };

    if (!runtimeRunning && !runtime?.busy && ACTIVE_RUNTIME_PHASES.has(String(normalized.phase || ''))) {
      normalized.phase = 'stopped';
    }

    return normalized;
  }

  configure(projectId, { workspacePath, intervalSeconds, validationCommand }) {
    const current = this.status(projectId);
    const project = this.project(projectId);
    const runtime = this.existingRuntime(projectId);
    const nextWorkspace = workspacePath ?? project?.workspace_path;
    const workspaceOwner = this.activeProjectForWorkspace(nextWorkspace, projectId);
    if ((runtime?.running || runtime?.busy) && workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }
    if (!project || !current) throw new Error('项目不存在');

    this.db.run(
      `UPDATE projects
       SET workspace_path = ?, updated_at = ?
       WHERE id = ?`,
      [nextWorkspace, nowIso(), projectId],
    );
    const nextInterval = Number(intervalSeconds || current.interval_seconds || 5);
    this.db.run(
      `UPDATE project_states
       SET interval_seconds = ?, validation_command = ?, updated_at = ?
       WHERE project_id = ?`,
      [
        nextInterval,
        validationCommand ?? current.validation_command,
        nowIso(),
        projectId,
      ],
    );
    if (runtime?.running) this.scheduleProject(projectId, nextInterval);
    this.emitUpdate(projectId);
  }

  scheduleProject(projectId, intervalSeconds) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.timer) clearInterval(runtime.timer);
    runtime.timer = setInterval(() => {
      if (!runtime.running) return;
      this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    }, Math.max(1, Number(intervalSeconds || 5)) * 1000);
  }

  activeProjectForWorkspace(workspace, projectId) {
    const key = workspaceKey(workspace);
    if (!key) return null;
    for (const [id, runtime] of this.runtimes.entries()) {
      if (Number(id) === Number(projectId)) continue;
      if (!runtime.running && !runtime.busy && !runtime.activeChild) continue;
      const project = this.project(id);
      if (workspaceKey(project?.workspace_path) === key) return project;
    }
    return null;
  }

  start(projectId) {
    const state = this.status(projectId);
    const project = this.project(projectId);
    if (!project || !state) throw new Error('项目不存在');
    if (!project.workspace_path) throw new Error('请先设置项目工作区路径');
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.running) return;
    const workspaceOwner = this.activeProjectForWorkspace(project.workspace_path, projectId);
    if (workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }

    runtime.running = true;
    this.db.run(
      'UPDATE project_states SET running = 1, phase = ?, updated_at = ? WHERE project_id = ?',
      ['running', nowIso(), projectId],
    );
    this.emitUpdate(projectId);
    this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    this.scheduleProject(projectId, state.interval_seconds);
  }

  stop(projectId = null) {
    if (!projectId) {
      for (const id of Array.from(this.runtimes.keys())) this.stop(id);
      return;
    }
    const runtime = this.runtime(projectId);
    if (runtime?.timer) clearInterval(runtime.timer);
    if (runtime) {
      runtime.timer = null;
      runtime.running = false;
    }
    if (runtime?.activeOperations?.size) {
      for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {
        const child = runtime.activeChildren.get(operationKey);
        const activeTaskId = operation?.taskId || null;
        const finishedAt = nowIso();
        killChildProcess(child);
        const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
        const stoppedTask = activeTaskId
          ? this.finishTaskRun(activeTaskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true })
          : null;
        this.addEvent(
          projectId,
          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
          operation?.label || '',
          stoppedTask || activeTask
            ? taskEventMeta(stoppedTask || activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt })
            : null,
        );
      }
    }
    this.db.run(
      'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',
      ['stopped', nowIso(), projectId],
    );
    this.addEvent(projectId, 'loop.stopped', '循环已停止');
    this.emitUpdate(projectId);
  }

  async runOnce(projectId = this.defaultProjectId()) {
    if (!projectId) return;
    const runtime = this.runtime(projectId);
    if (!runtime || runtime.busy) return;
    runtime.busy = true;
    try {
      const project = this.project(projectId);
      const workspace = project?.workspace_path;
      if (!workspace) return;
      this.setPhase(projectId, 'scan');
      this.ensureWorkspaceDirs(workspace);

      // 扫描未完成、未中断、尚未生成计划的需求和反馈
      const pendingRequirements = this.db
        .all(
          `SELECT * FROM requirements
           WHERE project_id = ? AND linked_plan_id IS NULL
             AND status NOT IN ('completed', 'closed')
           ORDER BY created_at ASC`,
          [projectId],
        )
        .map((row) => ({ ...row, __type: 'requirement' }));
      const pendingFeedback = this.db
        .all(
          `SELECT * FROM feedback
           WHERE project_id = ? AND linked_plan_id IS NULL
             AND status NOT IN ('completed', 'closed')
           ORDER BY created_at ASC`,
          [projectId],
        )
        .map((row) => ({ ...row, __type: 'feedback' }));
      const pendingIntakes = [...pendingRequirements, ...pendingFeedback];
      this.addEvent(projectId, 'scan.done', `待处理需求/反馈=${pendingIntakes.length}`);

      // 一次只生成一个计划（保持串行，避免 codex 并发），剩余等下一轮 timer
      let generatedPlanId = null;
      if (pendingIntakes.length > 0) {
        generatedPlanId = await this.generatePlanForIntake(projectId, workspace, pendingIntakes[0]);
      }

      // 同步 docs/plan 目录下的 plan 文件（兼容文件式需求）
      const planScan = this.scanDirectory(path.join(workspace, 'docs', 'plan'), workspace, ['.md']);
      this.saveScan(projectId, 'plan', planScan);

      // 执行队列里可运行的 plan
      const nextPlan = generatedPlanId
        ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [generatedPlanId, projectId])
        : this.nextRunnablePlan(projectId);
      if (!nextPlan) {
        if (runtime.running || this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
          this.setPhase(projectId, pendingIntakes.length > 0 && runtime.running ? 'waiting' : 'idle');
        }
        return;
      }

      await this.processPlan(workspace, nextPlan);
      if (runtime.running) {
        this.setPhase(projectId, 'waiting');
      } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
        this.setPhase(projectId, 'idle');
      }
    } finally {
      runtime.busy = false;
      this.emitUpdate(projectId);
    }
  }

  async runTask(projectId, taskId) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    if (runtime.busy) throw new Error('该项目已有任务正在执行，请稍后再试');
    const project = this.project(projectId);
    const task = this.taskForProject(projectId, taskId);
    const plan = task ? this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [task.plan_id, projectId]) : null;
    const workspace = project?.workspace_path;
    if (!project || !task || !plan) throw new Error('任务不存在');
    if (!workspace) throw new Error('请先设置项目工作区路径');
    const workspaceOwner = this.activeProjectForWorkspace(workspace, projectId);
    if (workspaceOwner) {
      throw new Error(`工作区正在被项目「${workspaceOwner.name}」使用，请先停止对应循环`);
    }

    runtime.busy = true;
    try {
      this.startTaskRun(taskId);
      this.emitUpdate(projectId);
      const result = await this.executeTask(workspace, plan, task);
      if (result.exitCode === 0) {
        this.completeTask(workspace, plan, task, result);
      }
      if (runtime.running) {
        this.setPhase(projectId, 'waiting');
      } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
        this.setPhase(projectId, 'idle');
      }
    } finally {
      runtime.busy = false;
      this.emitUpdate(projectId);
    }
  }

  stopTask(projectId, taskId) {
    const task = this.taskForProject(projectId, taskId);
    if (!task) throw new Error('任务不存在');
    const runtime = this.runtime(projectId);

    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));
    if (activeEntry) {
      killChildProcess(activeEntry.child);
      const finishedAt = nowIso();
      const stoppedTask = runtime?.running
        ? task
        : this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
      if (!runtime.running) {
        this.addEvent(
          projectId,
          LEGACY_TASK_EVENT_TYPES.STOPPING,
          task.title,
          taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt }),
        );
      }
    } else {
      const finishedAt = nowIso();
      const stoppedTask = this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
      this.addEvent(
        projectId,
        TASK_EVENT_TYPES.STOP_REQUESTED,
        task.title,
        taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPING, finishedAt }),
      );
    }

    if (runtime?.running) this.stop(projectId);
    else this.setPhase(projectId, 'stopped');
  }

  taskForProject(projectId, taskId) {
    if (!taskId) return null;
    return this.db.get(
      `SELECT plan_tasks.*
       FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
       WHERE plan_tasks.id = ? AND plans.project_id = ?`,
      [taskId, projectId],
    );
  }

  startTaskRun(taskId, startedAt = nowIso()) {
    this.db.run(
      `UPDATE plan_tasks
       SET status = ?,
           started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,
           finished_at = NULL,
           updated_at = ?
       WHERE id = ?`,
      [TASK_EVENT_STATUS.RUNNING, TASK_EVENT_STATUS.RUNNING, startedAt, startedAt, taskId],
    );
    return this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
  }

  finishTaskRun(taskId, status, finishedAt = nowIso(), options = {}) {
    const task = this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
    if (!task) return null;
    const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
    if (options.onlyIfRunning && !isRunning) return withTaskDurationMeta(task);

    const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;
    const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);
    this.db.run(
      `UPDATE plan_tasks
       SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?
       WHERE id = ?`,
      [status, durationMs, finishedAt, finishedAt, taskId],
    );
    return withTaskDurationMeta(
      {
        ...task,
        status,
        duration_ms: durationMs,
        finished_at: finishedAt,
        updated_at: finishedAt,
      },
      runDurationMs,
    );
  }

  /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
  interruptPlan(projectId, planId) {
    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
    if (!plan) return;
    // 若当前正在执行该 plan 的任务，kill 进程
    const runtime = this.runtime(projectId);
    const activePlanOperations = runtime
      ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))
      : [];
    for (const activeEntry of activePlanOperations) {
      const finishedAt = nowIso();
      killChildProcess(activeEntry.child);
      const activeTaskId = activeEntry.operation?.taskId;
      const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
      const interruptedTask = activeTaskId
        ? this.finishTaskRun(activeTaskId, 'blocked', finishedAt, { onlyIfRunning: true })
        : null;
      this.addEvent(
        projectId,
        LEGACY_TASK_EVENT_TYPES.STOPPING,
        `plan #${planId} 的任务被中断`,
        taskEventMeta(interruptedTask || activeTask, {
          planId,
          taskId: activeTaskId || undefined,
          status: TASK_EVENT_STATUS.INTERRUPTED,
          finishedAt,
        }),
      );
    }
    // 其余未完成任务 → blocked
    this.db.run(
      `UPDATE plan_tasks SET status = ?, updated_at = ?
       WHERE plan_id = ? AND status IN ('pending', 'running')`,
      ['blocked', nowIso(), planId],
    );
    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['interrupted', nowIso(), planId]);
    this.addEvent(projectId, 'plan.interrupted', `plan #${planId} 已中断，未完成任务已挂起`);
    this.emitUpdate(projectId);
  }

  /** 恢复被中断的 plan：blocked → pending，plan → pending，循环运行时自动继续执行 */
  resumePlan(projectId, planId) {
    this.db.run(
      `UPDATE plan_tasks SET status = ?, updated_at = ?
       WHERE plan_id = ? AND status = ?`,
      ['pending', nowIso(), planId, 'blocked'],
    );
    this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
    this.addEvent(projectId, 'plan.resumed', `plan #${planId} 已恢复`);
    this.emitUpdate(projectId);
  }

  /** 向现有 plan 追加一个任务：写回 plan 文件 + syncPlanTasks 重新解析 */
  appendTask(projectId, planId, title) {
    const project = this.project(projectId);
    const workspace = project?.workspace_path;
    const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
    if (!plan) throw new Error('计划不存在');
    if (!workspace) throw new Error('请先设置项目工作区路径');

    const planFile = path.join(workspace, plan.file_path);
    if (!fs.existsSync(planFile)) throw new Error('plan 文件不存在，无法追加任务');

    const cleanTitle = String(title || '').trim();
    if (!cleanTitle) throw new Error('任务标题不能为空');

    // 计算下一个 task_key
    const existing = this.db.all('SELECT task_key FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order', [planId]);
    const maxNum = existing.reduce((max, row) => {
      const m = String(row.task_key || '').match(/P0*(\d+)/i);
      return m ? Math.max(max, Number(m[1])) : max;
    }, 0);
    const taskKey = `P${String(maxNum + 1).padStart(3, '0')}`;

    // 追加到 plan 文件的"## 任务计划"段末尾
    let content = fs.readFileSync(planFile, 'utf8');
    const line = ensureTaskScopeComment(`- [ ] ${taskKey}: ${cleanTitle}`);
    const taskSectionIdx = content.search(/##\s*任务计划/);
    if (taskSectionIdx === -1) {
      content = `${content.trim()}\n\n## 任务计划\n${line}\n`;
    } else {
      content = `${content.trimEnd()}\n${line}\n`;
    }
    fs.writeFileSync(planFile, content, 'utf8');

    // 若 plan 被中断过，恢复为 pending 让新任务可执行
    if (plan.status === 'interrupted') {
      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), planId]);
    }
    // 重新解析任务入库
    this.syncPlanTasks(planId, planFile);
    const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [planId, taskKey]);
    this.addEvent(
      projectId,
      'task.appended',
      `追加 ${taskKey}: ${cleanTitle}`,
      taskEventMeta(task, {
        planId,
        taskKey,
        taskTitle: cleanTitle,
        status: TASK_EVENT_STATUS.PENDING,
      }),
    );
    this.emitUpdate(projectId);

    // 循环在运行则立即拾取
    if (this.status(projectId)?.running) {
      this.runOnce(projectId).catch((error) => this.recordError(projectId, error));
    }
    return taskKey;
  }

  ensureWorkspaceDirs(workspace) {
    for (const dir of ['docs/issues', 'docs/plan', 'docs/progress', 'docs/progress/logs']) {
      fs.mkdirSync(path.join(workspace, dir), { recursive: true });
    }
  }

  scanDirectory(root, workspace, extensions) {
    if (!fs.existsSync(root)) return { root, aggregateHash: hashText(''), files: [] };
    const files = [];
    const visit = (dir) => {
      for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) {
          visit(full);
        } else if (entry.isFile() && extensions.includes(path.extname(entry.name).toLowerCase())) {
          const stat = fs.statSync(full);
          files.push({
            path: normalizeRelative(workspace, full),
            hash: hashFile(full),
            size: stat.size,
            modifiedAt: stat.mtime.toISOString(),
          });
        }
      }
    };
    visit(root);
    files.sort((a, b) => a.path.localeCompare(b.path));
    return {
      root,
      aggregateHash: hashText(files.map((file) => `${file.path}|${file.hash}|${file.size}`).join('\n')),
      files,
    };
  }

  saveScan(projectId, type, scan) {
    const scannedAt = nowIso();
    this.db.run('DELETE FROM scan_files WHERE project_id = ? AND scan_type = ?', [projectId, type]);
    for (const file of scan.files) {
      this.db.run(
        `INSERT OR REPLACE INTO scan_files
         (project_id, scan_type, file_path, hash, size, modified_at, scanned_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)`,
        [projectId, type, file.path, file.hash, file.size, file.modifiedAt, scannedAt],
      );
    }
  }

  hasPlanForIssueHash(projectId, issueHash) {
    return Boolean(
      this.db.get('SELECT id FROM plans WHERE project_id = ? AND issue_hash = ? LIMIT 1', [projectId, issueHash]),
    );
  }

  nextRunnablePlan(projectId) {
    return this.db.get(
      `SELECT * FROM plans
       WHERE project_id = ? AND status NOT IN ('completed', 'interrupted')
       ORDER BY created_at ASC
       LIMIT 1`,
      [projectId],
    );
  }

  async generatePlan(projectId, workspace, issueScan) {
    this.setPhase(projectId, 'generate-plan');
    const planFile = path.join(
      workspace,
      'docs',
      'plan',
      `plan_${timestampForPath()}_${issueScan.aggregateHash.slice(0, 8)}.md`,
    );
    const issueBundle = issueScan.files
      .map((file) => {
        const full = path.join(workspace, file.path);
        return ['---', `path: ${file.path}`, `hash: ${file.hash}`, 'content:', readSnippet(full, 20000)].join('\n');
      })
      .join('\n');

    const prompt = [
      '你是需求整理与开发计划生成者。',
      '请根据 docs/issues 收集到的反馈和需求，生成一个开发计划和验收标准。',
      '',
      `输出文件：${planFile}`,
      '',
      '格式要求：',
      '- 每个任务必须严格使用固定格式：- [ ] P001: 任务标题 <!-- scope: lib/foo.dart,test/foo_test.dart -->',
      '- scope 必填，表示该任务预计修改的文件或模块；多个 scope 用英文逗号分隔；无法判断时写 <!-- scope: unknown -->，unknown 任务不会并发执行',
      '- 每个任务要有验收要点',
      '- 必须包含总体验收标准和进度区',
      '- 只写 plan 文件，不要改业务代码',
      '',
      '需求快照：',
      issueBundle,
    ].join('\n');

    const result = await this.runCodex(workspace, prompt, 'generate-plan', { projectId });
    if (result.exitCode !== 0 || !fs.existsSync(planFile)) {
      this.addEvent(projectId, 'plan.generate.failed', result.logFile);
      return;
    }

    const id = this.db.insert(
      `INSERT INTO plans
       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validation_passed, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
      [
        projectId,
        issueScan.aggregateHash,
        normalizeRelative(workspace, planFile),
        hashFile(planFile),
        'pending',
        nowIso(),
        nowIso(),
      ],
    );
    this.syncPlanTasks(id, planFile);
    this.db.run('UPDATE project_states SET last_issue_hash = ?, updated_at = ? WHERE project_id = ?', [
      issueScan.aggregateHash,
      nowIso(),
      projectId,
    ]);
    this.addEvent(projectId, 'plan.generated', normalizeRelative(workspace, planFile));
  }

  /** 为单条需求/反馈生成计划（调 codex），并回写 linked_plan_id。失败返回 null，下轮重试。 */
  async generatePlanForIntake(projectId, workspace, intake) {
    const table = intake.__type === 'feedback' ? 'feedback' : 'requirements';
    const sourceName = intake.__type === 'feedback' ? '反馈' : '需求';
    this.setPhase(projectId, 'generate-plan');
    const safeId = String(intake.id).replace(/[^0-9a-zA-Z_-]/g, '');
    const planFile = path.join(
      workspace,
      'docs',
      'plan',
      `plan_${intake.__type}_${safeId}_${timestampForPath()}.md`,
    );
    const prompt = [
      '你是需求整理与开发计划生成者。',
      `请根据以下${sourceName}，生成一个开发计划和验收标准。`,
      '',
      `输出文件：${planFile}`,
      '',
      '格式要求：',
      '- 每个任务必须严格使用固定格式：- [ ] P001: 任务标题 <!-- scope: lib/foo.dart,test/foo_test.dart -->',
      '- scope 必填，表示该任务预计修改的文件或模块；多个 scope 用英文逗号分隔；无法判断时写 <!-- scope: unknown -->，unknown 任务不会并发执行',
      '- 每个任务要有验收要点',
      '- 必须包含总体验收标准和进度区',
      '- 只写 plan 文件，不要改业务代码',
      '',
      `${sourceName} #${intake.id} 内容：`,
      String(intake.body || '').trim() || '（正文为空）',
    ].join('\n');

    const result = await this.runCodex(workspace, prompt, `gen-${intake.__type}-${intake.id}`, { projectId });
    if (result.exitCode !== 0 || !fs.existsSync(planFile)) {
      this.addEvent(projectId, 'plan.generate.failed', `${sourceName} #${intake.id} 计划生成失败：${result.logFile}`);
      return null;
    }

    const issueHash = `${intake.__type}-${intake.id}-${hashText(String(intake.body || '')).slice(0, 16)}`;
    const id = this.db.insert(
      `INSERT INTO plans
       (project_id, issue_hash, file_path, hash, status, total_tasks, completed_tasks, validation_passed, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, 0, 0, 0, ?, ?)`,
      [projectId, issueHash, normalizeRelative(workspace, planFile), hashFile(planFile), 'pending', nowIso(), nowIso()],
    );
    this.syncPlanTasks(id, planFile);
    // 回写关联
    this.db.run(`UPDATE ${table} SET linked_plan_id = ?, updated_at = ? WHERE id = ?`, [id, nowIso(), intake.id]);
    this.addEvent(projectId, 'plan.generated', `${sourceName} #${intake.id} 已生成计划：${normalizeRelative(workspace, planFile)}`);
    return id;
  }

  async processPlan(workspace, plan) {
    const planFile = path.join(workspace, plan.file_path);
    this.syncPlanTasks(plan.id, planFile);
    const pendingTasks = this.db.all(
      `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
       ORDER BY sort_order ASC`,
      [plan.id],
    );
    if (pendingTasks.length) {
      const batch = this.parallelTaskBatch(pendingTasks);
      if (batch.length > 1) {
        await this.executeTaskBatch(workspace, plan, batch);
      } else {
        const task = batch[0] || pendingTasks[0];
        const result = await this.executeTask(workspace, plan, task);
        if (result.exitCode === 0) {
          this.completeTask(workspace, plan, task, result);
        }
      }
      return;
    }
    await this.validatePlan(workspace, plan);
  }

  parallelTaskBatch(tasks) {
    const selected = [];
    const usedScopes = new Set();
    for (const task of tasks) {
      if (selected.length >= MAX_PARALLEL_TASKS) break;
      const scopes = taskParallelScopes(task);
      if (!scopes.length) {
        if (!selected.length) return [task];
        continue;
      }
      if (scopes.some((scope) => usedScopes.has(scope))) continue;
      selected.push(task);
      for (const scope of scopes) usedScopes.add(scope);
    }
    return selected.length > 1 ? selected : [tasks[0]];
  }

  async executeTaskBatch(workspace, plan, tasks) {
    this.setPhase(plan.project_id, 'execute-task');
    this.addEvent(
      plan.project_id,
      'tasks.parallel.started',
      `并发执行 ${tasks.map((task) => task.task_key).join(', ')}`,
      { taskIds: tasks.map((task) => task.id) },
    );
    const results = await Promise.all(
      tasks.map(async (task) => {
        try {
          return {
            task,
            result: await this.executeTask(workspace, plan, task, { parallel: true }),
          };
        } catch (error) {
          const finishedAt = nowIso();
          const failedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
          this.addEvent(
            plan.project_id,
            TASK_EVENT_TYPES.FAILED,
            task.title,
            taskEventMeta(failedTask, {
              planId: plan.id,
              status: TASK_EVENT_STATUS.FAILED,
              finishedAt,
              error: error?.message || String(error),
            }),
          );
          this.emitUpdate(plan.project_id);
          return { task, result: { exitCode: -1 } };
        }
      }),
    );
    for (const { task, result } of results) {
      if (result.exitCode === 0) {
        this.completeTask(workspace, plan, task, result);
      }
    }
  }

  async executeTask(workspace, plan, task, options = {}) {
    this.setPhase(plan.project_id, 'execute-task');
    this.startTaskRun(task.id);
    this.emitUpdate(plan.project_id);
    const planFile = path.join(workspace, plan.file_path);
    const completionRules = [
      '- plan 文件是只读上下文：不要修改 plan 文件，不要勾选 checkbox，不要更新 plan 进度区',
      '- AutoPlan 会在任务成功后统一写回数据库、checkbox 和进度区',
      '- 只修改当前任务 scope 直接相关的业务文件',
      '- 如需测试，只运行与当前任务直接相关的最小测试集，不要运行全量测试或与本任务无关的测试',
      '- 只有当前任务本身明确要求全量验证时，才允许运行全量测试',
      '- 不输出完整 diff、源码全文或长文件列表',
      '- 中文文件读写使用 UTF-8',
    ];
    if (options.parallel) {
      completionRules.unshift('- 当前为并发执行模式，不要读写其它任务的 scope');
    }
    const prompt = [
      '你是开发执行者。',
      `请只执行指定任务 ${task.task_key}，不要提前执行其它任务，也不要顺手处理其它 checkbox。`,
      '',
      `plan 文件（只读）：${planFile}`,
      `指定任务：${task.raw_line}`,
      `任务 scope：${task.scope || taskScopeText(task) || 'unknown'}`,
      '',
      '完成后必须：',
      ...completionRules,
    ].join('\n');
    const result = await this.runCodexWithPlanGuard(workspace, prompt, `execute-${task.task_key}`, {
      projectId: plan.project_id,
      planId: plan.id,
      taskId: task.id,
      parallel: Boolean(options.parallel),
    }, planFile);
    const finishedAt = nowIso();
    result.finishedAt = finishedAt;
    const succeeded = result.exitCode === 0;
    if (!succeeded) {
      const failedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
      this.emitUpdate(plan.project_id);
      this.addEvent(
        plan.project_id,
        TASK_EVENT_TYPES.FAILED,
        task.title,
        taskEventMeta(failedTask, {
          planId: plan.id,
          status: TASK_EVENT_STATUS.FAILED,
          finishedAt,
          exitCode: result.exitCode,
          log: result.logFile,
        }),
      );
    }
    return result;
  }

  completeTask(workspace, plan, task, result) {
    const planFile = path.join(workspace, plan.file_path);
    const finishedAt = result?.finishedAt || nowIso();
    this.markTaskCompletedInPlan(workspace, planFile, task, result);
    const completedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.COMPLETED, finishedAt) || task;
    this.addEvent(
      plan.project_id,
      LEGACY_TASK_EVENT_TYPES.EXECUTED,
      task.title,
      taskEventMeta(completedTask, {
        planId: plan.id,
        status: TASK_EVENT_STATUS.COMPLETED,
        finishedAt,
        exitCode: result?.exitCode,
        log: result?.logFile,
      }),
    );
    this.refreshPlanProgress(plan.id, planFile);
    this.emitUpdate(plan.project_id);
  }

  refreshPlanProgress(planId, planFile) {
    const totals = this.db.get(
      `SELECT COUNT(*) AS total,
              SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) AS completed
       FROM plan_tasks
       WHERE plan_id = ?`,
      [planId],
    ) || { total: 0, completed: 0 };
    const total = Number(totals.total || 0);
    const completed = Number(totals.completed || 0);
    const status = total > 0 && completed === total ? 'ready_for_validation' : 'running';
    const hash = fs.existsSync(planFile) ? hashFile(planFile) : '';
    this.db.run(
      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
      [hash, status, total, completed, nowIso(), planId],
    );
  }

  markTaskCompletedInPlan(workspace, planFile, task, result) {
    if (!fs.existsSync(planFile)) return;
    const relativeLog = result?.logFile ? normalizeRelative(workspace, result.logFile) : '';
    const key = escapeRegExp(String(task.task_key || ''));
    const checkboxRe = new RegExp(`(^\\s*[-*]\\s+\\[)([ xX])(\\]\\s+${key}(?:\\b|[:：\\s-]))`, 'm');
    let content = fs.readFileSync(planFile, 'utf8');
    if (checkboxRe.test(content)) {
      content = content.replace(checkboxRe, '$1x$3');
    }

    const note = `- ${task.task_key} AutoPlan 完成：${nowIso()}${relativeLog ? `；日志：${relativeLog}` : ''}`;
    const noteRe = new RegExp(`^\\s*-\\s+${key}\\s+AutoPlan 完成：.*$`, 'm');
    if (noteRe.test(content)) {
      content = content.replace(noteRe, note);
    } else if (/##\s*进度区/.test(content)) {
      content = `${content.trimEnd()}\n${note}\n`;
    } else {
      content = `${content.trimEnd()}\n\n## 进度区\n${note}\n`;
    }
    fs.writeFileSync(planFile, content, 'utf8');
  }

  async validatePlan(workspace, plan) {
    this.setPhase(plan.project_id, 'validate');
    const planFile = path.join(workspace, plan.file_path);
    const command = String(this.status(plan.project_id)?.validation_command || '').trim();
    if (!command) {
      // 验收命令为空：跳过校验，直接标记完成。
      this.db.run(
        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
        ['completed', nowIso(), plan.id],
      );
      this.addEvent(plan.project_id, 'plan.completed', '任务全部完成（验收命令为空，已跳过校验）');
      return;
    }
    let validation = await this.runShell(workspace, command, `validate-${plan.id}`, { projectId: plan.project_id });
    for (let attempt = 1; validation.exitCode !== 0 && attempt <= 2; attempt += 1) {
      this.db.run('UPDATE plans SET status = ?, updated_at = ? WHERE id = ?', [
        'validation_failed',
        nowIso(),
        plan.id,
      ]);
      const prompt = [
        'plan 已完成，但宿主项目验收失败。请只修复验证错误。',
        `plan 文件（只读）：${planFile}`,
        '不要修改 plan 文件、checkbox 或进度区。',
        `失败命令：${command}`,
        '失败输出摘要：',
        tailText(validation.output, 12000),
      ].join('\n');
      await this.runCodexWithPlanGuard(workspace, prompt, `repair-${plan.id}-${attempt}`, {
        projectId: plan.project_id,
        planId: plan.id,
      }, planFile);
      validation = await this.runShell(workspace, command, `validate-${plan.id}-repair-${attempt}`, {
        projectId: plan.project_id,
      });
    }

    if (validation.exitCode === 0) {
      this.db.run(
        'UPDATE plans SET status = ?, validation_passed = 1, updated_at = ? WHERE id = ?',
        ['completed', nowIso(), plan.id],
      );
      this.addEvent(plan.project_id, 'plan.completed', plan.file_path);
    } else {
      this.addEvent(plan.project_id, 'validation.failed', validation.logFile);
    }
  }

  syncPlanTasks(planId, planFile) {
    if (!fs.existsSync(planFile)) return;
    normalizePlanTaskScopes(planFile);
    const text = fs.readFileSync(planFile, 'utf8');
    const regex = /^\s*[-*]\s+\[([ xX])\]\s+(.+)$/gm;
    const tasks = [];
    let match;
    let index = 0;
    while ((match = regex.exec(text))) {
      index += 1;
      const rawTitle = match[2].trim();
      const titleWithoutScope = stripTaskScopeComment(rawTitle);
      const idMatch = titleWithoutScope.match(/^([A-Za-z]+[-_]?\d+|P\d+)[:：\s-]+(.+)$/);
      tasks.push({
        key: idMatch?.[1] || `P${String(index).padStart(3, '0')}`,
        title: idMatch?.[2]?.trim() || titleWithoutScope || rawTitle,
        rawLine: ensureTaskScopeComment(match[0]),
        scope: taskScopeText({ raw_line: match[0], title: rawTitle }),
        status: match[1].toLowerCase() === 'x' ? 'completed' : 'pending',
        sortOrder: index,
      });
    }

    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
    const existingByKey = new Map();
    for (const existing of existingTasks) {
      const matches = existingByKey.get(existing.task_key) || [];
      matches.push(existing);
      existingByKey.set(existing.task_key, matches);
    }

    const syncedStatuses = [];
    for (const task of tasks) {
      const existing = existingByKey.get(task.key)?.shift();
      const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
      syncedStatuses.push(status);
      if (existing) {
        this.db.run(
          `UPDATE plan_tasks
           SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
           WHERE id = ?`,
          [
            task.title,
            task.rawLine,
            task.scope,
            status,
            task.sortOrder,
            nowIso(),
            existing.id,
          ],
        );
      } else {
        this.db.run(
          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
           VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
          [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
        );
      }
    }

    for (const matches of existingByKey.values()) {
      for (const stale of matches) {
        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
      }
    }

    const completed = syncedStatuses.filter((status) => status === 'completed').length;
    const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
    this.db.run(
      'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
      [hashFile(planFile), status, tasks.length, completed, nowIso(), planId],
    );
  }

  /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
  archiveOperation(projectId, operationKey) {
    const runtime = this.runtime(projectId);
    if (!runtime) return;
    const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOperation;
    if (op) {
      if (op.activity && typeof op.activity.flush === 'function') {
        op.activity.flush();
      }
      runtime.lastOperation = {
        label: op.label || '',
        projectId: op.projectId || null,
        planId: op.planId || null,
        taskId: op.taskId || null,
        startedAt: op.startedAt || null,
        finishedAt: nowIso(),
        exitCode: typeof op.exitCode === 'number' ? op.exitCode : null,
        logTail: (op.logBuffer || '').slice(-8000),
        activity: op.activity ? op.activity.getLines() : [],
      };
    }
    if (operationKey) {
      runtime.activeChildren.delete(operationKey);
      runtime.activeOperations.delete(operationKey);
    } else {
      runtime.activeChildren.clear();
      runtime.activeOperations.clear();
    }
    refreshRuntimeActive(runtime);
  }

  async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
    const before = planFile && fs.existsSync(planFile) ? fs.readFileSync(planFile, 'utf8') : null;
    const result = await this.runCodex(workspace, prompt, label, operation);
    if (before !== null) {
      const changed = !fs.existsSync(planFile) || fs.readFileSync(planFile, 'utf8') !== before;
      if (changed) {
        fs.writeFileSync(planFile, before, 'utf8');
        this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
          planId: operation.planId || null,
          taskId: operation.taskId || null,
        });
      }
    }
    return result;
  }

  async runCodex(workspace, prompt, label, operation = {}) {
    const projectIdForEmit = operation.projectId;
    const runtime = this.runtime(projectIdForEmit);
    if (!runtime) throw new Error('projectId is required for codex operations');
    const logDir = path.join(workspace, 'docs', 'progress', 'logs');
    fs.mkdirSync(logDir, { recursive: true });
    const prefix = `${timestampForPath()}_${safePart(label)}`;
    const logFile = path.join(logDir, `${prefix}.log`);
    const lastFile = path.join(logDir, `${prefix}.last.txt`);
    const args = [
      'exec',
      '--cd',
      workspace,
      '--color',
      'never',
      '-o',
      lastFile,
      '--sandbox',
      'danger-full-access',
      '-',
    ];
    const child = spawn('codex', args, { shell: process.platform === 'win32', cwd: workspace });
    const activeOperation = {
      ...operation,
      label,
      logBuffer: '',
      activity: new CodexActivityPrinter(200),
      startedAt: nowIso(),
    };
    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
    child.stdin.setDefaultEncoding('utf8');
    child.stdin.end(prompt);
    const stream = fs.createWriteStream(logFile, { encoding: 'utf8' });
    // 累积原始日志 + 提取可读活动行
    const onChunk = (chunk) => {
      const text = chunk.toString('utf8');
      if (!runtime.activeOperations.has(operationKey)) return;
      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
      if (activeOperation.logBuffer.length > 24000) {
        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
      }
      // 喂给活动打印机，提取人类可读的进度行
      if (activeOperation.activity) activeOperation.activity.offer(text);
    };
    child.stdout.on('data', onChunk);
    child.stderr.on('data', onChunk);
    child.stdout.pipe(stream, { end: false });
    child.stderr.pipe(stream, { end: false });
    const tailTimer = setInterval(() => {
      if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
    }, 600);
    try {
      const exitCode = await waitForChild(child, 45 * 60 * 1000);
      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
      return { exitCode, logFile, lastFile };
    } finally {
      clearInterval(tailTimer);
      stream.end();
      if (runtime.activeOperations.has(operationKey)) {
        this.archiveOperation(projectIdForEmit, operationKey);
      }
    }
  }

  async runShell(workspace, command, label, operation = {}) {
    const projectIdForEmit = operation.projectId;
    const runtime = this.runtime(projectIdForEmit);
    if (!runtime) throw new Error('projectId is required for shell operations');
    const logDir = path.join(workspace, 'docs', 'progress', 'logs');
    fs.mkdirSync(logDir, { recursive: true });
    const logFile = path.join(logDir, `${timestampForPath()}_${safePart(label)}.log`);
    const shellCommand = process.platform === 'win32' ? `chcp 65001>nul && ${command}` : command;
    const child = spawn(shellCommand, {
      shell: true,
      cwd: workspace,
      env: { ...process.env },
    });
    const activeOperation = {
      ...operation,
      label,
      logBuffer: '',
      activity: new CodexActivityPrinter(200),
      startedAt: nowIso(),
    };
    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
    let output = '';
    const onChunk = (chunk) => {
      const text = chunk.toString('utf8');
      output += text;
      if (!runtime.activeOperations.has(operationKey)) return;
      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
      if (activeOperation.logBuffer.length > 24000) {
        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
      }
      if (activeOperation.activity) activeOperation.activity.offer(text);
    };
    child.stdout.on('data', onChunk);
    child.stderr.on('data', onChunk);
    const tailTimer = setInterval(() => {
      if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
    }, 600);
    try {
      const exitCode = await waitForChild(child, 10 * 60 * 1000);
      fs.writeFileSync(logFile, output, 'utf8');
      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
      return { exitCode, output, logFile };
    } finally {
      clearInterval(tailTimer);
      if (runtime.activeOperations.has(operationKey)) {
        this.archiveOperation(projectIdForEmit, operationKey);
      }
    }
  }

  setPhase(projectId, phase) {
    this.db.run('UPDATE project_states SET phase = ?, updated_at = ? WHERE project_id = ?', [
      phase,
      nowIso(),
      projectId,
    ]);
    this.emitUpdate(projectId);
  }

  recordError(projectId, error) {
    const message = error?.stack || error?.message || String(error);
    this.db.run(
      'UPDATE project_states SET phase = ?, last_error = ?, updated_at = ? WHERE project_id = ?',
      ['error', message, nowIso(), projectId],
    );
    this.addEvent(projectId, 'loop.error', message);
    this.emitUpdate(projectId);
  }

  addEvent(projectId, type, message, meta = null) {
    this.db.run('INSERT INTO events (project_id, type, message, meta, created_at) VALUES (?, ?, ?, ?, ?)', [
      projectId,
      type,
      message,
      meta ? JSON.stringify(meta) : null,
      nowIso(),
    ]);
    this.emitUpdate(projectId);
  }

  emitUpdate(projectId) {
    this.emit('update', this.snapshot(projectId));
  }

  snapshot(projectId = null) {
    const projects = this.projects();
    if (!projectId) return emptySnapshot(projects);

    const activeProject = this.project(projectId);
    if (!activeProject) return emptySnapshot(projects);

    const state = {
      ...(this.status(projectId) || {}),
      workspace_path: activeProject.workspace_path || '',
    };
    const runtime = this.existingRuntime(projectId);

    return {
      activeProjectId: projectId,
      activeProject,
      projects,
      state,
      requirements: this.db.all(
        `SELECT requirements.*, plans.status AS plan_status,
                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
         FROM requirements
         LEFT JOIN plans ON plans.id = requirements.linked_plan_id
         WHERE requirements.project_id = ?
         ORDER BY requirements.updated_at DESC`,
        [projectId],
      ),
      feedback: this.db.all(
        `SELECT feedback.*, plans.status AS plan_status,
                plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
         FROM feedback
         LEFT JOIN plans ON plans.id = feedback.linked_plan_id
         WHERE feedback.project_id = ?
         ORDER BY feedback.updated_at DESC`,
        [projectId],
      ),
      attachments: this.db.all(
        'SELECT * FROM attachments WHERE project_id = ? ORDER BY created_at DESC, id DESC',
        [projectId],
      ),
      planDrafts: this.db.all('SELECT * FROM plan_drafts WHERE project_id = ? ORDER BY updated_at DESC, id DESC', [
        projectId,
      ]),
      plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
      tasks: this.db.all(
        `SELECT plan_tasks.*, plans.file_path
         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
         WHERE plans.project_id = ?
         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
        [projectId],
      ),
      events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
      scans: this.db.all(
        'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
        [projectId],
      ),
      activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) === Number(projectId)
        ? {
            label: runtime.activeOperation.label || '',
            projectId: runtime.activeOperation.projectId || null,
            planId: runtime.activeOperation.planId || null,
            taskId: runtime.activeOperation.taskId || null,
            startedAt: runtime.activeOperation.startedAt || null,
            logTail: (runtime.activeOperation.logBuffer || '').slice(-8000),
            activity: runtime.activeOperation.activity ? runtime.activeOperation.activity.getLines() : [],
          }
        : null,
      activeOperations: runtime?.activeOperations
        ? Array.from(runtime.activeOperations.values())
            .filter((operation) => Number(operation.projectId) === Number(projectId))
            .map((operation) => ({
              label: operation.label || '',
              projectId: operation.projectId || null,
              planId: operation.planId || null,
              taskId: operation.taskId || null,
              startedAt: operation.startedAt || null,
              logTail: (operation.logBuffer || '').slice(-8000),
              activity: operation.activity ? operation.activity.getLines() : [],
            }))
        : [],
      lastOperation:
        runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
          ? runtime.lastOperation
          : null,
    };
  }
}

function taskEventMeta(task, overrides = {}) {
  const meta = {
    ...compactEventMeta({
      taskId: task?.id,
      taskKey: task?.task_key,
      taskTitle: task?.title,
      planId: task?.plan_id,
      status: task?.status,
      startedAt: task?.started_at,
      finishedAt: task?.finished_at,
      durationMs: task?.duration_ms,
      runDurationMs: task?.run_duration_ms,
    }),
    ...compactEventMeta(overrides),
  };
  meta.taskId = normalizeOptionalNumber(meta.taskId);
  meta.planId = normalizeOptionalNumber(meta.planId);
  meta.taskKey = normalizeOptionalString(meta.taskKey);
  meta.taskTitle = normalizeOptionalString(meta.taskTitle);
  meta.status = normalizeOptionalString(meta.status);
  meta.startedAt = normalizeOptionalString(meta.startedAt);
  meta.finishedAt = normalizeOptionalString(meta.finishedAt);
  meta.durationMs = normalizeOptionalNumber(meta.durationMs);
  meta.runDurationMs = normalizeOptionalNumber(meta.runDurationMs);
  const compacted = compactEventMeta(meta);
  return Object.keys(compacted).length ? compacted : null;
}

function syncedTaskStatus(parsedStatus, existingStatus) {
  const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
  const current = normalizeOptionalString(existingStatus);
  if (!current) return next;
  if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
  if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
  return next;
}

function compactEventMeta(meta) {
  const result = {};
  for (const [key, value] of Object.entries(meta || {})) {
    if (value !== undefined && value !== null && value !== '') result[key] = value;
  }
  return result;
}

function normalizeOptionalNumber(value) {
  if (value === undefined || value === null || value === '') return undefined;
  const number = Number(value);
  return Number.isFinite(number) ? number : undefined;
}

function normalizeOptionalString(value) {
  if (value === undefined || value === null) return undefined;
  const text = String(value).trim();
  return text || undefined;
}

function withTaskDurationMeta(task, runDurationMs) {
  if (!task) return null;
  return {
    ...task,
    duration_ms: normalizeDurationMs(task.duration_ms),
    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
  };
}

function normalizeDurationMs(value) {
  const number = Number(value);
  if (!Number.isFinite(number) || number <= 0) return 0;
  return Math.round(number);
}

function taskRunDurationMs(startedAt, finishedAt) {
  const started = Date.parse(startedAt || '');
  const finished = Date.parse(finishedAt || '');
  if (!Number.isFinite(started) || !Number.isFinite(finished) || finished <= started) return 0;
  return Math.round(finished - started);
}

function createProjectRuntime() {
  return {
    timer: null,
    running: false,
    busy: false,
    activeChild: null,
    activeOperation: null,
    activeChildren: new Map(),
    activeOperations: new Map(),
    lastOperation: null,
  };
}

function emptySnapshot(projects) {
  return {
    activeProjectId: null,
    activeProject: null,
    projects,
    state: null,
    requirements: [],
    feedback: [],
    attachments: [],
    planDrafts: [],
    plans: [],
    tasks: [],
    events: [],
    scans: [],
    activeOperation: null,
    activeOperations: [],
    lastOperation: null,
  };
}

function registerRuntimeOperation(runtime, child, operation) {
  if (!runtime.activeChildren) runtime.activeChildren = new Map();
  if (!runtime.activeOperations) runtime.activeOperations = new Map();
  const operationKey = `${Date.now()}-${Math.random().toString(36).slice(2)}`;
  runtime.activeChildren.set(operationKey, child);
  runtime.activeOperations.set(operationKey, operation);
  runtime.activeChild = child;
  runtime.activeOperation = operation;
  return operationKey;
}

function refreshRuntimeActive(runtime) {
  const entries = Array.from(runtime.activeOperations?.entries?.() || []);
  const latest = entries.at(-1);
  if (!latest) {
    runtime.activeChild = null;
    runtime.activeOperation = null;
    return;
  }
  runtime.activeChild = runtime.activeChildren.get(latest[0]) || null;
  runtime.activeOperation = latest[1] || null;
}

function findRuntimeOperation(runtime, predicate) {
  return findRuntimeOperations(runtime, predicate)[0] || null;
}

function findRuntimeOperations(runtime, predicate) {
  if (!runtime?.activeOperations) return [];
  const matches = [];
  for (const [operationKey, operation] of runtime.activeOperations.entries()) {
    if (predicate(operation)) {
      matches.push({
        operationKey,
        operation,
        child: runtime.activeChildren?.get(operationKey) || null,
      });
    }
  }
  return matches;
}

function waitForChild(child, timeoutMs) {
  return new Promise((resolve) => {
    let settled = false;
    let killTimer = null;
    const finish = (code) => {
      if (settled) return;
      settled = true;
      clearTimeout(timer);
      if (killTimer) clearTimeout(killTimer);
      resolve(code ?? 0);
    };
    const timer = setTimeout(() => {
      killChildProcess(child);
      killTimer = setTimeout(() => finish(-1), 5000);
    }, timeoutMs);
    child.on('exit', (code) => {
      finish(code);
    });
    child.on('error', () => finish(-1));
  });
}

function killChildProcess(child) {
  if (!child || child.killed) return;
  if (process.platform === 'win32' && child.pid) {
    const killer = spawn('taskkill', ['/pid', String(child.pid), '/T', '/F'], { windowsHide: true });
    killer.on('error', () => child.kill());
    return;
  }
  child.kill('SIGTERM');
}

function hashFile(filePath) {
  return crypto.createHash('sha256').update(fs.readFileSync(filePath)).digest('hex');
}

function hashText(text) {
  return crypto.createHash('sha256').update(text).digest('hex');
}

function taskParallelScopes(task) {
  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  if (PARALLEL_BLOCKING_TASK_RE.test(raw)) return [];
  return taskDeclaredScopes(task, { keepUnknown: false });
}

function taskScopeText(task) {
  const explicit = taskDeclaredScopes(task, { keepUnknown: true, includePathFallback: false });
  if (explicit.length) return explicit.join(', ');
  const inferred = taskDeclaredScopes(task, { keepUnknown: false });
  return inferred.join(', ') || 'unknown';
}

function taskDeclaredScopes(task, options = {}) {
  const { keepUnknown = false, includePathFallback = true } = options;
  const raw = `${task.task_key || ''} ${task.title || ''} ${task.raw_line || ''}`;
  const scopes = new Set();

  addScopeParts(scopes, String(task.scope || '').split(TASK_SCOPE_SPLIT_RE), { keepUnknown });
  addScopeParts(scopes, explicitTaskScopeParts(raw), { keepUnknown });

  if (includePathFallback) {
    for (const match of raw.matchAll(TASK_PATH_RE)) {
      const scope = normalizeTaskScope(match[0], { keepUnknown });
      if (scope && !scope.startsWith('docs/plan/') && !scope.startsWith('docs/progress/')) {
        scopes.add(scope);
      }
    }
  }

  return Array.from(scopes);
}

function explicitTaskScopeParts(raw) {
  const explicit = String(raw || '').match(TASK_SCOPE_RE);
  return explicit?.[1] ? explicit[1].split(TASK_SCOPE_SPLIT_RE) : [];
}

function addScopeParts(scopes, parts, options = {}) {
  for (const part of parts) {
    const scope = normalizeTaskScope(part, options);
    if (scope) scopes.add(scope);
  }
}

function normalizeTaskScope(value, options = {}) {
  const scope = String(value || '')
    .trim()
    .replace(/^["'`[{(]+|["'`\]})]+$/g, '')
    .replace(/\s*--$/, '')
    .replaceAll('\\', '/')
    .toLowerCase();
  if (!scope || scope === '-') return '';
  if (scope === 'unknown') return options.keepUnknown ? 'unknown' : '';
  return scope;
}

function ensureTaskScopeComment(line, fallbackScope = 'unknown') {
  const text = String(line || '').trimEnd();
  return TASK_SCOPE_RE.test(text) ? text : `${text} <!-- scope: ${fallbackScope} -->`;
}

function stripTaskScopeComment(value) {
  return String(value || '').replace(TASK_SCOPE_COMMENT_RE, ' ').replace(/\s+/g, ' ').trim();
}

function normalizePlanTaskScopes(planFile) {
  const content = fs.readFileSync(planFile, 'utf8');
  let changed = false;
  const next = content.replace(/^(\s*[-*]\s+\[[ xX]\]\s+.+)$/gm, (line) => {
    if (TASK_SCOPE_RE.test(line)) return line;
    changed = true;
    return ensureTaskScopeComment(line);
  });
  if (changed) fs.writeFileSync(planFile, next, 'utf8');
}

function normalizeRelative(root, fullPath) {
  return path.relative(root, fullPath).replaceAll(path.sep, '/');
}

function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function workspaceKey(workspace) {
  const value = String(workspace || '').trim();
  if (!value) return '';
  const resolved = path.resolve(value);
  return process.platform === 'win32' ? resolved.toLowerCase() : resolved;
}

function timestampForPath() {
  const now = new Date();
  const pad = (n) => String(n).padStart(2, '0');
  return `${now.getFullYear()}${pad(now.getMonth() + 1)}${pad(now.getDate())}-${pad(
    now.getHours(),
  )}${pad(now.getMinutes())}${pad(now.getSeconds())}`;
}

function readSnippet(filePath, maxChars) {
  const text = fs.readFileSync(filePath, 'utf8');
  return text.length > maxChars ? `${text.slice(0, maxChars)}\n...[truncated]` : text;
}

function tailText(text, maxChars) {
  return text.length > maxChars ? text.slice(text.length - maxChars) : text;
}

function safePart(value) {
  return String(value).replace(/[^A-Za-z0-9_.-]+/g, '_');
}

module.exports = {
  LoopService,
  LEGACY_TASK_EVENT_TYPES,
  TASK_EVENT_COMPATIBILITY,
  TASK_EVENT_SEMANTICS,
  TASK_EVENT_STATUS,
  TASK_EVENT_TYPES,
};

export type IntakeType = 'requirement' | 'feedback';
export type WorkspaceTab = 'overview' | 'requirement' | 'feedback' | 'tasks' | 'events';

export const WORKSPACE_SEARCH_SOURCE_TYPES = {
  REQUIREMENT: 'requirement',
  FEEDBACK: 'feedback',
  PLAN_DRAFT: 'planDraft',
  PLAN: 'plan',
  TASK: 'task',
  EVENT: 'event',
} as const;

export type WorkspaceSearchSourceType =
  (typeof WORKSPACE_SEARCH_SOURCE_TYPES)[keyof typeof WORKSPACE_SEARCH_SOURCE_TYPES];

export const WORKSPACE_SEARCH_HIT_FIELDS = {
  TITLE: 'title',
  BODY: 'body',
  STATUS: 'status',
  MARKDOWN: 'markdown',
  FILE_PATH: 'filePath',
  SOURCE_PATH: 'sourcePath',
  TASK_KEY: 'taskKey',
  SCOPE: 'scope',
  RAW_LINE: 'rawLine',
  EVENT_TYPE: 'eventType',
  EVENT_MESSAGE: 'eventMessage',
  EVENT_META: 'eventMeta',
} as const;

export type WorkspaceSearchHitField =
  (typeof WORKSPACE_SEARCH_HIT_FIELDS)[keyof typeof WORKSPACE_SEARCH_HIT_FIELDS];

export interface WorkspaceSearchQuery {
  raw: string;
  normalized: string;
  terms: string[];
  isEmpty: boolean;
}

export interface WorkspaceSearchSourceConfig {
  type: WorkspaceSearchSourceType;
  label: string;
  targetTab: WorkspaceTab;
}

export interface WorkspaceSearchMatch {
  field: WorkspaceSearchHitField;
  label: string;
  value: string;
  snippet?: string;
}

export interface WorkspaceSearchResult {
  id: string;
  source: WorkspaceSearchSourceType;
  targetTab: WorkspaceTab;
  recordId: number;
  title: string;
  summary: string;
  status: string | null;
  updatedAt: string;
  matches: WorkspaceSearchMatch[];
}

export interface WorkspaceSearchGroup {
  source: WorkspaceSearchSourceType;
  label: string;
  targetTab: WorkspaceTab;
  count: number;
  results: WorkspaceSearchResult[];
}

export interface WorkspaceSearchState {
  query: WorkspaceSearchQuery;
  total: number;
  results: WorkspaceSearchResult[];
  groups: WorkspaceSearchGroup[];
}

export interface Project {
  id: number;
  name: string;
  workspace_path: string;
  description: string;
  created_at: string;
  updated_at: string;
  running?: number;
  phase?: string;
  interval_seconds?: number;
  validation_command?: string;
}

export interface ProjectState {
  project_id: number;
  running: number;
  phase: string;
  interval_seconds: number;
  validation_command: string;
  last_issue_hash?: string | null;
  last_error?: string | null;
  updated_at: string;
  /** 由 snapshot 合并自 project.workspace_path */
  workspace_path?: string;
}

export interface Requirement {
  id: number;
  project_id: number;
  title: string;
  body: string;
  status: string;
  source_path?: string | null;
  source_hash?: string | null;
  linked_plan_id?: number | null;
  plan_status?: string | null;
  plan_completed?: number | null;
  plan_total?: number | null;
  created_at: string;
  updated_at: string;
}

export interface Feedback {
  id: number;
  project_id: number;
  requirement_id?: number | null;
  title: string;
  body: string;
  status: string;
  linked_plan_id?: number | null;
  plan_status?: string | null;
  plan_completed?: number | null;
  plan_total?: number | null;
  created_at: string;
  updated_at: string;
}

export interface Attachment {
  id: number;
  project_id: number;
  owner_type: IntakeType;
  owner_id: number;
  original_name: string;
  stored_path: string;
  mime_type?: string | null;
  size: number;
  hash: string;
  created_at: string;
}

export interface PlanDraft {
  id: number;
  project_id: number;
  source_type: IntakeType;
  source_id: number;
  markdown: string;
  status: string;
  linked_plan_id?: number | null;
  created_at: string;
  updated_at: string;
}

export interface Plan {
  id: number;
  project_id: number;
  issue_hash: string;
  file_path: string;
  hash: string;
  status: string;
  total_tasks: number;
  completed_tasks: number;
  validation_passed: number;
  created_at: string;
  updated_at: string;
}

export interface ReadPlanResult {
  ok: boolean;
  id: number | null;
  project_id: number | null;
  file_path: string;
  markdown: string;
  hash: string;
  updated_at: string;
  error: string | null;
}

export interface PlanTask {
  id: number;
  plan_id: number;
  task_key: string;
  title: string;
  raw_line: string;
  scope: string;
  status: string;
  sort_order: number;
  started_at: string | null;
  finished_at: string | null;
  duration_ms: number;
  codex_session_id: string | null;
  updated_at: string;
  /** JOIN plans 得到 */
  file_path: string;
}

export const TASK_EVENT_TYPES = {
  STARTED: 'task.started',
  SUCCEEDED: 'task.succeeded',
  FAILED: 'task.failed',
  STOP_REQUESTED: 'task.stop.requested',
  STOPPED: 'task.stopped',
  INTERRUPTED: 'task.interrupted',
} as const;

export const LEGACY_TASK_EVENT_TYPES = {
  EXECUTED: 'task.executed',
  STOPPING: 'task.stopping',
} as const;

export const TASK_EVENT_STATUS = {
  PENDING: 'pending',
  RUNNING: 'running',
  COMPLETED: 'completed',
  FAILED: 'failed',
  STOPPING: 'stopping',
  STOPPED: 'stopped',
  INTERRUPTED: 'interrupted',
} as const;

export type TaskEventType = (typeof TASK_EVENT_TYPES)[keyof typeof TASK_EVENT_TYPES];
export type LegacyTaskEventType = (typeof LEGACY_TASK_EVENT_TYPES)[keyof typeof LEGACY_TASK_EVENT_TYPES];
export type TaskEventStatus = (typeof TASK_EVENT_STATUS)[keyof typeof TASK_EVENT_STATUS];

export interface TaskEventSemantics {
  status: TaskEventStatus;
  label: string;
}

export const TASK_EVENT_SEMANTICS: Record<TaskEventType, TaskEventSemantics> = {
  [TASK_EVENT_TYPES.STARTED]: { status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务' },
  [TASK_EVENT_TYPES.SUCCEEDED]: { status: TASK_EVENT_STATUS.COMPLETED, label: '结束了任务' },
  [TASK_EVENT_TYPES.FAILED]: { status: TASK_EVENT_STATUS.FAILED, label: '任务失败' },
  [TASK_EVENT_TYPES.STOP_REQUESTED]: { status: TASK_EVENT_STATUS.STOPPING, label: '请求停止任务' },
  [TASK_EVENT_TYPES.STOPPED]: { status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务' },
  [TASK_EVENT_TYPES.INTERRUPTED]: { status: TASK_EVENT_STATUS.INTERRUPTED, label: '已中断任务' },
};

export const TASK_EVENT_COMPATIBILITY: Record<LegacyTaskEventType, TaskEventType> = {
  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,
  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,
};

export interface TaskEventMeta {
  taskId?: number | null;
  taskKey?: string | null;
  taskTitle?: string | null;
  planId?: number | null;
  status?: TaskEventStatus | string | null;
  startedAt?: string | null;
  finishedAt?: string | null;
  exitCode?: number | null;
  log?: string | null;
  error?: string | null;
  [key: string]: unknown;
}

export type AppEventStructuredMeta = TaskEventMeta | Record<string, unknown>;
export type AppEventMeta = string | AppEventStructuredMeta;

export interface AppEvent<TMeta extends AppEventMeta | null = string | null> {
  id: number;
  project_id: number;
  type: string;
  message: string;
  meta?: TMeta;
  created_at: string;
}

export interface ScanFile {
  project_id: number;
  scan_type: string;
  file_path: string;
  hash: string;
  size: number;
  modified_at: string;
  scanned_at: string;
}

export interface AppSnapshot {
  activeProjectId: number | null;
  activeProject: Project | null;
  projects: Project[];
  state: ProjectState | null;
  requirements: Requirement[];
  feedback: Feedback[];
  attachments: Attachment[];
  planDrafts: PlanDraft[];
  plans: Plan[];
  tasks: PlanTask[];
  events: AppEvent[];
  scans: ScanFile[];
  activeOperation: ActiveOperation | null;
  activeOperations: ActiveOperation[];
  lastOperation: ActiveOperation | null;
}

export interface ActivityLine {
  role: string;
  text: string;
  at: string;
}

export interface ActiveOperation {
  label: string;
  projectId: number | null;
  planId: number | null;
  taskId: number | null;
  startedAt: string | null;
  finishedAt?: string | null;
  exitCode?: number | null;
  logTail: string;
  activity: ActivityLine[];
}

export interface PendingAttachment {
  path: string;
  name: string;
  size: number;
  type: string;
}

export interface CreateIntakeInput {
  projectId: number;
  body: string;
  attachments: PendingAttachment[];
  title?: string;
  status?: string;
  autoRun?: boolean;
  requirementId?: number | null;
}

export interface CreateProjectInput {
  name: string;
  workspacePath: string;
  description?: string;
}

export interface UpdateProjectInput extends CreateProjectInput {
  id: number;
}

export interface LoopConfigInput {
  projectId: number;
  workspacePath?: string;
  intervalSeconds?: number;
  validationCommand?: string;
}

export interface ProjectIdInput {
  projectId: number;
  manual?: boolean;
}

export interface RecordIdInput extends ProjectIdInput {
  id: number;
}

export interface TaskIdInput extends ProjectIdInput {
  taskId: number;
}

export interface ReadPlanInput extends ProjectIdInput {
  planId: number;
}

export interface IntakeActionInput extends ProjectIdInput {
  type: IntakeType;
  id: number;
  title?: string;
}

export interface UpdateRequirementInput extends RecordIdInput {
  title?: string;
  body?: string;
  status?: string;
  attachments?: PendingAttachment[];
}

export interface UpdateFeedbackInput extends UpdateRequirementInput {
  requirementId?: number | null;
}

export interface AutoplanApi {
  snapshot: (projectId?: number | null) => Promise<AppSnapshot>;
  createProject: (input: CreateProjectInput) => Promise<AppSnapshot>;
  updateProject: (input: UpdateProjectInput) => Promise<AppSnapshot>;
  deleteProject: (input: ProjectIdInput) => Promise<AppSnapshot>;
  configureLoop: (config: LoopConfigInput) => Promise<AppSnapshot>;
  startLoop: (input: ProjectIdInput) => Promise<AppSnapshot>;
  stopLoop: (input: ProjectIdInput) => Promise<AppSnapshot>;
  runOnce: (input: ProjectIdInput) => Promise<AppSnapshot>;
  readPlan: (input: ReadPlanInput) => Promise<ReadPlanResult>;
  runTask: (input: TaskIdInput) => Promise<AppSnapshot>;
  stopTask: (input: TaskIdInput) => Promise<AppSnapshot>;
  createRequirement: (input: CreateIntakeInput) => Promise<AppSnapshot>;
  updateRequirement: (input: UpdateRequirementInput) => Promise<AppSnapshot>;
  deleteRequirement: (input: RecordIdInput) => Promise<AppSnapshot>;
  createFeedback: (input: CreateIntakeInput) => Promise<AppSnapshot>;
  updateFeedback: (input: UpdateFeedbackInput) => Promise<AppSnapshot>;
  deleteFeedback: (input: RecordIdInput) => Promise<AppSnapshot>;
  updatePlanDraft: (input: { id: number; markdown: string }) => Promise<AppSnapshot>;
  interruptIntake: (input: IntakeActionInput) => Promise<AppSnapshot>;
  resumeIntake: (input: IntakeActionInput) => Promise<AppSnapshot>;
  appendIntakeTask: (input: IntakeActionInput) => Promise<AppSnapshot>;
  getDroppedFilePath: (file: File) => string;
  toFileUrl: (filePath: string) => string;
  onLoopUpdate: (handler: (snapshot: AppSnapshot) => void) => () => void;
}

declare global {
  interface Window {
    autoplan: AutoplanApi;
  }
}




codex
文件较长；我会只检索快照和耗时相关符号。
exec
powershell.exe -Command "rg -n \"snapshot|activeOperation|duration|elapsed|startedAt|finishedAt|started_at|finished_at|operation|TaskEventMeta|AppSnapshot|ActiveOperation\" src\\loopService.js src\\renderer\\types.ts" in D:\project\GitHub\autoplan
 succeeded in 211ms:
src\renderer\types.ts:103:  /** 由 snapshot 合并自 project.workspace_path */
src\renderer\types.ts:197:  started_at: string | null;
src\renderer\types.ts:198:  finished_at: string | null;
src\renderer\types.ts:199:  duration_ms: number;
src\renderer\types.ts:253:export interface TaskEventMeta {
src\renderer\types.ts:259:  startedAt?: string | null;
src\renderer\types.ts:260:  finishedAt?: string | null;
src\renderer\types.ts:267:export type AppEventStructuredMeta = TaskEventMeta | Record<string, unknown>;
src\renderer\types.ts:289:export interface AppSnapshot {
src\renderer\types.ts:302:  activeOperation: ActiveOperation | null;
src\renderer\types.ts:303:  activeOperations: ActiveOperation[];
src\renderer\types.ts:304:  lastOperation: ActiveOperation | null;
src\renderer\types.ts:313:export interface ActiveOperation {
src\renderer\types.ts:318:  startedAt: string | null;
src\renderer\types.ts:319:  finishedAt?: string | null;
src\renderer\types.ts:394:  snapshot: (projectId?: number | null) => Promise<AppSnapshot>;
src\renderer\types.ts:395:  createProject: (input: CreateProjectInput) => Promise<AppSnapshot>;
src\renderer\types.ts:396:  updateProject: (input: UpdateProjectInput) => Promise<AppSnapshot>;
src\renderer\types.ts:397:  deleteProject: (input: ProjectIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:398:  configureLoop: (config: LoopConfigInput) => Promise<AppSnapshot>;
src\renderer\types.ts:399:  startLoop: (input: ProjectIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:400:  stopLoop: (input: ProjectIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:401:  runOnce: (input: ProjectIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:403:  runTask: (input: TaskIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:404:  stopTask: (input: TaskIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:405:  createRequirement: (input: CreateIntakeInput) => Promise<AppSnapshot>;
src\renderer\types.ts:406:  updateRequirement: (input: UpdateRequirementInput) => Promise<AppSnapshot>;
src\renderer\types.ts:407:  deleteRequirement: (input: RecordIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:408:  createFeedback: (input: CreateIntakeInput) => Promise<AppSnapshot>;
src\renderer\types.ts:409:  updateFeedback: (input: UpdateFeedbackInput) => Promise<AppSnapshot>;
src\renderer\types.ts:410:  deleteFeedback: (input: RecordIdInput) => Promise<AppSnapshot>;
src\renderer\types.ts:411:  updatePlanDraft: (input: { id: number; markdown: string }) => Promise<AppSnapshot>;
src\renderer\types.ts:412:  interruptIntake: (input: IntakeActionInput) => Promise<AppSnapshot>;
src\renderer\types.ts:413:  resumeIntake: (input: IntakeActionInput) => Promise<AppSnapshot>;
src\renderer\types.ts:414:  appendIntakeTask: (input: IntakeActionInput) => Promise<AppSnapshot>;
src\renderer\types.ts:417:  onLoopUpdate: (handler: (snapshot: AppSnapshot) => void) => () => void;
src\loopService.js:258:    if (runtime?.activeOperations?.size) {
src\loopService.js:259:      for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {
src\loopService.js:260:        const child = runtime.activeChildren.get(operationKey);
src\loopService.js:261:        const activeTaskId = operation?.taskId || null;
src\loopService.js:262:        const finishedAt = nowIso();
src\loopService.js:266:          ? this.finishTaskRun(activeTaskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true })
src\loopService.js:270:          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
src\loopService.js:271:          operation?.label || '',
src\loopService.js:273:            ? taskEventMeta(stoppedTask || activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt })
src\loopService.js:392:    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));
src\loopService.js:395:      const finishedAt = nowIso();
src\loopService.js:398:        : this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
src\loopService.js:404:          taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt }),
src\loopService.js:408:      const finishedAt = nowIso();
src\loopService.js:409:      const stoppedTask = this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
src\loopService.js:414:        taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPING, finishedAt }),
src\loopService.js:432:  startTaskRun(taskId, startedAt = nowIso()) {
src\loopService.js:436:           started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,
src\loopService.js:437:           finished_at = NULL,
src\loopService.js:440:      [TASK_EVENT_STATUS.RUNNING, TASK_EVENT_STATUS.RUNNING, startedAt, startedAt, taskId],
src\loopService.js:445:  finishTaskRun(taskId, status, finishedAt = nowIso(), options = {}) {
src\loopService.js:451:    const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;
src\loopService.js:452:    const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);
src\loopService.js:455:       SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?
src\loopService.js:457:      [status, durationMs, finishedAt, finishedAt, taskId],
src\loopService.js:463:        duration_ms: durationMs,
src\loopService.js:464:        finished_at: finishedAt,
src\loopService.js:465:        updated_at: finishedAt,
src\loopService.js:478:      ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))
src\loopService.js:481:      const finishedAt = nowIso();
src\loopService.js:483:      const activeTaskId = activeEntry.operation?.taskId;
src\loopService.js:486:        ? this.finishTaskRun(activeTaskId, 'blocked', finishedAt, { onlyIfRunning: true })
src\loopService.js:496:          finishedAt,
src\loopService.js:813:          const finishedAt = nowIso();
src\loopService.js:814:          const failedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
src\loopService.js:822:              finishedAt,
src\loopService.js:872:    const finishedAt = nowIso();
src\loopService.js:873:    result.finishedAt = finishedAt;
src\loopService.js:876:      const failedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
src\loopService.js:885:          finishedAt,
src\loopService.js:896:    const finishedAt = result?.finishedAt || nowIso();
src\loopService.js:898:    const completedTask = this.finishTaskRun(task.id, TASK_EVENT_STATUS.COMPLETED, finishedAt) || task;
src\loopService.js:906:        finishedAt,
src\loopService.js:1077:  /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
src\loopService.js:1078:  archiveOperation(projectId, operationKey) {
src\loopService.js:1081:    const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOperation;
src\loopService.js:1091:        startedAt: op.startedAt || null,
src\loopService.js:1092:        finishedAt: nowIso(),
src\loopService.js:1098:    if (operationKey) {
src\loopService.js:1099:      runtime.activeChildren.delete(operationKey);
src\loopService.js:1100:      runtime.activeOperations.delete(operationKey);
src\loopService.js:1103:      runtime.activeOperations.clear();
src\loopService.js:1108:  async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
src\loopService.js:1110:    const result = await this.runCodex(workspace, prompt, label, operation);
src\loopService.js:1115:        this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
src\loopService.js:1116:          planId: operation.planId || null,
src\loopService.js:1117:          taskId: operation.taskId || null,
src\loopService.js:1124:  async runCodex(workspace, prompt, label, operation = {}) {
src\loopService.js:1125:    const projectIdForEmit = operation.projectId;
src\loopService.js:1127:    if (!runtime) throw new Error('projectId is required for codex operations');
src\loopService.js:1146:    const activeOperation = {
src\loopService.js:1147:      ...operation,
src\loopService.js:1151:      startedAt: nowIso(),
src\loopService.js:1153:    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
src\loopService.js:1160:      if (!runtime.activeOperations.has(operationKey)) return;
src\loopService.js:1161:      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
src\loopService.js:1162:      if (activeOperation.logBuffer.length > 24000) {
src\loopService.js:1163:        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
src\loopService.js:1166:      if (activeOperation.activity) activeOperation.activity.offer(text);
src\loopService.js:1177:      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
src\loopService.js:1182:      if (runtime.activeOperations.has(operationKey)) {
src\loopService.js:1183:        this.archiveOperation(projectIdForEmit, operationKey);
src\loopService.js:1188:  async runShell(workspace, command, label, operation = {}) {
src\loopService.js:1189:    const projectIdForEmit = operation.projectId;
src\loopService.js:1191:    if (!runtime) throw new Error('projectId is required for shell operations');
src\loopService.js:1201:    const activeOperation = {
src\loopService.js:1202:      ...operation,
src\loopService.js:1206:      startedAt: nowIso(),
src\loopService.js:1208:    const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
src\loopService.js:1213:      if (!runtime.activeOperations.has(operationKey)) return;
src\loopService.js:1214:      activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
src\loopService.js:1215:      if (activeOperation.logBuffer.length > 24000) {
src\loopService.js:1216:        activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
src\loopService.js:1218:      if (activeOperation.activity) activeOperation.activity.offer(text);
src\loopService.js:1228:      if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
src\loopService.js:1232:      if (runtime.activeOperations.has(operationKey)) {
src\loopService.js:1233:        this.archiveOperation(projectIdForEmit, operationKey);
src\loopService.js:1269:    this.emit('update', this.snapshot(projectId));
src\loopService.js:1272:  snapshot(projectId = null) {
src\loopService.js:1328:      activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) === Number(projectId)
src\loopService.js:1330:            label: runtime.activeOperation.label || '',
src\loopService.js:1331:            projectId: runtime.activeOperation.projectId || null,
src\loopService.js:1332:            planId: runtime.activeOperation.planId || null,
src\loopService.js:1333:            taskId: runtime.activeOperation.taskId || null,
src\loopService.js:1334:            startedAt: runtime.activeOperation.startedAt || null,
src\loopService.js:1335:            logTail: (runtime.activeOperation.logBuffer || '').slice(-8000),
src\loopService.js:1336:            activity: runtime.activeOperation.activity ? runtime.activeOperation.activity.getLines() : [],
src\loopService.js:1339:      activeOperations: runtime?.activeOperations
src\loopService.js:1340:        ? Array.from(runtime.activeOperations.values())
src\loopService.js:1341:            .filter((operation) => Number(operation.projectId) === Number(projectId))
src\loopService.js:1342:            .map((operation) => ({
src\loopService.js:1343:              label: operation.label || '',
src\loopService.js:1344:              projectId: operation.projectId || null,
src\loopService.js:1345:              planId: operation.planId || null,
src\loopService.js:1346:              taskId: operation.taskId || null,
src\loopService.js:1347:              startedAt: operation.startedAt || null,
src\loopService.js:1348:              logTail: (operation.logBuffer || '').slice(-8000),
src\loopService.js:1349:              activity: operation.activity ? operation.activity.getLines() : [],
src\loopService.js:1368:      startedAt: task?.started_at,
src\loopService.js:1369:      finishedAt: task?.finished_at,
src\loopService.js:1370:      durationMs: task?.duration_ms,
src\loopService.js:1371:      runDurationMs: task?.run_duration_ms,
src\loopService.js:1380:  meta.startedAt = normalizeOptionalString(meta.startedAt);
src\loopService.js:1381:  meta.finishedAt = normalizeOptionalString(meta.finishedAt);
src\loopService.js:1382:  meta.durationMs = normalizeOptionalNumber(meta.durationMs);
src\loopService.js:1421:    duration_ms: normalizeDurationMs(task.duration_ms),
src\loopService.js:1422:    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
src\loopService.js:1432:function taskRunDurationMs(startedAt, finishedAt) {
src\loopService.js:1433:  const started = Date.parse(startedAt || '');
src\loopService.js:1434:  const finished = Date.parse(finishedAt || '');
src\loopService.js:1445:    activeOperation: null,
src\loopService.js:1447:    activeOperations: new Map(),
src\loopService.js:1466:    activeOperation: null,
src\loopService.js:1467:    activeOperations: [],
src\loopService.js:1472:function registerRuntimeOperation(runtime, child, operation) {
src\loopService.js:1474:  if (!runtime.activeOperations) runtime.activeOperations = new Map();
src\loopService.js:1475:  const operationKey = `${Date.now()}-${Math.random().toString(36).slice(2)}`;
src\loopService.js:1476:  runtime.activeChildren.set(operationKey, child);
src\loopService.js:1477:  runtime.activeOperations.set(operationKey, operation);
src\loopService.js:1479:  runtime.activeOperation = operation;
src\loopService.js:1480:  return operationKey;
src\loopService.js:1484:  const entries = Array.from(runtime.activeOperations?.entries?.() || []);
src\loopService.js:1488:    runtime.activeOperation = null;
src\loopService.js:1492:  runtime.activeOperation = latest[1] || null;
src\loopService.js:1500:  if (!runtime?.activeOperations) return [];
src\loopService.js:1502:  for (const [operationKey, operation] of runtime.activeOperations.entries()) {
src\loopService.js:1503:    if (predicate(operation)) {
src\loopService.js:1505:        operationKey,
src\loopService.js:1506:        operation,
src\loopService.js:1507:        child: runtime.activeChildren?.get(operationKey) || null,

**Getting file sections with PowerShell**

I need to work with relevant sections of a file using PowerShell. I'm considering using Get-Content with indices, and I'm thinking that maybe I don't need to resort to Python for larger chunks. Shell commands could work fine, especially with Select-Object. I should focus on specific line ranges, particularly lines 160-230 and a loop for lines 1120-1355 and 1360-1440, while avoiding loading the entire file.
codex
找到已有任务耗时字段；继续查看快照构造附近的代码。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; foreach($range in @(@(420,470),@(1180,1245),@(1260,1355),@(1360,1445),@(1468,1495))) { Write-Host "--- $($range[0])-$($range[1]) ---"; for($i=$range[0]; $i -le $range[1] -and $i -le $lines.Length; $i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] } } ; Write-Host '"'--- types 180-225 ---'; "'$t=Get-Content -Path src'"\\renderer\\types.ts -Encoding UTF8; for("'$i=180;$i -le 225 -and $i -le $t.Length;$i++){ '"'{0}: {1}' -f "'$i,$t[$i-1] }; Write-Host '"'--- types 305-325 ---'; for("'$i=305;$i -le 325 -and $i -le $t.Length;$i++){ '"'{0}: {1}' -f "'$i,$t[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 289ms:
--- 420-470 ---
420:   }
421: 
422:   taskForProject(projectId, taskId) {
423:     if (!taskId) return null;
424:     return this.db.get(
425:       `SELECT plan_tasks.*
426:        FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
427:        WHERE plan_tasks.id = ? AND plans.project_id = ?`,
428:       [taskId, projectId],
429:     );
430:   }
431: 
432:   startTaskRun(taskId, startedAt = nowIso()) {
433:     this.db.run(
434:       `UPDATE plan_tasks
435:        SET status = ?,
436:            started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,
437:            finished_at = NULL,
438:            updated_at = ?
439:        WHERE id = ?`,
440:       [TASK_EVENT_STATUS.RUNNING, TASK_EVENT_STATUS.RUNNING, startedAt, startedAt, taskId],
441:     );
442:     return this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
443:   }
444: 
445:   finishTaskRun(taskId, status, finishedAt = nowIso(), options = {}) {
446:     const task = this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
447:     if (!task) return null;
448:     const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
449:     if (options.onlyIfRunning && !isRunning) return withTaskDurationMeta(task);
450: 
451:     const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;
452:     const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);
453:     this.db.run(
454:       `UPDATE plan_tasks
455:        SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?
456:        WHERE id = ?`,
457:       [status, durationMs, finishedAt, finishedAt, taskId],
458:     );
459:     return withTaskDurationMeta(
460:       {
461:         ...task,
462:         status,
463:         duration_ms: durationMs,
464:         finished_at: finishedAt,
465:         updated_at: finishedAt,
466:       },
467:       runDurationMs,
468:     );
469:   }
470: 
--- 1180-1245 ---
1180:       clearInterval(tailTimer);
1181:       stream.end();
1182:       if (runtime.activeOperations.has(operationKey)) {
1183:         this.archiveOperation(projectIdForEmit, operationKey);
1184:       }
1185:     }
1186:   }
1187: 
1188:   async runShell(workspace, command, label, operation = {}) {
1189:     const projectIdForEmit = operation.projectId;
1190:     const runtime = this.runtime(projectIdForEmit);
1191:     if (!runtime) throw new Error('projectId is required for shell operations');
1192:     const logDir = path.join(workspace, 'docs', 'progress', 'logs');
1193:     fs.mkdirSync(logDir, { recursive: true });
1194:     const logFile = path.join(logDir, `${timestampForPath()}_${safePart(label)}.log`);
1195:     const shellCommand = process.platform === 'win32' ? `chcp 65001>nul && ${command}` : command;
1196:     const child = spawn(shellCommand, {
1197:       shell: true,
1198:       cwd: workspace,
1199:       env: { ...process.env },
1200:     });
1201:     const activeOperation = {
1202:       ...operation,
1203:       label,
1204:       logBuffer: '',
1205:       activity: new CodexActivityPrinter(200),
1206:       startedAt: nowIso(),
1207:     };
1208:     const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
1209:     let output = '';
1210:     const onChunk = (chunk) => {
1211:       const text = chunk.toString('utf8');
1212:       output += text;
1213:       if (!runtime.activeOperations.has(operationKey)) return;
1214:       activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
1215:       if (activeOperation.logBuffer.length > 24000) {
1216:         activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
1217:       }
1218:       if (activeOperation.activity) activeOperation.activity.offer(text);
1219:     };
1220:     child.stdout.on('data', onChunk);
1221:     child.stderr.on('data', onChunk);
1222:     const tailTimer = setInterval(() => {
1223:       if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
1224:     }, 600);
1225:     try {
1226:       const exitCode = await waitForChild(child, 10 * 60 * 1000);
1227:       fs.writeFileSync(logFile, output, 'utf8');
1228:       if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
1229:       return { exitCode, output, logFile };
1230:     } finally {
1231:       clearInterval(tailTimer);
1232:       if (runtime.activeOperations.has(operationKey)) {
1233:         this.archiveOperation(projectIdForEmit, operationKey);
1234:       }
1235:     }
1236:   }
1237: 
1238:   setPhase(projectId, phase) {
1239:     this.db.run('UPDATE project_states SET phase = ?, updated_at = ? WHERE project_id = ?', [
1240:       phase,
1241:       nowIso(),
1242:       projectId,
1243:     ]);
1244:     this.emitUpdate(projectId);
1245:   }
--- 1260-1355 ---
1260:       type,
1261:       message,
1262:       meta ? JSON.stringify(meta) : null,
1263:       nowIso(),
1264:     ]);
1265:     this.emitUpdate(projectId);
1266:   }
1267: 
1268:   emitUpdate(projectId) {
1269:     this.emit('update', this.snapshot(projectId));
1270:   }
1271: 
1272:   snapshot(projectId = null) {
1273:     const projects = this.projects();
1274:     if (!projectId) return emptySnapshot(projects);
1275: 
1276:     const activeProject = this.project(projectId);
1277:     if (!activeProject) return emptySnapshot(projects);
1278: 
1279:     const state = {
1280:       ...(this.status(projectId) || {}),
1281:       workspace_path: activeProject.workspace_path || '',
1282:     };
1283:     const runtime = this.existingRuntime(projectId);
1284: 
1285:     return {
1286:       activeProjectId: projectId,
1287:       activeProject,
1288:       projects,
1289:       state,
1290:       requirements: this.db.all(
1291:         `SELECT requirements.*, plans.status AS plan_status,
1292:                 plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
1293:          FROM requirements
1294:          LEFT JOIN plans ON plans.id = requirements.linked_plan_id
1295:          WHERE requirements.project_id = ?
1296:          ORDER BY requirements.updated_at DESC`,
1297:         [projectId],
1298:       ),
1299:       feedback: this.db.all(
1300:         `SELECT feedback.*, plans.status AS plan_status,
1301:                 plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
1302:          FROM feedback
1303:          LEFT JOIN plans ON plans.id = feedback.linked_plan_id
1304:          WHERE feedback.project_id = ?
1305:          ORDER BY feedback.updated_at DESC`,
1306:         [projectId],
1307:       ),
1308:       attachments: this.db.all(
1309:         'SELECT * FROM attachments WHERE project_id = ? ORDER BY created_at DESC, id DESC',
1310:         [projectId],
1311:       ),
1312:       planDrafts: this.db.all('SELECT * FROM plan_drafts WHERE project_id = ? ORDER BY updated_at DESC, id DESC', [
1313:         projectId,
1314:       ]),
1315:       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
1316:       tasks: this.db.all(
1317:         `SELECT plan_tasks.*, plans.file_path
1318:          FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
1319:          WHERE plans.project_id = ?
1320:          ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
1321:         [projectId],
1322:       ),
1323:       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
1324:       scans: this.db.all(
1325:         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
1326:         [projectId],
1327:       ),
1328:       activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) === Number(projectId)
1329:         ? {
1330:             label: runtime.activeOperation.label || '',
1331:             projectId: runtime.activeOperation.projectId || null,
1332:             planId: runtime.activeOperation.planId || null,
1333:             taskId: runtime.activeOperation.taskId || null,
1334:             startedAt: runtime.activeOperation.startedAt || null,
1335:             logTail: (runtime.activeOperation.logBuffer || '').slice(-8000),
1336:             activity: runtime.activeOperation.activity ? runtime.activeOperation.activity.getLines() : [],
1337:           }
1338:         : null,
1339:       activeOperations: runtime?.activeOperations
1340:         ? Array.from(runtime.activeOperations.values())
1341:             .filter((operation) => Number(operation.projectId) === Number(projectId))
1342:             .map((operation) => ({
1343:               label: operation.label || '',
1344:               projectId: operation.projectId || null,
1345:               planId: operation.planId || null,
1346:               taskId: operation.taskId || null,
1347:               startedAt: operation.startedAt || null,
1348:               logTail: (operation.logBuffer || '').slice(-8000),
1349:               activity: operation.activity ? operation.activity.getLines() : [],
1350:             }))
1351:         : [],
1352:       lastOperation:
1353:         runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
1354:           ? runtime.lastOperation
1355:           : null,
--- 1360-1445 ---
1360: function taskEventMeta(task, overrides = {}) {
1361:   const meta = {
1362:     ...compactEventMeta({
1363:       taskId: task?.id,
1364:       taskKey: task?.task_key,
1365:       taskTitle: task?.title,
1366:       planId: task?.plan_id,
1367:       status: task?.status,
1368:       startedAt: task?.started_at,
1369:       finishedAt: task?.finished_at,
1370:       durationMs: task?.duration_ms,
1371:       runDurationMs: task?.run_duration_ms,
1372:     }),
1373:     ...compactEventMeta(overrides),
1374:   };
1375:   meta.taskId = normalizeOptionalNumber(meta.taskId);
1376:   meta.planId = normalizeOptionalNumber(meta.planId);
1377:   meta.taskKey = normalizeOptionalString(meta.taskKey);
1378:   meta.taskTitle = normalizeOptionalString(meta.taskTitle);
1379:   meta.status = normalizeOptionalString(meta.status);
1380:   meta.startedAt = normalizeOptionalString(meta.startedAt);
1381:   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
1382:   meta.durationMs = normalizeOptionalNumber(meta.durationMs);
1383:   meta.runDurationMs = normalizeOptionalNumber(meta.runDurationMs);
1384:   const compacted = compactEventMeta(meta);
1385:   return Object.keys(compacted).length ? compacted : null;
1386: }
1387: 
1388: function syncedTaskStatus(parsedStatus, existingStatus) {
1389:   const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
1390:   const current = normalizeOptionalString(existingStatus);
1391:   if (!current) return next;
1392:   if (next === TASK_EVENT_STATUS.COMPLETED || current === TASK_EVENT_STATUS.COMPLETED) return TASK_EVENT_STATUS.COMPLETED;
1393:   if (current === TASK_EVENT_STATUS.RUNNING || current === 'blocked') return current;
1394:   return next;
1395: }
1396: 
1397: function compactEventMeta(meta) {
1398:   const result = {};
1399:   for (const [key, value] of Object.entries(meta || {})) {
1400:     if (value !== undefined && value !== null && value !== '') result[key] = value;
1401:   }
1402:   return result;
1403: }
1404: 
1405: function normalizeOptionalNumber(value) {
1406:   if (value === undefined || value === null || value === '') return undefined;
1407:   const number = Number(value);
1408:   return Number.isFinite(number) ? number : undefined;
1409: }
1410: 
1411: function normalizeOptionalString(value) {
1412:   if (value === undefined || value === null) return undefined;
1413:   const text = String(value).trim();
1414:   return text || undefined;
1415: }
1416: 
1417: function withTaskDurationMeta(task, runDurationMs) {
1418:   if (!task) return null;
1419:   return {
1420:     ...task,
1421:     duration_ms: normalizeDurationMs(task.duration_ms),
1422:     ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
1423:   };
1424: }
1425: 
1426: function normalizeDurationMs(value) {
1427:   const number = Number(value);
1428:   if (!Number.isFinite(number) || number <= 0) return 0;
1429:   return Math.round(number);
1430: }
1431: 
1432: function taskRunDurationMs(startedAt, finishedAt) {
1433:   const started = Date.parse(startedAt || '');
1434:   const finished = Date.parse(finishedAt || '');
1435:   if (!Number.isFinite(started) || !Number.isFinite(finished) || finished <= started) return 0;
1436:   return Math.round(finished - started);
1437: }
1438: 
1439: function createProjectRuntime() {
1440:   return {
1441:     timer: null,
1442:     running: false,
1443:     busy: false,
1444:     activeChild: null,
1445:     activeOperation: null,
--- 1468-1495 ---
1468:     lastOperation: null,
1469:   };
1470: }
1471: 
1472: function registerRuntimeOperation(runtime, child, operation) {
1473:   if (!runtime.activeChildren) runtime.activeChildren = new Map();
1474:   if (!runtime.activeOperations) runtime.activeOperations = new Map();
1475:   const operationKey = `${Date.now()}-${Math.random().toString(36).slice(2)}`;
1476:   runtime.activeChildren.set(operationKey, child);
1477:   runtime.activeOperations.set(operationKey, operation);
1478:   runtime.activeChild = child;
1479:   runtime.activeOperation = operation;
1480:   return operationKey;
1481: }
1482: 
1483: function refreshRuntimeActive(runtime) {
1484:   const entries = Array.from(runtime.activeOperations?.entries?.() || []);
1485:   const latest = entries.at(-1);
1486:   if (!latest) {
1487:     runtime.activeChild = null;
1488:     runtime.activeOperation = null;
1489:     return;
1490:   }
1491:   runtime.activeChild = runtime.activeChildren.get(latest[0]) || null;
1492:   runtime.activeOperation = latest[1] || null;
1493: }
1494: 
1495: function findRuntimeOperation(runtime, predicate) {
--- types 180-225 ---
180:   project_id: number | null;
181:   file_path: string;
182:   markdown: string;
183:   hash: string;
184:   updated_at: string;
185:   error: string | null;
186: }
187: 
188: export interface PlanTask {
189:   id: number;
190:   plan_id: number;
191:   task_key: string;
192:   title: string;
193:   raw_line: string;
194:   scope: string;
195:   status: string;
196:   sort_order: number;
197:   started_at: string | null;
198:   finished_at: string | null;
199:   duration_ms: number;
200:   codex_session_id: string | null;
201:   updated_at: string;
202:   /** JOIN plans 得到 */
203:   file_path: string;
204: }
205: 
206: export const TASK_EVENT_TYPES = {
207:   STARTED: 'task.started',
208:   SUCCEEDED: 'task.succeeded',
209:   FAILED: 'task.failed',
210:   STOP_REQUESTED: 'task.stop.requested',
211:   STOPPED: 'task.stopped',
212:   INTERRUPTED: 'task.interrupted',
213: } as const;
214: 
215: export const LEGACY_TASK_EVENT_TYPES = {
216:   EXECUTED: 'task.executed',
217:   STOPPING: 'task.stopping',
218: } as const;
219: 
220: export const TASK_EVENT_STATUS = {
221:   PENDING: 'pending',
222:   RUNNING: 'running',
223:   COMPLETED: 'completed',
224:   FAILED: 'failed',
225:   STOPPING: 'stopping',
--- types 305-325 ---
305: }
306: 
307: export interface ActivityLine {
308:   role: string;
309:   text: string;
310:   at: string;
311: }
312: 
313: export interface ActiveOperation {
314:   label: string;
315:   projectId: number | null;
316:   planId: number | null;
317:   taskId: number | null;
318:   startedAt: string | null;
319:   finishedAt?: string | null;
320:   exitCode?: number | null;
321:   logTail: string;
322:   activity: ActivityLine[];
323: }
324: 
325: export interface PendingAttachment {

**Inspecting snapshot functions**

I need to get a separate snapshot of a section since my output was truncated. I’m realizing that we already have the PlanTask duration in milliseconds, but need to also expose duration info in the snapshot data. I wonder if the active operation could show elapsed time or just the total duration. Currently, the active operation has only the start time, while the last operation tracks the finish time. I should inspect the snapshot functions fully to clarify these details.
codex
快照里活动操作未含耗时；我会精确查看该段生成逻辑。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; for($i=1248;$i -le 1358 -and $i -le $lines.Length;$i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 257ms:
1248:     const message = error?.stack || error?.message || String(error);
1249:     this.db.run(
1250:       'UPDATE project_states SET phase = ?, last_error = ?, updated_at = ? WHERE project_id = ?',
1251:       ['error', message, nowIso(), projectId],
1252:     );
1253:     this.addEvent(projectId, 'loop.error', message);
1254:     this.emitUpdate(projectId);
1255:   }
1256: 
1257:   addEvent(projectId, type, message, meta = null) {
1258:     this.db.run('INSERT INTO events (project_id, type, message, meta, created_at) VALUES (?, ?, ?, ?, ?)', [
1259:       projectId,
1260:       type,
1261:       message,
1262:       meta ? JSON.stringify(meta) : null,
1263:       nowIso(),
1264:     ]);
1265:     this.emitUpdate(projectId);
1266:   }
1267: 
1268:   emitUpdate(projectId) {
1269:     this.emit('update', this.snapshot(projectId));
1270:   }
1271: 
1272:   snapshot(projectId = null) {
1273:     const projects = this.projects();
1274:     if (!projectId) return emptySnapshot(projects);
1275: 
1276:     const activeProject = this.project(projectId);
1277:     if (!activeProject) return emptySnapshot(projects);
1278: 
1279:     const state = {
1280:       ...(this.status(projectId) || {}),
1281:       workspace_path: activeProject.workspace_path || '',
1282:     };
1283:     const runtime = this.existingRuntime(projectId);
1284: 
1285:     return {
1286:       activeProjectId: projectId,
1287:       activeProject,
1288:       projects,
1289:       state,
1290:       requirements: this.db.all(
1291:         `SELECT requirements.*, plans.status AS plan_status,
1292:                 plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
1293:          FROM requirements
1294:          LEFT JOIN plans ON plans.id = requirements.linked_plan_id
1295:          WHERE requirements.project_id = ?
1296:          ORDER BY requirements.updated_at DESC`,
1297:         [projectId],
1298:       ),
1299:       feedback: this.db.all(
1300:         `SELECT feedback.*, plans.status AS plan_status,
1301:                 plans.completed_tasks AS plan_completed, plans.total_tasks AS plan_total
1302:          FROM feedback
1303:          LEFT JOIN plans ON plans.id = feedback.linked_plan_id
1304:          WHERE feedback.project_id = ?
1305:          ORDER BY feedback.updated_at DESC`,
1306:         [projectId],
1307:       ),
1308:       attachments: this.db.all(
1309:         'SELECT * FROM attachments WHERE project_id = ? ORDER BY created_at DESC, id DESC',
1310:         [projectId],
1311:       ),
1312:       planDrafts: this.db.all('SELECT * FROM plan_drafts WHERE project_id = ? ORDER BY updated_at DESC, id DESC', [
1313:         projectId,
1314:       ]),
1315:       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
1316:       tasks: this.db.all(
1317:         `SELECT plan_tasks.*, plans.file_path
1318:          FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
1319:          WHERE plans.project_id = ?
1320:          ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
1321:         [projectId],
1322:       ),
1323:       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
1324:       scans: this.db.all(
1325:         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
1326:         [projectId],
1327:       ),
1328:       activeOperation: runtime?.activeOperation && Number(runtime.activeOperation.projectId) === Number(projectId)
1329:         ? {
1330:             label: runtime.activeOperation.label || '',
1331:             projectId: runtime.activeOperation.projectId || null,
1332:             planId: runtime.activeOperation.planId || null,
1333:             taskId: runtime.activeOperation.taskId || null,
1334:             startedAt: runtime.activeOperation.startedAt || null,
1335:             logTail: (runtime.activeOperation.logBuffer || '').slice(-8000),
1336:             activity: runtime.activeOperation.activity ? runtime.activeOperation.activity.getLines() : [],
1337:           }
1338:         : null,
1339:       activeOperations: runtime?.activeOperations
1340:         ? Array.from(runtime.activeOperations.values())
1341:             .filter((operation) => Number(operation.projectId) === Number(projectId))
1342:             .map((operation) => ({
1343:               label: operation.label || '',
1344:               projectId: operation.projectId || null,
1345:               planId: operation.planId || null,
1346:               taskId: operation.taskId || null,
1347:               startedAt: operation.startedAt || null,
1348:               logTail: (operation.logBuffer || '').slice(-8000),
1349:               activity: operation.activity ? operation.activity.getLines() : [],
1350:             }))
1351:         : [],
1352:       lastOperation:
1353:         runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
1354:           ? runtime.lastOperation
1355:           : null,
1356:     };
1357:   }
1358: }

codex
确认快照映射点后，我会查看归档操作以保持字段一致。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; for($i=1100;$i -le 1188 -and $i -le $lines.Length;$i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 245ms:
1100:       runtime.activeOperations.delete(operationKey);
1101:     } else {
1102:       runtime.activeChildren.clear();
1103:       runtime.activeOperations.clear();
1104:     }
1105:     refreshRuntimeActive(runtime);
1106:   }
1107: 
1108:   async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {
1109:     const before = planFile && fs.existsSync(planFile) ? fs.readFileSync(planFile, 'utf8') : null;
1110:     const result = await this.runCodex(workspace, prompt, label, operation);
1111:     if (before !== null) {
1112:       const changed = !fs.existsSync(planFile) || fs.readFileSync(planFile, 'utf8') !== before;
1113:       if (changed) {
1114:         fs.writeFileSync(planFile, before, 'utf8');
1115:         this.addEvent(operation.projectId, 'plan.guard.restored', `Codex 修改了 plan，已恢复：${normalizeRelative(workspace, planFile)}`, {
1116:           planId: operation.planId || null,
1117:           taskId: operation.taskId || null,
1118:         });
1119:       }
1120:     }
1121:     return result;
1122:   }
1123: 
1124:   async runCodex(workspace, prompt, label, operation = {}) {
1125:     const projectIdForEmit = operation.projectId;
1126:     const runtime = this.runtime(projectIdForEmit);
1127:     if (!runtime) throw new Error('projectId is required for codex operations');
1128:     const logDir = path.join(workspace, 'docs', 'progress', 'logs');
1129:     fs.mkdirSync(logDir, { recursive: true });
1130:     const prefix = `${timestampForPath()}_${safePart(label)}`;
1131:     const logFile = path.join(logDir, `${prefix}.log`);
1132:     const lastFile = path.join(logDir, `${prefix}.last.txt`);
1133:     const args = [
1134:       'exec',
1135:       '--cd',
1136:       workspace,
1137:       '--color',
1138:       'never',
1139:       '-o',
1140:       lastFile,
1141:       '--sandbox',
1142:       'danger-full-access',
1143:       '-',
1144:     ];
1145:     const child = spawn('codex', args, { shell: process.platform === 'win32', cwd: workspace });
1146:     const activeOperation = {
1147:       ...operation,
1148:       label,
1149:       logBuffer: '',
1150:       activity: new CodexActivityPrinter(200),
1151:       startedAt: nowIso(),
1152:     };
1153:     const operationKey = registerRuntimeOperation(runtime, child, activeOperation);
1154:     child.stdin.setDefaultEncoding('utf8');
1155:     child.stdin.end(prompt);
1156:     const stream = fs.createWriteStream(logFile, { encoding: 'utf8' });
1157:     // 累积原始日志 + 提取可读活动行
1158:     const onChunk = (chunk) => {
1159:       const text = chunk.toString('utf8');
1160:       if (!runtime.activeOperations.has(operationKey)) return;
1161:       activeOperation.logBuffer = (activeOperation.logBuffer || '') + text;
1162:       if (activeOperation.logBuffer.length > 24000) {
1163:         activeOperation.logBuffer = activeOperation.logBuffer.slice(-16000);
1164:       }
1165:       // 喂给活动打印机，提取人类可读的进度行
1166:       if (activeOperation.activity) activeOperation.activity.offer(text);
1167:     };
1168:     child.stdout.on('data', onChunk);
1169:     child.stderr.on('data', onChunk);
1170:     child.stdout.pipe(stream, { end: false });
1171:     child.stderr.pipe(stream, { end: false });
1172:     const tailTimer = setInterval(() => {
1173:       if (projectIdForEmit) this.emitUpdate(projectIdForEmit);
1174:     }, 600);
1175:     try {
1176:       const exitCode = await waitForChild(child, 45 * 60 * 1000);
1177:       if (runtime.activeOperations.has(operationKey)) activeOperation.exitCode = exitCode;
1178:       return { exitCode, logFile, lastFile };
1179:     } finally {
1180:       clearInterval(tailTimer);
1181:       stream.end();
1182:       if (runtime.activeOperations.has(operationKey)) {
1183:         this.archiveOperation(projectIdForEmit, operationKey);
1184:       }
1185:     }
1186:   }
1187: 
1188:   async runShell(workspace, command, label, operation = {}) {

codex
还需要归档函数本体，确保完成后的快照也有耗时。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; for($i=1030;$i -le 1108 -and $i -le $lines.Length;$i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 277ms:
1030:       matches.push(existing);
1031:       existingByKey.set(existing.task_key, matches);
1032:     }
1033: 
1034:     const syncedStatuses = [];
1035:     for (const task of tasks) {
1036:       const existing = existingByKey.get(task.key)?.shift();
1037:       const status = existing ? syncedTaskStatus(task.status, existing.status) : task.status;
1038:       syncedStatuses.push(status);
1039:       if (existing) {
1040:         this.db.run(
1041:           `UPDATE plan_tasks
1042:            SET title = ?, raw_line = ?, scope = ?, status = ?, sort_order = ?, updated_at = ?
1043:            WHERE id = ?`,
1044:           [
1045:             task.title,
1046:             task.rawLine,
1047:             task.scope,
1048:             status,
1049:             task.sortOrder,
1050:             nowIso(),
1051:             existing.id,
1052:           ],
1053:         );
1054:       } else {
1055:         this.db.run(
1056:           `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
1057:            VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
1058:           [planId, task.key, task.title, task.rawLine, task.scope, status, task.sortOrder, nowIso()],
1059:         );
1060:       }
1061:     }
1062: 
1063:     for (const matches of existingByKey.values()) {
1064:       for (const stale of matches) {
1065:         this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
1066:       }
1067:     }
1068: 
1069:     const completed = syncedStatuses.filter((status) => status === 'completed').length;
1070:     const status = tasks.length > 0 && completed === tasks.length ? 'ready_for_validation' : 'running';
1071:     this.db.run(
1072:       'UPDATE plans SET hash = ?, status = ?, total_tasks = ?, completed_tasks = ?, updated_at = ? WHERE id = ?',
1073:       [hashFile(planFile), status, tasks.length, completed, nowIso(), planId],
1074:     );
1075:   }
1076: 
1077:   /** 把当前 activeOperation 转存为 lastOperation（保留日志），然后清空 active */
1078:   archiveOperation(projectId, operationKey) {
1079:     const runtime = this.runtime(projectId);
1080:     if (!runtime) return;
1081:     const op = operationKey ? runtime.activeOperations.get(operationKey) : runtime.activeOperation;
1082:     if (op) {
1083:       if (op.activity && typeof op.activity.flush === 'function') {
1084:         op.activity.flush();
1085:       }
1086:       runtime.lastOperation = {
1087:         label: op.label || '',
1088:         projectId: op.projectId || null,
1089:         planId: op.planId || null,
1090:         taskId: op.taskId || null,
1091:         startedAt: op.startedAt || null,
1092:         finishedAt: nowIso(),
1093:         exitCode: typeof op.exitCode === 'number' ? op.exitCode : null,
1094:         logTail: (op.logBuffer || '').slice(-8000),
1095:         activity: op.activity ? op.activity.getLines() : [],
1096:       };
1097:     }
1098:     if (operationKey) {
1099:       runtime.activeChildren.delete(operationKey);
1100:       runtime.activeOperations.delete(operationKey);
1101:     } else {
1102:       runtime.activeChildren.clear();
1103:       runtime.activeOperations.clear();
1104:     }
1105:     refreshRuntimeActive(runtime);
1106:   }
1107: 
1108:   async runCodexWithPlanGuard(workspace, prompt, label, operation, planFile) {

codex
任务字段已有数据库耗时；我会检查前端是否读取操作耗时命名。
exec
powershell.exe -Command "rg -n \"duration_ms|durationMs|elapsed|ActiveOperation|activeOperation|lastOperation|activeOperations|startedAt|finishedAt|formatDuration\" src\\renderer src\\*.js" in D:\project\GitHub\autoplan
2026-06-24T20:19:03.383557Z ERROR codex_core::tools::router: error={"output":"src\\renderer\\types.ts:199:  duration_ms: number;\nsrc\\renderer\\types.ts:259:  startedAt?: string | null;\r\nsrc\\renderer\\types.ts:260:  finishedAt?: string | null;\r\nsrc\\renderer\\types.ts:302:  activeOperation: ActiveOperation | null;\r\nsrc\\renderer\\types.ts:303:  activeOperations: ActiveOperation[];\r\nsrc\\renderer\\types.ts:304:  lastOperation: ActiveOperation | null;\r\nsrc\\renderer\\types.ts:313:export interface ActiveOperation {\r\nsrc\\renderer\\types.ts:318:  startedAt: string | null;\r\nsrc\\renderer\\types.ts:319:  finishedAt?: string | null;\r\nsrc\\renderer\\pages\\WorkspacePage.tsx:670:  const operation = snapshot.activeOperation || snapshot.lastOperation;\nsrc\\renderer\\pages\\WorkspacePage.tsx:671:  const operationActive = Boolean(snapshot.activeOperation);\nsrc\\renderer\\pages\\WorkspacePage.tsx:672:  const operationTime = operation?.startedAt ? `开始于 ${formatChinaTime(operation.startedAt)}` : '';\nsrc\\renderer\\components\\PlanLists.tsx:3:import { formatChinaDateTime, formatDuration, getRunningDurationMs } from '../utils/time';\nsrc\\renderer\\components\\PlanLists.tsx:9:  duration_ms?: number | null;\nsrc\\renderer\\components\\PlanLists.tsx:34:  if (task.duration_ms === null || typeof task.duration_ms === 'undefined') return null;\nsrc\\renderer\\components\\PlanLists.tsx:35:  const duration = Number(task.duration_ms);\nsrc\\renderer\\components\\PlanLists.tsx:52:  if (task.status === 'running') return `已运行 ${formatDuration(duration, '0秒')}`;\nsrc\\renderer\\components\\PlanLists.tsx:54:  return `耗时 ${formatDuration(duration, '0秒')}`;\nsrc\\renderer\\components\\PlanLists.tsx:71:  return `总耗时 ${formatDuration(totalMs, '0秒')} · 已完成 ${formatDuration(completedMs, '0秒')}`;\nsrc\\renderer\\components\\PlanLists.tsx:150:  const durationMs = readMetaNumber(meta, ['durationMs', 'duration_ms']);\nsrc\\renderer\\components\\PlanLists.tsx:155:    durationMs !== null ? `耗时 ${formatDuration(durationMs, '0秒')}` : '',\nsrc\\renderer\\utils\\time.ts:55:export function formatDuration(milliseconds?: number | null, fallback = '未开始') {\nsrc\\renderer\\utils\\time.ts:75:export function getRunningDurationMs(durationMs?: number | null, startedAt?: string | null, now = Date.now()) {\nsrc\\renderer\\utils\\time.ts:76:  const baseDuration = normalizeDurationMs(durationMs) ?? 0;\nsrc\\renderer\\utils\\time.ts:77:  const startedDate = parseTimestamp(startedAt);\nsrc\\renderer\\utils\\time.ts:78:  if (!startedDate) return normalizeDurationMs(durationMs);\nrg: src\\*.js: 文件名、目录名或卷标语法不正确。 (os error 123)\n","metadata":{"exit_code":1,"duration_seconds":0.2}}
 exited 1 in 236ms:
src\renderer\types.ts:199:  duration_ms: number;
src\renderer\types.ts:259:  startedAt?: string | null;
src\renderer\types.ts:260:  finishedAt?: string | null;
src\renderer\types.ts:302:  activeOperation: ActiveOperation | null;
src\renderer\types.ts:303:  activeOperations: ActiveOperation[];
src\renderer\types.ts:304:  lastOperation: ActiveOperation | null;
src\renderer\types.ts:313:export interface ActiveOperation {
src\renderer\types.ts:318:  startedAt: string | null;
src\renderer\types.ts:319:  finishedAt?: string | null;
src\renderer\pages\WorkspacePage.tsx:670:  const operation = snapshot.activeOperation || snapshot.lastOperation;
src\renderer\pages\WorkspacePage.tsx:671:  const operationActive = Boolean(snapshot.activeOperation);
src\renderer\pages\WorkspacePage.tsx:672:  const operationTime = operation?.startedAt ? `开始于 ${formatChinaTime(operation.startedAt)}` : '';
src\renderer\components\PlanLists.tsx:3:import { formatChinaDateTime, formatDuration, getRunningDurationMs } from '../utils/time';
src\renderer\components\PlanLists.tsx:9:  duration_ms?: number | null;
src\renderer\components\PlanLists.tsx:34:  if (task.duration_ms === null || typeof task.duration_ms === 'undefined') return null;
src\renderer\components\PlanLists.tsx:35:  const duration = Number(task.duration_ms);
src\renderer\components\PlanLists.tsx:52:  if (task.status === 'running') return `已运行 ${formatDuration(duration, '0秒')}`;
src\renderer\components\PlanLists.tsx:54:  return `耗时 ${formatDuration(duration, '0秒')}`;
src\renderer\components\PlanLists.tsx:71:  return `总耗时 ${formatDuration(totalMs, '0秒')} · 已完成 ${formatDuration(completedMs, '0秒')}`;
src\renderer\components\PlanLists.tsx:150:  const durationMs = readMetaNumber(meta, ['durationMs', 'duration_ms']);
src\renderer\components\PlanLists.tsx:155:    durationMs !== null ? `耗时 ${formatDuration(durationMs, '0秒')}` : '',
src\renderer\utils\time.ts:55:export function formatDuration(milliseconds?: number | null, fallback = '未开始') {
src\renderer\utils\time.ts:75:export function getRunningDurationMs(durationMs?: number | null, startedAt?: string | null, now = Date.now()) {
src\renderer\utils\time.ts:76:  const baseDuration = normalizeDurationMs(durationMs) ?? 0;
src\renderer\utils\time.ts:77:  const startedDate = parseTimestamp(startedAt);
src\renderer\utils\time.ts:78:  if (!startedDate) return normalizeDurationMs(durationMs);
rg: src\*.js: 文件名、目录名或卷标语法不正确。 (os error 123)

**Inspecting plan tasks**

I'm considering inspecting PlanLists and possibly the task.duration_ms in a snapshot with active running calculations. There could be a bug since the snapshot tasks show raw DB data, and duration_ms only accounts for completed tasks. This might not include the current runtime for active tasks. I’ll need to open the plan file in read-only mode to understand the P003 context. Since the user mentioned only reading this file, I'll focus on retrieving relevant tasks and content carefully.
codex
为避免误解 P003，我会只读 plan 中相关上下文。
exec
powershell.exe -Command "rg -n \"P003|耗时|快照|duration|duration_ms|ActiveOperation|elapsed\" docs\\plan\\plan_requirement_4_20260625-030248.md" in D:\project\GitHub\autoplan
 succeeded in 176ms:
1:# 需求 #4 开发计划：任务系统增加耗时统计
5:当前任务系统已支持 Plan 解析、任务状态流转、并发执行、手动执行/停止、任务列表展示和 smoke 回归。需求 #4 要求在任务系统中增加耗时统计，核心目标是让每个任务从开始执行到结束/停止/失败的耗时可被记录、查询和展示，并在不影响既有任务执行流程的前提下兼容历史数据。
9:- [x] P001: 扩展任务耗时数据模型 <!-- scope: src/database.js,src/renderer/types.ts -->
10:  - 验收要点：`plan_tasks` 表新增任务开始时间、结束时间、累计耗时毫秒等字段，并通过现有迁移机制兼容旧数据库。
12:  - 验收要点：前端 `PlanTask` 类型同步新增耗时字段，TypeScript 编译不出现类型缺口。
14:- [x] P002: 在任务生命周期记录耗时 <!-- scope: src/loopService.js -->
16:  - 验收要点：任务完成、失败回退、手动停止时计算本次耗时并累加到任务耗时字段，同时写入 `finished_at` 和 `updated_at`。
17:  - 验收要点：任务重试时不丢失历史累计耗时，运行中实时耗时可通过 `started_at` 推导，状态流转保持原有语义。
18:  - 验收要点：`task.executed`、`task.failed` 或停止相关事件的元信息包含耗时，便于后续审计和日志排查。
20:- [ ] P003: 在快照数据中暴露耗时信息 <!-- scope: src/loopService.js,src/renderer/types.ts -->
21:  - 验收要点：`snapshot().tasks` 返回每个任务的开始时间、结束时间、累计耗时毫秒和运行中状态所需字段。
22:  - 验收要点：运行中任务的耗时展示不依赖额外 IPC 接口，现有定时 `emitUpdate` 可驱动刷新。
23:  - 验收要点：快照查询仍按 Plan 创建时间和任务顺序排序，新增字段不破坏现有前端消费逻辑。
25:- [x] P004: 展示任务耗时与计划汇总 <!-- scope: src/renderer/utils/time.ts,src/renderer/components/PlanLists.tsx,src/renderer/pages/WorkspacePage.tsx,src/styles.css -->
26:  - 验收要点：任务列表展示每个任务的耗时，未开始显示“未开始”，运行中显示“已运行 xx”，已完成/失败/停止显示累计耗时。
27:  - 验收要点：新增耗时格式化函数，支持毫秒输入并输出秒、分、小时级中文短格式，空值和异常值有兜底显示。
28:  - 验收要点：任务与计划区域展示当前 Plan 的总耗时/已完成任务耗时汇总，页面在窄屏和长任务标题下不溢出。
29:  - 验收要点：运行中任务耗时能随现有快照刷新更新，不需要用户手动刷新页面。
31:- [ ] P005: 补充耗时统计回归测试 <!-- scope: scripts/smoke-test.js -->
32:  - 验收要点：smoke 测试覆盖任务开始后写入 `started_at`，完成后写入 `finished_at` 和正数 `duration_ms`。
33:  - 验收要点：smoke 测试覆盖失败或停止回退到 `pending` 时耗时仍被累计，后续重试继续累加。
34:  - 验收要点：smoke 测试覆盖 `snapshot().tasks` 中耗时字段存在且类型符合前端预期。
39:  - 验收要点：`npm run smoke` 通过，确认数据库迁移、任务执行、耗时统计和原有任务系统行为无回归。
40:  - 验收要点：如验证失败，仅修复与耗时统计直接相关的问题，不扩大修改范围。
44:- 数据可靠：任务耗时以毫秒为基础存储，开始、完成、失败、停止和重试场景均能得到可解释的累计耗时。
45:- 兼容安全：旧数据库自动补齐新增字段，历史任务不因空耗时字段导致页面或任务流程异常。
46:- 展示清晰：任务列表能直观看到每个任务的耗时状态，Plan 区域能看到汇总耗时，运行中耗时会自动刷新。

**Evaluating task duration handling**

I'm looking at snapshot tasks and noticing that the current approach with raw db.all lacks withTaskDurationMeta. I think it might be better to map tasks so that I can return fields like started_at, finished_at, and duration_ms, if they exist. I need to ensure duration_ms is normalized. P003 doesn't include run_duration_ms, but maybe it should. I also notice that P004 PlanLists are using different calculations for duration. I need to determine the exact expected changes.
**Considering TypeScript updates**

I think the TypeScript PlanTask might need an update to add an optional `run_duration_ms`. I should take a closer look at the P002 helper withTaskDurationMeta, which includes run_duration_ms for event returns. However, it seems this field might not be utilized in snapshots. I need to clarify how this affects overall task handling and whether integrating this field can enhance the accuracy of duration tracking in my tasks. It's important to make sure everything lines up correctly!
**Inspecting database migration**

I need to focus on normalizing the old rows and checking the `task_id`. It makes sense to inspect the database migration, especially the PlanTask schema and its defaults. I'll take a closer look at how these are structured, since understanding this will help me ensure everything is aligned properly and working as it should. Finding the right information there should clarify my next steps!
codex
我会核对数据库字段默认值，判断快照是否需要规范化。
exec
powershell.exe -Command "rg -n \"plan_tasks|duration_ms|started_at|finished_at|run_duration\" src\\database.js src\\loopService.js src\\renderer\\components\\PlanLists.tsx src\\renderer\\utils\\time.ts" in D:\project\GitHub\autoplan
 succeeded in 208ms:
src\renderer\components\PlanLists.tsx:7:  started_at?: string | null;
src\renderer\components\PlanLists.tsx:8:  finished_at?: string | null;
src\renderer\components\PlanLists.tsx:9:  duration_ms?: number | null;
src\renderer\components\PlanLists.tsx:34:  if (task.duration_ms === null || typeof task.duration_ms === 'undefined') return null;
src\renderer\components\PlanLists.tsx:35:  const duration = Number(task.duration_ms);
src\renderer\components\PlanLists.tsx:40:  if (task.status === 'running') return getRunningDurationMs(readDurationMs(task), task.started_at);
src\renderer\components\PlanLists.tsx:53:  if (duration === null || (duration === 0 && !task.started_at && !task.finished_at)) return '未开始';
src\renderer\components\PlanLists.tsx:150:  const durationMs = readMetaNumber(meta, ['durationMs', 'duration_ms']);
src\loopService.js:144:    this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE status = ?', ['pending', now, 'running']);
src\loopService.js:425:      `SELECT plan_tasks.*
src\loopService.js:426:       FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
src\loopService.js:427:       WHERE plan_tasks.id = ? AND plans.project_id = ?`,
src\loopService.js:434:      `UPDATE plan_tasks
src\loopService.js:436:           started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,
src\loopService.js:437:           finished_at = NULL,
src\loopService.js:442:    return this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
src\loopService.js:446:    const task = this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
src\loopService.js:451:    const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;
src\loopService.js:452:    const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);
src\loopService.js:454:      `UPDATE plan_tasks
src\loopService.js:455:       SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?
src\loopService.js:463:        duration_ms: durationMs,
src\loopService.js:464:        finished_at: finishedAt,
src\loopService.js:502:      `UPDATE plan_tasks SET status = ?, updated_at = ?
src\loopService.js:514:      `UPDATE plan_tasks SET status = ?, updated_at = ?
src\loopService.js:538:    const existing = this.db.all('SELECT task_key FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order', [planId]);
src\loopService.js:562:    const task = this.db.get('SELECT * FROM plan_tasks WHERE plan_id = ? AND task_key = ?', [planId, taskKey]);
src\loopService.js:760:      `SELECT * FROM plan_tasks WHERE plan_id = ? AND status = 'pending'
src\loopService.js:919:       FROM plan_tasks
src\loopService.js:1026:    const existingTasks = this.db.all('SELECT * FROM plan_tasks WHERE plan_id = ? ORDER BY sort_order ASC, id ASC', [planId]);
src\loopService.js:1041:          `UPDATE plan_tasks
src\loopService.js:1056:          `INSERT INTO plan_tasks (plan_id, task_key, title, raw_line, scope, status, sort_order, updated_at)
src\loopService.js:1065:        this.db.run('DELETE FROM plan_tasks WHERE id = ?', [stale.id]);
src\loopService.js:1317:        `SELECT plan_tasks.*, plans.file_path
src\loopService.js:1318:         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
src\loopService.js:1320:         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
src\loopService.js:1368:      startedAt: task?.started_at,
src\loopService.js:1369:      finishedAt: task?.finished_at,
src\loopService.js:1370:      durationMs: task?.duration_ms,
src\loopService.js:1371:      runDurationMs: task?.run_duration_ms,
src\loopService.js:1421:    duration_ms: normalizeDurationMs(task.duration_ms),
src\loopService.js:1422:    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
src\database.js:131:      CREATE TABLE IF NOT EXISTS plan_tasks (
src\database.js:140:        started_at TEXT,
src\database.js:141:        finished_at TEXT,
src\database.js:142:        duration_ms INTEGER NOT NULL DEFAULT 0,
src\database.js:181:    this.ensureColumn('plan_tasks', 'scope', "TEXT NOT NULL DEFAULT ''");
src\database.js:182:    this.ensureColumn('plan_tasks', 'started_at', 'TEXT');
src\database.js:183:    this.ensureColumn('plan_tasks', 'finished_at', 'TEXT');
src\database.js:184:    this.ensureColumn('plan_tasks', 'duration_ms', 'INTEGER NOT NULL DEFAULT 0');
src\database.js:185:    this.ensureColumn('plan_tasks', 'codex_session_id', 'TEXT');

codex
已有事件元信息包含耗时，我会复用同一规范化逻辑到快照任务。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; for($i=1348;$i -le 1390 -and $i -le $lines.Length;$i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 226ms:
1348:               logTail: (operation.logBuffer || '').slice(-8000),
1349:               activity: operation.activity ? operation.activity.getLines() : [],
1350:             }))
1351:         : [],
1352:       lastOperation:
1353:         runtime?.lastOperation && Number(runtime.lastOperation.projectId) === Number(projectId)
1354:           ? runtime.lastOperation
1355:           : null,
1356:     };
1357:   }
1358: }
1359: 
1360: function taskEventMeta(task, overrides = {}) {
1361:   const meta = {
1362:     ...compactEventMeta({
1363:       taskId: task?.id,
1364:       taskKey: task?.task_key,
1365:       taskTitle: task?.title,
1366:       planId: task?.plan_id,
1367:       status: task?.status,
1368:       startedAt: task?.started_at,
1369:       finishedAt: task?.finished_at,
1370:       durationMs: task?.duration_ms,
1371:       runDurationMs: task?.run_duration_ms,
1372:     }),
1373:     ...compactEventMeta(overrides),
1374:   };
1375:   meta.taskId = normalizeOptionalNumber(meta.taskId);
1376:   meta.planId = normalizeOptionalNumber(meta.planId);
1377:   meta.taskKey = normalizeOptionalString(meta.taskKey);
1378:   meta.taskTitle = normalizeOptionalString(meta.taskTitle);
1379:   meta.status = normalizeOptionalString(meta.status);
1380:   meta.startedAt = normalizeOptionalString(meta.startedAt);
1381:   meta.finishedAt = normalizeOptionalString(meta.finishedAt);
1382:   meta.durationMs = normalizeOptionalNumber(meta.durationMs);
1383:   meta.runDurationMs = normalizeOptionalNumber(meta.runDurationMs);
1384:   const compacted = compactEventMeta(meta);
1385:   return Object.keys(compacted).length ? compacted : null;
1386: }
1387: 
1388: function syncedTaskStatus(parsedStatus, existingStatus) {
1389:   const next = normalizeOptionalString(parsedStatus) || TASK_EVENT_STATUS.PENDING;
1390:   const current = normalizeOptionalString(existingStatus);

**Figuring out event handling**

I’m thinking about possibly needing to map events' metadata. I wonder if the raw metadata strings can be handled in the front end? But then, I'm realizing it won't work for P003. I really need to inspect the PlanLists closely to see what's going on there. It feels like there's something crucial I need to uncover! Let's take a deeper look.
codex
前端已基于 `duration_ms` 和 `started_at` 推导运行中耗时。
exec
powershell.exe -Command '$p='"'src\\renderer\\components\\PlanLists.tsx'; "'$lines=Get-Content -Path $p -Encoding UTF8; for($i=1;$i -le 90 -and $i -le $lines.Length;$i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 292ms:
1: import type { AppEvent, Plan, PlanDraft, PlanTask } from '../types';
2: import { RecordCard } from './IntakePanel';
3: import { formatChinaDateTime, formatDuration, getRunningDurationMs } from '../utils/time';
4: 
5: type TimedPlanTask = PlanTask & {
6:   plan_id?: number | null;
7:   started_at?: string | null;
8:   finished_at?: string | null;
9:   duration_ms?: number | null;
10: };
11: 
12: type EventMetaRecord = Record<string, unknown>;
13: 
14: type TaskEventTone = 'start' | 'success' | 'failed' | 'stopped' | 'stopping' | 'updated';
15: 
16: type TaskEventDisplay = {
17:   title: string;
18:   body: string;
19:   meta: string;
20:   badge?: string;
21:   tone?: TaskEventTone;
22: };
23: 
24: const TASK_EVENT_PRESENTATION: Record<TaskEventTone, { action: string; badge: string }> = {
25:   start: { action: '开始了', badge: '开始' },
26:   success: { action: '结束了', badge: '成功' },
27:   failed: { action: '执行失败', badge: '失败' },
28:   stopped: { action: '停止了', badge: '停止' },
29:   stopping: { action: '请求停止', badge: '停止中' },
30:   updated: { action: '更新了', badge: '任务' },
31: };
32: 
33: function readDurationMs(task: TimedPlanTask) {
34:   if (task.duration_ms === null || typeof task.duration_ms === 'undefined') return null;
35:   const duration = Number(task.duration_ms);
36:   return Number.isFinite(duration) && duration >= 0 ? duration : null;
37: }
38: 
39: function getTaskDurationMs(task: TimedPlanTask) {
40:   if (task.status === 'running') return getRunningDurationMs(readDurationMs(task), task.started_at);
41:   return readDurationMs(task);
42: }
43: 
44: function readPlanId(task: TimedPlanTask) {
45:   if (task.plan_id === null || typeof task.plan_id === 'undefined') return null;
46:   const planId = Number(task.plan_id);
47:   return Number.isFinite(planId) ? planId : null;
48: }
49: 
50: function formatTaskDuration(task: TimedPlanTask) {
51:   const duration = getTaskDurationMs(task);
52:   if (task.status === 'running') return `已运行 ${formatDuration(duration, '0秒')}`;
53:   if (duration === null || (duration === 0 && !task.started_at && !task.finished_at)) return '未开始';
54:   return `耗时 ${formatDuration(duration, '0秒')}`;
55: }
56: 
57: function tasksForPlan(tasks: PlanTask[], plan: Plan, planCount: number) {
58:   const timedTasks = tasks as TimedPlanTask[];
59:   const hasPlanIds = timedTasks.some((task) => readPlanId(task) !== null);
60:   if (!hasPlanIds) return planCount === 1 ? timedTasks : [];
61:   return timedTasks.filter((task) => readPlanId(task) === plan.id);
62: }
63: 
64: function formatPlanDurationSummary(tasks: TimedPlanTask[]) {
65:   const totalMs = tasks.reduce((sum, task) => sum + (getTaskDurationMs(task) ?? 0), 0);
66:   const completedMs = tasks.reduce(
67:     (sum, task) => sum + (task.status === 'completed' ? getTaskDurationMs(task) ?? 0 : 0),
68:     0,
69:   );
70: 
71:   return `总耗时 ${formatDuration(totalMs, '0秒')} · 已完成 ${formatDuration(completedMs, '0秒')}`;
72: }
73: 
74: function toEventMetaRecord(value: unknown): EventMetaRecord | null {
75:   if (!value) return null;
76:   if (typeof value === 'string') {
77:     try {
78:       return toEventMetaRecord(JSON.parse(value));
79:     } catch {
80:       return null;
81:     }
82:   }
83:   if (typeof value === 'object' && !Array.isArray(value)) return value as EventMetaRecord;
84:   return null;
85: }
86: 
87: function readEventMeta(event: AppEvent) {
88:   return toEventMetaRecord((event as AppEvent & { meta?: unknown }).meta);
89: }
90: 

**Planning Updates**

I see that P004 has been modified outside of the scope but it's already done, so there's no need to edit it. I think I need to come up with a plan for a potential update because there are multiple steps involved. I wonder if I should include those steps in my plan. Maybe I could call now to make sure everything's set in place. Let's think about this moving forward!
codex
我会按 P003 只改快照任务输出和类型声明。
  ✓ 确认快照耗时缺口
  → 规范化任务快照字段
  • 同步前端任务类型
  • 执行最小验证
**Normalizing task data**

I’m dealing with the "existing timed emitUpdate," which sends a snapshot every 600ms, but UI calculates based on the current time, only rendering on updates. I think normalizing `duration_ms` is good, so I might include `run_duration_ms` as optional. 

I should ensure that if the migration hasn't run, `SELECT plan_tasks.*` might return undefined. Setting fields like `started_at` and `finished_at` to null seems necessary. Event metadata already uses `runDurationMs`, but database column issues need consideration when thinking about naming conventions. I want to streamline all this for clarity!
**Noting naming conventions**

I just realized that the renderer for PlanTask currently uses snake_case. That might be important to consider when I'm normalizing other fields or thinking about consistency throughout the code. Since I've been looking at naming conventions, I need to make sure that any new fields align with this style. This will help maintain clarity and avoid confusion later on, especially when dealing with interactions between different parts of the system. I've got to pay attention to this detail!
codex
我会让 `snapshot().tasks` 返回规范化后的耗时字段，不改变排序查询。
apply patch
patch: completed
D:\project\GitHub\autoplan\src\loopService.js
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;

**Figuring out patch application**

Oh no, the developer mentioned I should use the apply_patch tool via shell, but I don't have a direct apply_patch namespace—only shell. It warned me but still applied, which is a bit concerning. I guess I need to update types now. It seems using the shell with apply_patch is unavoidable since that's the only instruction available for it. I need to ensure I do this correctly!
codex
快照任务已规范化；现在同步 `PlanTask` 的运行中耗时类型。
apply patch
patch: completed
D:\project\GitHub\autoplan\src\renderer\types.ts
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
我会先看可用脚本，再只跑与这两处改动相关的最小检查。
exec
powershell.exe -Command "Get-Content -Path package.json -Raw -Encoding UTF8 | Select-String -Pattern '\"scripts\"|typecheck|test|smoke|build' -Context 0,20" in D:\project\GitHub\autoplan
 succeeded in 359ms:

> {
  "name": "autoplan",
  "version": "0.1.4",
  "description": "Electron app for issue-driven autonomous planning and development loops.",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "node scripts/dev.js",
    "build": "tsc --noEmit && vite build",
    "package:win": "npm run build && electron-builder --win nsis --x64",
    "check": "tsc --noEmit && node --check src/main.js && node --check src/attachments.js && node --check src/database.
js && node --check src/loopService.js && node --check src/planDrafts.js && node --check src/preload.js && node --check 
scripts/dev.js && node --check scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "build": {
    "appId": "com.autoplan.desktop",
    "productName": "AutoPlan",
    "directories": {
      "output": "release"
    },
    "files": [
      "assets/**/*",
      "dist/**/*",
      "src/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "node_modules/sql.js/dist/*.wasm"
    ],
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-${arch}-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.18.0",
    "remark-gfm": "^4.0.1",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "electron": "^33.3.2",
    "electron-builder": "^26.15.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  }
}




diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
类型已同步；现在运行 JS 语法和 TypeScript 编译检查。
  ✓ 确认快照耗时缺口
  ✓ 规范化任务快照字段
  ✓ 同步前端任务类型
  → 执行最小验证
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
先做 `loopService.js` 语法检查，再做类型检查。
exec
powershell.exe -Command "node --check src\\loopService.js" in D:\project\GitHub\autoplan
 succeeded in 303ms:
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
JS 语法通过；继续运行最小 TypeScript 类型检查。
exec
powershell.exe -Command 'npx tsc --noEmit --pretty false' in D:\project\GitHub\autoplan
 succeeded in 4717ms:
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

**Inspecting summaries**

I need to look closely at the differences between the summaries and maybe check the line references. To do this, I'll use the command `git diff -- src/loopService.js src/renderer/types.ts` for comparisons. It might also be useful to run `git status` to see what else is happening. Although I don't need a full diff in the final output, these steps are essential for my understanding right now. Let’s make sure I stay organized!
codex
验证通过；我会确认只改了任务范围内文件。
exec
powershell.exe -Command "git status --short -- src\\loopService.js src\\renderer\\types.ts docs\\plan\\plan_requirement_4_20260625-030248.md; git diff --stat -- src\\loopService.js src\\renderer\\types.ts; rg -n \"taskSnapshotRow|run_duration_ms|tasks: this\\.db\" src\\loopService.js src\\renderer\\types.ts" in D:\project\GitHub\autoplan
 succeeded in 344ms:
 M src/loopService.js
 M src/renderer/types.ts
?? docs/plan/plan_requirement_4_20260625-030248.md
 src/loopService.js    | 777 ++++++++++++++++++++++++++++++++++++++++++++------
 src/renderer/types.ts | 168 ++++++++++-
 2 files changed, 849 insertions(+), 96 deletions(-)
src\renderer\types.ts:200:  run_duration_ms?: number;
src\loopService.js:1316:      tasks: this.db
src\loopService.js:1324:        .map((task) => taskSnapshotRow(task)),
src\loopService.js:1373:      runDurationMs: task?.run_duration_ms,
src\loopService.js:1424:    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
src\loopService.js:1428:function taskSnapshotRow(task) {
src\loopService.js:1439:    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
warning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/renderer/types.ts', LF will be replaced by CRLF the next time Git touches it

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
发现行尾导致 diff 过大；我会还原为仓库原有 CRLF 行尾。
exec
powershell.exe -Command "git diff --numstat --ignore-cr-at-eol -- src\\loopService.js src\\renderer\\types.ts; git diff --check -- src\\loopService.js src\\renderer\\types.ts" in D:\project\GitHub\autoplan
 succeeded in 287ms:
683	94	src/loopService.js
166	2	src/renderer/types.ts
warning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/renderer/types.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/renderer/types.ts', LF will be replaced by CRLF the next time Git touches it

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

**Investigating line endings**

I’m thinking about why the file has huge discrepancies in line endings. It could be that the content has strange CR (Carriage Return) characters. I wonder if the Get-Content and Set commands normalize the line endings, but maybe the apply_patch ignored these CRs. I'm curious if the original file used CRLF line endings and the patch tool created LF line endings, which caused the issue. Maybe running a diff with context could help clarify what’s going on here.
codex
差异仍偏大，我会查看局部 diff 判断是否有意外格式化。
exec
powershell.exe -Command "git diff -- src\\loopService.js src\\renderer\\types.ts | Select-Object -First 220" in D:\project\GitHub\autoplan
2026-06-24T20:21:49.787345Z ERROR codex_core::tools::router: error={"output":"Total output lines: 222\n\ndiff --git a/src/loopService.js b/src/loopService.js\r\nindex 7d17bb9..5be1ea2 100644\r\n--- a/src/loopService.js\r\n+++ b/src/loopService.js\r\n@@ -8,6 +8,51 @@ const { CodexActivityPrinter } = require('./codexActivity');\r\n \r\n const ACTIVE_RUNTIME_PHASES = new Set(['running', 'scan', 'generate-plan', 'execute-task', 'validate']);\r\n const ACTIVE_RUNTIME_PHASE_SQL = \"('running','scan','generate-plan','execute-task','validate')\";\r\n+const MAX_PARALLEL_TASKS = 2;\r\n+const PARALLEL_BLOCKING_TASK_RE = /(全量|回归|验证|验收|测试|记录|整理|发布|部署|test|validate|regression|release|deploy)/i;\r\n+const TASK_SCOPE_LABEL_RE = '(?:scope|scopes|files?|影响范围|并发键)';\r\n+const TASK_SCOPE_RE = new RegExp(`${TASK_SCOPE_LABEL_RE}\\\\s*[:=：]\\\\s*([^>\\\\]\\\\n]+)`, 'i');\r\n+const TASK_SCOPE_COMMENT_RE = new RegExp(`\\\\s*<!--\\\\s*${TASK_SCOPE_LABEL_RE}\\\\s*[:=：]\\\\s*([^>]*?)\\\\s*-->\\\\s*`, 'i');\r\n+const TASK_SCOPE_SPLIT_RE = /[,，、;；]+/;\r\n+const TASK_PATH_RE = /[\\w./\\\\-]+\\.(?:dart|js|jsx|ts|tsx|css|scss|html|md|json|ya?ml)/gi;\r\n+\r\n+const TASK_EVENT_TYPES = Object.freeze({\r\n+  STARTED: 'task.started',\r\n+  SUCCEEDED: 'task.succeeded',\r\n+  FAILED: 'task.failed',\r\n+  STOP_REQUESTED: 'task.stop.requested',\r\n+  STOPPED: 'task.stopped',\r\n+  INTERRUPTED: 'task.interrupted',\r\n+});\r\n+\r\n+const LEGACY_TASK_EVENT_TYPES = Object.freeze({\r\n+  EXECUTED: 'task.executed',\r\n+  STOPPING: 'task.stopping',\r\n+});\r\n+\r\n+const TASK_EVENT_STATUS = Object.freeze({\r\n+  PENDING: 'pending',\r\n+  RUNNING: 'running',\r\n+  COMPLETED: 'completed',\r\n+  FAILED: 'failed',\r\n+  STOPPING: 'stopping',\r\n+  STOPPED: 'stopped',\r\n+  INTERRUPTED: 'interrupted',\r\n+});\r\n+\r\n+const TASK_EVENT_SEMANTICS = Object.freeze({\r\n+  [TASK_EVENT_TYPES.STARTED]: Object.freeze({ status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务' }),\r\n+  [TASK_EVENT_TYPES.SUCCEEDED]: Object.freeze({ status: TASK_EVENT_STATUS.COMPLETED, label: '结束了任务' }),\r\n+  [TASK_EVENT_TYPES.FAILED]: Object.freeze({ status: TASK_EVENT_STATUS.FAILED, label: '任务失败' }),\r\n+  [TASK_EVENT_TYPES.STOP_REQUESTED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPING, label: '请求停止任务' }),\r\n+  [TASK_EVENT_TYPES.STOPPED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务' }),\r\n+  [TASK_EVENT_TYPES.INTERRUPTED]: Object.freeze({ status: TASK_EVENT_STATUS.INTERRUPTED, label: '已中断任务' }),\r\n+});\r\n+\r\n+const TASK_EVENT_COMPATIBILITY = Object.freeze({\r\n+  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,\r\n+  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,\r\n+});\r\n \r\n class LoopService extends EventEmitter {\r\n   constructor(db) {\r\n@@ -210,17 +255,25 @@ class LoopService extends EventEmitter {\r\n       runtime.timer = null;\r\n       runtime.running = false;\r\n     }\r\n-    if (runtime?.activeChild) {\r\n-      const activeTaskId = runtime.activeOperation?.taskId || null;\r\n-      killChildProcess(runtime.activeChild);\r\n-      if (activeTaskId) {\r\n-        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [\r\n-          'pending',\r\n-          nowIso(),\r\n-          activeTaskId,\r\n-        ]);\r\n+    if (runtime?.activeOperations?.size) {\r\n+      for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {\r\n+        const child = runtime.activeChildren.get(operationKey);\r\n+        const activeTaskId = operation?.taskId || null;\r\n+        const finishedAt = nowIso();\r\n+        killChildProcess(child);\r\n+        const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;\r\n+        const stoppedTask = activeTaskId\r\n+          ? this.finishTaskRun(activeTaskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true })\r\n+          : null;\r\n+        this.addEvent(\r\n+          projectId,\r\n+          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',\r\n+          operation?.label || '',\r\n+          stoppedTask || activeTask\r\n+            ? taskEventMeta(stoppedTask || activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt })\r\n+            : null,\r\n+        );\r\n       }\r\n-      this.addEvent(projectId, activeTaskId ? 'task.stopping' : 'operation.stopping', runtime.activeOperation?.label || '');\r\n     }\r\n     this.db.run(\r\n       'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',\r\n@@ -314,10 +367,12 @@ class LoopService extends EventEmitter {\r\n \r\n     runtime.busy = true;\r\n     try {\r\n-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), taskId]);\r\n+      this.startTaskRun(taskId);\r\n       this.emitUpdate(projectId);\r\n-      await this.executeTask(workspace, plan, task);\r\n-      this.syncPlanTasks(plan.id, path.join(workspace, plan.file_path));\r\n+      const result = await this.executeTask(workspace, plan, task);\r\n+      if (result.exitCode === 0) {\r\n+        this.completeTask(workspace, plan, task, result);\r\n+      }\r\n       if (runtime.running) {\r\n         …368 chars truncated…activeOperation?.taskId) === Number(taskId)) {\r\n-      killChildProcess(runtime.activeChild);\r\n-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), taskId]);\r\n+    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));\r\n+    if (activeEntry) {\r\n+      killChildProcess(activeEntry.child);\r\n+      const finishedAt = nowIso();\r\n+      const stoppedTask = runtime?.running\r\n+        ? task\r\n+        : this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;\r\n       if (!runtime.running) {\r\n-        this.addEvent(projectId, 'task.stopping', task.title);\r\n+        this.addEvent(\r\n+          projectId,\r\n+          LEGACY_TASK_EVENT_TYPES.STOPPING,\r\n+          task.title,\r\n+          taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt }),\r\n+        );\r\n       }\r\n     } else {\r\n-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = ?', [\r\n-        'pending',\r\n-        nowIso(),\r\n-        taskId,\r\n-        'running',\r\n-      ]);\r\n-      this.addEvent(projectId, 'task.stop.requested', task.title);\r\n+      const finishedAt = nowIso();\r\n+      const stoppedTask = this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;\r\n+      this.addEvent(\r\n+        projectId,\r\n+        TASK_EVENT_TYPES.STOP_REQUESTED,\r\n+        task.title,\r\n+        taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPING, finishedAt }),\r\n+      );\r\n     }\r\n \r\n     if (runtime?.running) this.stop(projectId);\r\n@@ -364,19 +429,73 @@ class LoopService extends EventEmitter {\r\n     );\r\n   }\r\n \r\n+  startTaskRun(taskId, startedAt = nowIso()) {\r\n+    this.db.run(\r\n+      `UPDATE plan_tasks\r\n+       SET status = ?,\r\n+           started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,\r\n+           finished_at = NULL,\r\n+           updated_at = ?\r\n+       WHERE id = ?`,\r\n+      [TASK_EVENT_STATUS.RUNNING, TASK_EVENT_STATUS.RUNNING, startedAt, startedAt, taskId],\r\n+    );\r\n+    return this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);\r\n+  }\r\n+\r\n+  finishTaskRun(taskId, status, finishedAt = nowIso(), options = {}) {\r\n+    const task = this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);\r\n+    if (!task) return null;\r\n+    const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;\r\n+    if (options.onlyIfRunning && !isRunning) return withTaskDurationMeta(task);\r\n+\r\n+    const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;\r\n+    const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);\r\n+    this.db.run(\r\n+      `UPDATE plan_tasks\r\n+       SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?\r\n+       WHERE id = ?`,\r\n+      [status, durationMs, finishedAt, finishedAt, taskId],\r\n+    );\r\n+    return withTaskDurationMeta(\r\n+      {\r\n+        ...task,\r\n+        status,\r\n+        duration_ms: durationMs,\r\n+        finished_at: finishedAt,\r\n+        updated_at: finishedAt,\r\n+      },\r\n+      runDurationMs,\r\n+    );\r\n+  }\r\n+\r\n   /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */\r\n   interruptPlan(projectId, planId) {\r\n     const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);\r\n     if (!plan) return;\r\n     // 若当前正在执行该 plan 的任务，kill 进程\r\n     const runtime = this.runtime(projectId);\r\n-    if (runtime?.activeChild && Number(runtime.activeOperation?.planId) === Number(planId)) {\r\n-      killChildProcess(runtime.activeChild);\r\n-      const activeTaskId = runtime.activeOperation?.taskId;\r\n-      if (activeTaskId) {\r\n-        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked', nowIso(), activeTaskId]);\r\n-      }\r\n-      this.addEvent(projectId, 'task.stopping', `plan #${planId} 的任务被中断`);\r\n+    const activePlanOperations = runtime\r\n+      ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))\r\n+      : [];\r\n+    for (const activeEntry of activePlanOperations) {\r\n+      const finishedAt = nowIso();\r\n+      killChildProcess(activeEntry.child);\r\n+      const activeTaskId = activeEntry.operation?.taskId;\r\n+      const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;\r\n+      const interruptedTask = activeTaskId\r\n+        ? this.finishTaskRun(activeTaskId, 'blocked', finishedAt, { onlyIfRunning: true })\r\n+        : null;\r\n+      this.addEvent(\r\n+        projectId,\r\n+        LEGACY_TASK_EVENT_TYPES.STOPPING,\r\nwarning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it\nwarning: in the working copy of 'src/renderer/types.ts', LF will be replaced by CRLF the next time Git touches it\n","metadata":{"exit_code":1,"duration_seconds":0.3}}
 exited 1 in 312ms:
diff --git a/src/loopService.js b/src/loopService.js
index 7d17bb9..5be1ea2 100644
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -8,6 +8,51 @@ const { CodexActivityPrinter } = require('./codexActivity');
 
 const ACTIVE_RUNTIME_PHASES = new Set(['running', 'scan', 'generate-plan', 'execute-task', 'validate']);
 const ACTIVE_RUNTIME_PHASE_SQL = "('running','scan','generate-plan','execute-task','validate')";
+const MAX_PARALLEL_TASKS = 2;
+const PARALLEL_BLOCKING_TASK_RE = /(全量|回归|验证|验收|测试|记录|整理|发布|部署|test|validate|regression|release|deploy)/i;
+const TASK_SCOPE_LABEL_RE = '(?:scope|scopes|files?|影响范围|并发键)';
+const TASK_SCOPE_RE = new RegExp(`${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>\\]\\n]+)`, 'i');
+const TASK_SCOPE_COMMENT_RE = new RegExp(`\\s*<!--\\s*${TASK_SCOPE_LABEL_RE}\\s*[:=：]\\s*([^>]*?)\\s*-->\\s*`, 'i');
+const TASK_SCOPE_SPLIT_RE = /[,，、;；]+/;
+const TASK_PATH_RE = /[\w./\\-]+\.(?:dart|js|jsx|ts|tsx|css|scss|html|md|json|ya?ml)/gi;
+
+const TASK_EVENT_TYPES = Object.freeze({
+  STARTED: 'task.started',
+  SUCCEEDED: 'task.succeeded',
+  FAILED: 'task.failed',
+  STOP_REQUESTED: 'task.stop.requested',
+  STOPPED: 'task.stopped',
+  INTERRUPTED: 'task.interrupted',
+});
+
+const LEGACY_TASK_EVENT_TYPES = Object.freeze({
+  EXECUTED: 'task.executed',
+  STOPPING: 'task.stopping',
+});
+
+const TASK_EVENT_STATUS = Object.freeze({
+  PENDING: 'pending',
+  RUNNING: 'running',
+  COMPLETED: 'completed',
+  FAILED: 'failed',
+  STOPPING: 'stopping',
+  STOPPED: 'stopped',
+  INTERRUPTED: 'interrupted',
+});
+
+const TASK_EVENT_SEMANTICS = Object.freeze({
+  [TASK_EVENT_TYPES.STARTED]: Object.freeze({ status: TASK_EVENT_STATUS.RUNNING, label: '开始了任务' }),
+  [TASK_EVENT_TYPES.SUCCEEDED]: Object.freeze({ status: TASK_EVENT_STATUS.COMPLETED, label: '结束了任务' }),
+  [TASK_EVENT_TYPES.FAILED]: Object.freeze({ status: TASK_EVENT_STATUS.FAILED, label: '任务失败' }),
+  [TASK_EVENT_TYPES.STOP_REQUESTED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPING, label: '请求停止任务' }),
+  [TASK_EVENT_TYPES.STOPPED]: Object.freeze({ status: TASK_EVENT_STATUS.STOPPED, label: '已停止任务' }),
+  [TASK_EVENT_TYPES.INTERRUPTED]: Object.freeze({ status: TASK_EVENT_STATUS.INTERRUPTED, label: '已中断任务' }),
+});
+
+const TASK_EVENT_COMPATIBILITY = Object.freeze({
+  [LEGACY_TASK_EVENT_TYPES.EXECUTED]: TASK_EVENT_TYPES.SUCCEEDED,
+  [LEGACY_TASK_EVENT_TYPES.STOPPING]: TASK_EVENT_TYPES.STOPPED,
+});
 
 class LoopService extends EventEmitter {
   constructor(db) {
@@ -210,17 +255,25 @@ class LoopService extends EventEmitter {
       runtime.timer = null;
       runtime.running = false;
     }
-    if (runtime?.activeChild) {
-      const activeTaskId = runtime.activeOperation?.taskId || null;
-      killChildProcess(runtime.activeChild);
-      if (activeTaskId) {
-        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', [
-          'pending',
-          nowIso(),
-          activeTaskId,
-        ]);
+    if (runtime?.activeOperations?.size) {
+      for (const [operationKey, operation] of Array.from(runtime.activeOperations.entries())) {
+        const child = runtime.activeChildren.get(operationKey);
+        const activeTaskId = operation?.taskId || null;
+        const finishedAt = nowIso();
+        killChildProcess(child);
+        const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
+        const stoppedTask = activeTaskId
+          ? this.finishTaskRun(activeTaskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true })
+          : null;
+        this.addEvent(
+          projectId,
+          activeTaskId ? LEGACY_TASK_EVENT_TYPES.STOPPING : 'operation.stopping',
+          operation?.label || '',
+          stoppedTask || activeTask
+            ? taskEventMeta(stoppedTask || activeTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt })
+            : null,
+        );
       }
-      this.addEvent(projectId, activeTaskId ? 'task.stopping' : 'operation.stopping', runtime.activeOperation?.label || '');
     }
     this.db.run(
       'UPDATE project_states SET running = 0, phase = ?, updated_at = ? WHERE project_id = ?',
@@ -314,10 +367,12 @@ class LoopService extends EventEmitter {
 
     runtime.busy = true;
     try {
-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['running', nowIso(), taskId]);
+      this.startTaskRun(taskId);
       this.emitUpdate(projectId);
-      await this.executeTask(workspace, plan, task);
-      this.syncPlanTasks(plan.id, path.join(workspace, plan.file_path));
+      const result = await this.executeTask(workspace, plan, task);
+      if (result.exitCode === 0) {
+        this.completeTask(workspace, plan, task, result);
+      }
       if (runtime.running) {
         this.setPhase(projectId, 'waiting');
       } else if (this.db.get('SELECT phase FROM project_states WHERE project_id = ?', [projectId])?.phase !== 'stopped') {
@@ -334,20 +389,30 @@ class LoopService extends EventEmitter {
     if (!task) throw new Error('任务不存在');
     const runtime = this.runtime(projectId);
 
-    if (runtime?.activeChild && Number(runtime.activeOperation?.taskId) === Number(taskId)) {
-      killChildProcess(runtime.activeChild);
-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['pending', nowIso(), taskId]);
+    const activeEntry = findRuntimeOperation(runtime, (operation) => Number(operation?.taskId) === Number(taskId));
+    if (activeEntry) {
+      killChildProcess(activeEntry.child);
+      const finishedAt = nowIso();
+      const stoppedTask = runtime?.running
+        ? task
+        : this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
       if (!runtime.running) {
-        this.addEvent(projectId, 'task.stopping', task.title);
+        this.addEvent(
+          projectId,
+          LEGACY_TASK_EVENT_TYPES.STOPPING,
+          task.title,
+          taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPED, finishedAt }),
+        );
       }
     } else {
-      this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ? AND status = ?', [
-        'pending',
-        nowIso(),
-        taskId,
-        'running',
-      ]);
-      this.addEvent(projectId, 'task.stop.requested', task.title);
+      const finishedAt = nowIso();
+      const stoppedTask = this.finishTaskRun(taskId, TASK_EVENT_STATUS.PENDING, finishedAt, { onlyIfRunning: true }) || task;
+      this.addEvent(
+        projectId,
+        TASK_EVENT_TYPES.STOP_REQUESTED,
+        task.title,
+        taskEventMeta(stoppedTask, { status: TASK_EVENT_STATUS.STOPPING, finishedAt }),
+      );
     }
 
     if (runtime?.running) this.stop(projectId);
@@ -364,19 +429,73 @@ class LoopService extends EventEmitter {
     );
   }
 
+  startTaskRun(taskId, startedAt = nowIso()) {
+    this.db.run(
+      `UPDATE plan_tasks
+       SET status = ?,
+           started_at = CASE WHEN status = ? AND started_at IS NOT NULL THEN started_at ELSE ? END,
+           finished_at = NULL,
+           updated_at = ?
+       WHERE id = ?`,
+      [TASK_EVENT_STATUS.RUNNING, TASK_EVENT_STATUS.RUNNING, startedAt, startedAt, taskId],
+    );
+    return this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
+  }
+
+  finishTaskRun(taskId, status, finishedAt = nowIso(), options = {}) {
+    const task = this.db.get('SELECT * FROM plan_tasks WHERE id = ?', [taskId]);
+    if (!task) return null;
+    const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+    if (options.onlyIfRunning && !isRunning) return withTaskDurationMeta(task);
+
+    const runDurationMs = isRunning ? taskRunDurationMs(task.started_at, finishedAt) : undefined;
+    const durationMs = normalizeDurationMs(task.duration_ms) + (runDurationMs || 0);
+    this.db.run(
+      `UPDATE plan_tasks
+       SET status = ?, duration_ms = ?, finished_at = ?, updated_at = ?
+       WHERE id = ?`,
+      [status, durationMs, finishedAt, finishedAt, taskId],
+    );
+    return withTaskDurationMeta(
+      {
+        ...task,
+        status,
+        duration_ms: durationMs,
+        finished_at: finishedAt,
+        updated_at: finishedAt,
+      },
+      runDurationMs,
+    );
+  }
+
   /** 中断某个 plan：停止正在执行的 codex 进程，未完成任务标记为 blocked，plan 标记为 interrupted */
   interruptPlan(projectId, planId) {
     const plan = this.db.get('SELECT * FROM plans WHERE id = ? AND project_id = ?', [planId, projectId]);
     if (!plan) return;
     // 若当前正在执行该 plan 的任务，kill 进程
     const runtime = this.runtime(projectId);
-    if (runtime?.activeChild && Number(runtime.activeOperation?.planId) === Number(planId)) {
-      killChildProcess(runtime.activeChild);
-      const activeTaskId = runtime.activeOperation?.taskId;
-      if (activeTaskId) {
-        this.db.run('UPDATE plan_tasks SET status = ?, updated_at = ? WHERE id = ?', ['blocked', nowIso(), activeTaskId]);
-      }
-      this.addEvent(projectId, 'task.stopping', `plan #${planId} 的任务被中断`);
+    const activePlanOperations = runtime
+      ? findRuntimeOperations(runtime, (operation) => Number(operation?.planId) === Number(planId))
+      : [];
+    for (const activeEntry of activePlanOperations) {
+      const finishedAt = nowIso();
+      killChildProcess(activeEntry.child);
+      const activeTaskId = activeEntry.operation?.taskId;
+      const activeTask = activeTaskId ? this.taskForProject(projectId, activeTaskId) : null;
+      const interruptedTask = activeTaskId
+        ? this.finishTaskRun(activeTaskId, 'blocked', finishedAt, { onlyIfRunning: true })
+        : null;
+      this.addEvent(
+        projectId,
+        LEGACY_TASK_EVENT_TYPES.STOPPING,
warning: in the working copy of 'src/loopService.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/renderer/types.ts', LF will be replaced by CRLF the next time Git touches it

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

**Checking git status**

I’m considering whether I should use “git status” to get an overview, but I see that the user doesn’t want a long file list. That’s okay! I wonder what specific information the user is after—perhaps I can provide a more concise output? I think I’ll proceed by focusing on just what’s relevant without overwhelming with details. Let’s see how I can keep it brief and useful for the user!
codex
当前仓库已有未提交任务改动；我只核对本次新增的 P003 位置。
exec
powershell.exe -Command '$p='"'src\\loopService.js'; "'$lines=Get-Content -Path $p -Encoding UTF8; foreach($range in @(@(1312,1327),@(1424,1441))) { Write-Host "--- $($range[0])-$($range[1]) ---"; for($i=$range[0]; $i -le $range[1] -and $i -le $lines.Length; $i++){ '"'{0}: {1}' -f "'$i,$lines[$i-1] } }; $t=Get-Content -Path src'"\\renderer\\types.ts -Encoding UTF8; Write-Host '--- types 194-203 ---'; for("'$i=194;$i -le 203 -and $i -le $t.Length;$i++){ '"'{0}: {1}' -f "'$i,$t[$i-1] }' in D:\project\GitHub\autoplan
 succeeded in 278ms:
--- 1312-1327 ---
1312:       planDrafts: this.db.all('SELECT * FROM plan_drafts WHERE project_id = ? ORDER BY updated_at DESC, id DESC', [
1313:         projectId,
1314:       ]),
1315:       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
1316:       tasks: this.db
1317:         .all(
1318:           `SELECT plan_tasks.*, plans.file_path
1319:            FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
1320:            WHERE plans.project_id = ?
1321:            ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
1322:           [projectId],
1323:         )
1324:         .map((task) => taskSnapshotRow(task)),
1325:       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
1326:       scans: this.db.all(
1327:         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
--- 1424-1441 ---
1424:     ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
1425:   };
1426: }
1427: 
1428: function taskSnapshotRow(task) {
1429:   if (!task) return task;
1430:   const startedAt = normalizeOptionalString(task.started_at) || null;
1431:   const finishedAt = normalizeOptionalString(task.finished_at) || null;
1432:   const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
1433:   const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
1434:   return {
1435:     ...task,
1436:     started_at: startedAt,
1437:     finished_at: finishedAt,
1438:     duration_ms: normalizeDurationMs(task.duration_ms),
1439:     ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
1440:   };
1441: }
--- types 194-203 ---
194:   scope: string;
195:   status: string;
196:   sort_order: number;
197:   started_at: string | null;
198:   finished_at: string | null;
199:   duration_ms: number;
200:   run_duration_ms?: number;
201:   codex_session_id: string | null;
202:   updated_at: string;
203:   /** JOIN plans 得到 */

diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

**Considering task duration**

I'm noticing a potential issue where `taskRunDurationMs(startedAt, nowIso())` returns 0 if `startedAt` is null. That's something I need to keep in mind! It seems like I might need to run some tests first and ensure everything is functioning correctly before I update the plan. It's important to confirm that tests are passing to avoid any unexpected problems down the line. Definitely a detail to keep an eye on!
codex
本次新增位置确认无偏移；验证步骤已完成。
  ✓ 确认快照耗时缺口
  ✓ 规范化任务快照字段
  ✓ 同步前端任务类型
  ✓ 执行最小验证
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

codex
已完成 P003，未修改 plan 文件、未勾选 checkbox。

- `src/loopService.js:1316` 将 `snapshot().tasks` 查询结果映射为任务快照行，保持原排序不变。
- `src/loopService.js:1428` 新增快照任务字段规范化：`started_at`、`finished_at`、`duration_ms`，运行中额外暴露 `run_duration_ms`。
- `src/renderer/types.ts:197` 同步 `PlanTask` 类型，新增可选 `run_duration_ms`。
- 已验证：`node --check src\loopService.js`
- 已验证：`npx tsc --noEmit --pretty false`
diff --git a/src/loopService.js b/src/loopService.js
index c4e247c1d33e2d966676d6ab75413bc9f45c5bdc..5be1ea2f5ffb6c8559bce9a8c03a4c7df6fdb53b
--- a/src/loopService.js
+++ b/src/loopService.js
@@ -1313,13 +1313,15 @@
         projectId,
       ]),
       plans: this.db.all('SELECT * FROM plans WHERE project_id = ? ORDER BY created_at DESC', [projectId]),
-      tasks: this.db.all(
-        `SELECT plan_tasks.*, plans.file_path
-         FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
-         WHERE plans.project_id = ?
-         ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
-        [projectId],
-      ),
+      tasks: this.db
+        .all(
+          `SELECT plan_tasks.*, plans.file_path
+           FROM plan_tasks JOIN plans ON plans.id = plan_tasks.plan_id
+           WHERE plans.project_id = ?
+           ORDER BY plans.created_at DESC, plan_tasks.sort_order ASC`,
+          [projectId],
+        )
+        .map((task) => taskSnapshotRow(task)),
       events: this.db.all('SELECT * FROM events WHERE project_id = ? ORDER BY id DESC LIMIT 80', [projectId]),
       scans: this.db.all(
         'SELECT * FROM scan_files WHERE project_id = ? ORDER BY scanned_at DESC, file_path ASC',
@@ -1423,6 +1425,21 @@
   };
 }
 
+function taskSnapshotRow(task) {
+  if (!task) return task;
+  const startedAt = normalizeOptionalString(task.started_at) || null;
+  const finishedAt = normalizeOptionalString(task.finished_at) || null;
+  const isRunning = task.status === TASK_EVENT_STATUS.RUNNING;
+  const runDurationMs = isRunning ? taskRunDurationMs(startedAt, nowIso()) : undefined;
+  return {
+    ...task,
+    started_at: startedAt,
+    finished_at: finishedAt,
+    duration_ms: normalizeDurationMs(task.duration_ms),
+    ...(runDurationMs !== undefined ? { run_duration_ms: normalizeDurationMs(runDurationMs) } : {}),
+  };
+}
+
 function normalizeDurationMs(value) {
   const number = Number(value);
   if (!Number.isFinite(number) || number <= 0) return 0;
diff --git a/src/renderer/types.ts b/src/renderer/types.ts
index 85a2543c48dde7a29f616e3becc94f06b5de3bee..4741464c46507c9daa325e47bef2af8cb05d62b1
--- a/src/renderer/types.ts
+++ b/src/renderer/types.ts
@@ -197,6 +197,7 @@
   started_at: string | null;
   finished_at: string | null;
   duration_ms: number;
+  run_duration_ms?: number;
   codex_session_id: string | null;
   updated_at: string;
   /** JOIN plans 得到 */

tokens used
116,130
已完成 P003，未修改 plan 文件、未勾选 checkbox。

- `src/loopService.js:1316` 将 `snapshot().tasks` 查询结果映射为任务快照行，保持原排序不变。
- `src/loopService.js:1428` 新增快照任务字段规范化：`started_at`、`finished_at`、`duration_ms`，运行中额外暴露 `run_duration_ms`。
- `src/renderer/types.ts:197` 同步 `PlanTask` 类型，新增可选 `run_duration_ms`。
- 已验证：`node --check src\loopService.js`
- 已验证：`npx tsc --noEmit --pretty false`
